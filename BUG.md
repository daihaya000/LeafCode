# BUG インベントリ

> 対象リポジトリ: OpenCodeWebUI → LeafCode（`web/` + `host/` + `browser-bridge/`）
> このファイルは**発見記録のみ**。修正は本ファイルを参照して別途行う（修正禁止）。
> バグではなく構造・リファクタリングの改善余地は [IMPROVEMENT.md](./IMPROVEMENT.md) を参照。

---

# 状態一覧（BR-1〜20・ターン 37 時点・最終版）

| ID | 優先度 | 状態 | 対象 |
|----|--------|------|------|
| BR-1 | 高 | ✅ 修正済み | host index.js の removeBrokenWebBuild import 漏れ |
| BR-2 | 中 | ✅ 修正済み | TaskView テストの localStorage 旧キー期待 |
| BR-3 | 中 | ✅ 修正済み | db.memory-migration テスト前提乖離 |
| BR-4 | 低 | 🔬 反証（クローズ候補） | SettingsView タブ切替の編集喪失（jsdom 実測で onBlur 保存が機能） |
| BR-5 | 中 | ✅ 修正済み | CLI sync の isDistributable 未注入 |
| BR-6 | 中 | ✅ 修正済み | stopOpencodeOnly の dispose 欠落 |
| BR-7 | 中 | ✅ 修正済み | lock-file 内部呼び出しの引数欠落 |
| BR-8 | 中 | ✅ 修正済み | api-guard-coverage の @/lib/ re-export 未追跡 |
| BR-9 | 低 | ✅ 修正済み | production-webui-build-guard の export 公開 |
| BR-10 | 中 | ✅ 修正済み | workflow.integration の mock 未更新 |
| **BR-11** | 高 | ✅ **修正済み（`9a641a5c`・別セッション）** | instrumentation.ts の register() から起動処理 7 種が消失 — 7 処理全て復元 + env shim 維持 + NEXT_RUNTIME チェック（0dc01130^ と同一構造・推奨方針どおり） |
| **BR-12** | 中 | ⬜ **未修正** | Host ヘッダ偽装による control-plane のセッションなし admin 昇格（LEAFCODE_HOST=0.0.0.0 時・実測済み） |
| **BR-13** | 低 | ⬜ **未修正** | README のビルドミラーパス記述が旧名のまま |
| **BR-14** | 低 | ⬜ **未修正** | ProfilesSettings の「OpenCode host」（二重置換の取り違え・1 箇所のみ） |
| **BR-15** | 中 | ⬜ **未修正** | dataDir rename 後に profiles.json / DB worktree_path の絶対パスが旧名残存（切替 409・worktree ワークスペース再開失敗・実測済み） |
| **BR-16** | 低 | ⬜ **未修正** | docs/specs の 5 仕様書がデータディレクトリを `%APPDATA%\opencode-webui` と記載したまま |
| **BR-17** | 低 | ⬜ **未修正** | proxy.ts:133 の 403 エラーのみ「disabled in WebUI」残存（oc-server.ts は LeafCode 済み・文言不統一） |
| **BR-18** | 低 | ⬜ **未修正** | host ログ文言に「WebUI」が 5 箇所残存（2dd9bbeb のログ文言統一からの漏れ） |
| **BR-19** | 低 | ⬜ **未修正** | ブラウザ拡張ポップアップの日本語文言と build guard のコンソールメッセージに「WebUI」残存 |
| **BR-20** | 低 | ⬜ **未修正** | 通知のフォールバックタイトル「OpenCode タスク」が残存（タスクタイトル空時のみ表示） |
| **BR-21** | 低 | ⬜ **未修正** | IMPROVEMENT.md / OPTIMIZATION.md のヘッダ「対象リポジトリ: OpenCodeWebUI」が旧名のまま |

**修正推奨順**: BR-12（中・セキュリティ）→ BR-15（中・移行後機能）→ BR-13 / BR-14 / BR-16 / BR-17 / BR-18 / BR-19 / BR-20 / BR-21（低・表示/ドキュメント）。BR-4 はクローズ可（恒久テスト化する場合のみ SettingsView.test.tsx へ「フォーカス中タブ切替で保存が走る」ケースを追加）。**BR-11 は別セッションの `9a641a5c` で修正済み**（ターン 35 で git log 確認）。
**未修正 10 件のうち実測済み**: BR-12（一時テスト）/ BR-15（一時テスト）。BR-13 / BR-14 / BR-16 / BR-17 / BR-18 / BR-19 / BR-20 / BR-21 はコード・ドキュメント確認のみ。

# rebrand 追跡調査の完了サマリ（ターン 44・最終版）

**調査範囲**: rebrand 8 コミット（`5c17cc20` UI/UA/内部識別子・`802bfe8a` npm 名/SW キャッシュ/GitHub 参照・`fe439fc5` データディレクトリ移行・`0dc01130` env リネーム shim・`a1316e3f` ランチャー/FW/MCP/ビルドミラー/ログ prefix・`5f260f65` + `2dd9bbeb` 表示文言の二重リネーム・`27ce8787`/`3ea008f8` アイコン）+ auth 2 コミット（`af543e20` ログイン強制 / `ec47229c` ループバック admin）+ next 16.3.1 更新。全 rebrand コミットの差分ファイルを漏れなく精査（env shim・dataDir 移行・package 名・FW 規則・テスト追従・e2e・ドキュメント・.gitignore・プロンプト・通知・拡張 UI・実データ DB と host.log まで）。

**発見 11 件**（BR-4 反証含む）:
- **機能回帰 1**: BR-11（高）instrumentation.ts の起動処理 7 種消失（goal-loop/workflow scheduler・hang watchdog・メモリ自動抽出・git 復元・deps 配布・API 世代リゾルバ）。影響は機能別に確定（workflow は start/resume 1 tick のみで自動進行停止・goal loop は reactive tick で部分稼働）
- **セキュリティ 1**: BR-12（中）Host ヘッダ偽装による control-plane のセッションなし admin 昇格（`LEAFCODE_HOST=0.0.0.0` 時・一時テストで実証）
- **移行機能 1**: BR-15（中）dataDir rename 後に profiles.json / DB worktree_path の絶対パスが旧名残存（一時テストで実証・実 DB で未顕在化を裏付け）
- **表示/ドキュメント 7**: BR-13（README ミラーパス + 改名移行中の矛盾表現）/ BR-14（「OpenCode host」二重置換の取り違え・唯一の名前誤用）/ BR-16（docs/specs 5 ファイルの旧データパス）/ BR-17（proxy.ts 403 文言）/ BR-18（host ログ 5 箇所）/ BR-19（拡張ポップアップ + build guard コンソール）/ BR-20（通知フォールバック「OpenCode タスク」）
- **反証 1**: BR-4（SettingsView タブ切替の編集喪失 → jsdom 実測で onBlur 保存が機能・クローズ候補）

**健全確認（バグではないと確認した項目）**: env shim の全 entry point 適用（host / next.config / instrumentation / MCP / bat / Launcher.cs）・dataDir 移行の host/web 両側実行・npm パッケージ名 + lock 全追従・FW 規則名の orphan 除去整合（旧 add 名と delete 名一致）・e2e セレクタと実 UI 整合・goal-loop プロンプトマーカーとログ source 名 `webui` の内部識別子としての維持（変更リスクあり）・二重リネームの全数突き合わせ（誤りは BR-14 のみ）・.gitignore 追従・next 16 での instrumentation register 有効（BR-11 修正の前提）・host/browser-bridge テスト追従（env 名のみで意味不変）・`GITHUB_REPO = daihaya000/LeafCode` の実在確認（webfetch・public）・動的タブタイトル/エラーページ整合・OR-1〜23 との関連なし（BR-16/OR-16 のみ整合注意）・実環境 host.log の起動正常・LoginGate 強制遷移のループなし（全体ラップ + fetchAuthRequirement フェイルセーフ）・audit IP（x-ocw-client-ip）の偽装耐性（Caddyfile に trusted_proxies なし = Caddy が XFF を実 IP に置換）・実物 Caddyfile の host-only handle 5 パス設定・/api/remote プレースホルダーと devcontainer 実装・`webui` を含む内部識別子 9 件（ファイル名・API パスは破壊的変更のため維持）・leafcode/LeafCode/LEAFCODE の大小文字使い分け（パス小文字・表示 Pascal・env 大文字で意図的）・CI ワークフロー（旧名ゼロ・node 24 統一で OR-17 解消）・config.json と docs 残り（旧名は BR-16 の既知 1 行のみ）。

**検証**: web vitest 327 files / 3943 tests・host 480 本・browser-bridge 91 本・tsc --noEmit — 全てクリーン（調査差分は BUG.md のみ・一時テストは全て削除済み・各ターンでコミット済み）。

**残課題**: 未修正 10 件（**BR-11 は別セッションの `9a641a5c` で修正済み** — ターン 35 の git log で確認。7 処理全て復元 + env shim 維持。`61135b26`（OneDrive クラウドファイルのミラー修正）も別セッションの修正で host テスト 474→480 本の増加要因）。修正推奨順: BR-12（中）→ BR-15（中）→ BR-13/14/16/17/18/19/20/21（低）。修正時は本ファイルの各 BR の「→」以降の修正方針を参照すること。**調査はターン 44 時点で完了**（BR-21 まで記録・健全確認 25 項目・検証クリーン・MEMORY.md にローカル記録済み）。

---

# 2026-08-14 rebrand（サービス名変更: WebUI → LeafCode）後の追跡調査

> 検証環境: git HEAD `ec47229c`（クリーン）、Windows / OneDrive
> スコープ: `5c17cc20`〜`ec47229c` の rebrand 8 コミット（UI 文言・User-Agent・SW キャッシュ・npm 名・
> データディレクトリ移行・env リネーム shim・ランチャー/FW/MCP 名・GitHub 参照）+ auth 2 コミット
> （`af543e20` ログイン強制 / `ec47229c` ループバック admin）+ next 16.3.1 更新。
> 検証: web vitest **327 files / 3943 tests 全 PASS** / host `node --test` **480 本 PASS** /
> browser-bridge **91 本 PASS** / `tsc --noEmit` クリーン。本ラウンドは**発見記録のみ・修正しない**。
> **最終検証（ターン 18・HEAD `5d592919` 時点の再実行）**: web 327 files / 3943 tests PASS（1 skipped・52s）/
> host 480 本 PASS（fail 0・63s）/ browser-bridge 91 本 PASS。調査によるソース変更は BUG.md のみで、
> 全スイートがクリーンなことを再確認。

| ID | 優先度 | 要約 | 根拠 / 影響 |
|----|--------|------|-------------|
| **BR-11** | 高 | **`0dc01130`（env リネーム）で `web/src/instrumentation.ts` の `register()` が env shim に書き換えられ、サーバー起動時のバックグラウンド処理 7 種が全て失われた** | 分割前（`0dc01130^`）の `register()` は 7 処理を実行していた: (1) `startGoalLoopScheduler()` (2) `startWorkflowScheduler()` (3) `startHangWatchdog()` (4) `startMemoryAutoExtractionMonitor()` (5) `void runStartupGitRestore()` (6) `installDependenciesOnStartup()` (7) `registerServerOpenCodeApiGenerationResolver(readServerOpenCodeApiGeneration)`。`0dc01130` で register() の中身が `normalizeWebuiEnv()` のみになり、**7 処理の呼び出しが全て消滅**（HEAD でも非テストコードからの呼び出しゼロを `rg` で機械確認。各関数は定義のみ残存）。影響: **(1) goal loop の自動進行停止**（`goal-db.ts` の DB 書き込みイベント経由の reactive tick `:160,249,289` のみ残存で、時間ベースのスケジュール・待機中の進行は止まる）、**(2) workflow の interval ドライバ停止**（`/api/tasks/[id]/workflow` の start/resume 時 tick のみ残存）、**(3) ハング検知・自動再開の完全停止**、**(4) 会話完了後のメモリ自動抽出の停止**、**(5) zip 配布インストールの起動時 git 復元の停止**、**(6) プロファイルへの WebUI deps 自動配布の停止**、**(7) サーバー側 API 世代が常に v1 に固定**（v2 設定ユーザーでクライアント v2 / サーバー v1 の**世代混在**が発生しうる）。**テストは全 3943+474+91 本 PASS のまま**（起動経路はユニットテストがカバーしないため検出されず）。`0dc01130` のコミットタイトルは「環境変数を LEAFCODE_* へリネーム」であり、rebrand の巻き添えで起動処理が誤削除された可能性が高い（登録を他へ移した痕跡なし）。→ `register()` に 0dc01130^ 相当の 7 処理を復元（env shim と併存）。**機能別影響の詳細（ターン 5 精査）**: **(a) workflow — 重大**: `runWorkflowSchedulerTick` の非テスト呼び出しは `src/app/api/tasks/[id]/workflow/route.ts:120`（start/resume アクション）**のみ**。ノード完了の検知（`processRunningAttempt`）・次ノード dispatch（`readyAttempts`）・`recoverInterruptedAttempts`（クラッシュリカバリ）は全て `startWorkflowScheduler` の interval/初期化に依存（`workflow-scheduler.ts:689-696`）→ **start/resume 直後の 1 tick で最初のノード群が dispatch されるだけで、ノード完了後の自動進行と再起動後のリカバリが停止**。 **(b) goal loop — 部分停止**: `goal-db.ts` の DB 書き込み時の reactive tick（`:160` createGoalLoop / `:249` updateGoalLoopStatus / `:289` applyAssistantResult 経由）でターン進行は駆動されるが、transient error のリトライ・時間ベース進行・DB イベントのない待機ループのリカバリは interval tick 依存（`goal-scheduler.ts:394-400`）→ 通常フルランは動くが異常系で止まり得る。 **(c) hang watchdog — 完全停止**: interval のみ（`hang-watchdog.ts:664-666`）。 **(d) memory auto-extract — 完全停止**: `startMemoryAutoExtractionMonitor`（`memory-auto-extract.ts:312`）は SSE モニター接続の確立そのもの → 起動されないためイベント受信が存在しない。 **(e) git restore / deps 配布 / API 世代リゾルバ — 完全停止**。**✅ 修正済み（`9a641a5c`・別セッション・ターン 36 で git log 確認）**: register() に 7 処理全て復元 + env shim 維持 + NEXT_RUNTIME チェック（0dc01130^ と同一構造）。最終検証（ターン 17）は修正込みで全 PASS 済み。 |
| **BR-12** | 中 | **`ec47229c`（ループバック admin）で、BFF の `isLocalHostRequest`（Host ヘッダ + X-Forwarded-For ヒューリスティック）への依存が control-plane の「セッションなし admin」に昇格した。`LEAFCODE_HOST=0.0.0.0`（LAN 直接バインド）時、LAN クライアントが `Host: 127.0.0.1:3000` を偽装すると BFF が `x-ocw-local-request: 1` を付与し、control-plane が admin セッションなしで管理操作を許可する（権限昇格）** | `web/src/lib/local-request.ts` の `isLocalHostRequest` は「Host ヘッダがループバック名 + XFF なし（または XFF 先頭がループバック）」のみで判定し、**実ソケットの remoteAddress は見ない**（`web/src/lib/local-request.ts:94-116`）。BFF が 0.0.0.0 にバインドされていると LAN クライアントは直接接続でき、Host ヘッダは任意偽装可能（HTTP/1.1 仕様）→ `hostForwardHeaders` が local フラグを付与 → `host/src/control-server.js` の `isLocalRequest`（`:30-36`、値 `'1'` のみ判定）がセッションなし admin を許可（`/users` POST/DELETE・`/auth/config` POST・`/browser/config` GET/POST）。**昇格の性質**: `af543e20` 以前は spoofed-local でも管理系操作は admin セッション必須だったが、`ec47229c` で「local = admin」が管理系に拡張された。**実測（ターン 4・br12-probe 一時テスト 3 本全 PASS、実行後に削除）**: (1) `Host: 127.0.0.1:3000` の偽装リクエストで `isLocalHostRequest` = true → `hostForwardHeaders` が `x-ocw-local-request: 1` を付与（= 昇格チェーン成立）、(2) コントロール: 正規 LAN Host はヘッダなし、(3) Caddy 経路（Host ループバック + XFF=LAN）はヘッダなし（既存の fail-safe が機能）。control-server 側は既存テスト（"POST /users treats a loopback caller as admin without a session" 等、host 474 本 PASS）がセッションなし admin を実証済み。**デフォルト構成（`LEAFCODE_HOST=127.0.0.1` バインド + Caddy 経由）では到達不能**（Caddy は X-Forwarded-For に実 IP を付与するため LAN クライアントは local 判定されない。deploy/Caddyfile.example:75-88 のコメントも同様の設計前提）。control-plane 本体は 127.0.0.1 バインド + DNS rebinding ガードで直接攻撃は不可。つまり**条件付き（LAN 直接バインドの明示オプトイン時）で成立する権限昇格**。→ `isLocalHostRequest` に実接続元アドレス検証を追加（例: BFF の `req.socket.remoteAddress` / 信頼済みプロキシの明示）か、`x-ocw-local-request` を HMAC 署名付きトークンに変更 |
| **BR-4（再確認）** | 低 | **SettingsView 上位タブ切替の未保存編集喪失 — jsdom 実測で反証（onBlur は発火し保存される）** | 今回の rebrand コミット群では SettingsView.tsx は文言変更のみ（`5f260f65` / `2dd9bbeb`）。条件付きマウント構造（`{activeTab === "engine" && ...}` 20 箇所）は不変。**ターン 3 で一時検証テスト（br4-probe、3 本全 PASS、実行後に削除）により反証**: (1) ハング判定時間 input を `3` に変更 → フォーカス中にエンジンタブへフォーカス移動 + click → アンマウント確認 → `commitHangTimeout` が実行され `PUT /api/settings/hang-timeout` が発火（jsdom が blur/focusout を発火し React onBlur が捕捉）、(2) 同タブ内フォーカス移動コントロールも発火、(3) localStorage `webui:hang-timeout` = 180000 で commit 実行を確定。SettingsView のタブは `<button>`（focusable・`tabIndex` 付き）のため、実ブラウザの mousedown でも同様にフォーカス移動 → blur → commit が起きると判断。**残る理論的リスク（実害極小）**: タブ切替がクリック以外の手段（フォーカス移動を伴わない）で起きる場合のみ編集が失われるが、現実装のタブ切替は `setActiveTab`（onClick）のみで該当なし。→ BR-4 はクローズ候補（テストとして恒久化する場合は SettingsView.test.tsx に「フォーカス中タブ切替で保存が走る」テストを追加） |
| **BR-13** | 低 | **README.md の「production build のミラー」の既定パス記述が旧名 `%LOCALAPPDATA%\opencode-webui\build\...` のまま（実装は `%LOCALAPPDATA%\leafcode\build\...`）** | README.md「production build のミラー」節は `%LOCALAPPDATA%\opencode-webui\build\<インストール名>-<ハッシュ>` と記載。実装（`scripts/web-build-mirror.mjs:84`）は `a1316e3f` で `leafcode` に変更済み。**README のみ更新漏れ**。ユーザーが README のパスを参照してビルドミラーを探すと旧パスへ導かれる（実害は軽微・ディスク上の残存ディレクトリとの混同）。同じく README の「改名移行中」説明で「データ保存先 %APPDATA%\opencode-webui（初回起動時に %APPDATA%\leafcode へ自動移行）は互換性のため移行前の名称を引き継ぎます」とあり、移行済みなら leafcode が正だが説明が紛らわしい（表現のみ）。→ README のミラーパス記述を leafcode に更新、データ保存先の説明を現状（leafcode に移行済み）に合わせる。**追加確認（ターン 27）**: README の該当 3 箇所を精査 → (a) `:13`「改名移行中」の「実行ファイル名（LeafCode.exe）とデータ保存先 %APPDATA%\opencode-webui（初回起動時に %APPDATA%\leafcode へ自動移行）は互換性のため移行前の名称を引き継ぎます」は**表現が矛盾**（実行ファイルは新名 LeafCode.exe・データ保存先は移行後に leafcode が正なのに「移行前の名称を引き継ぐ」と読める）、(b) `:102` env 変数表の `LEAFCODE_BUILD_DIR` は `%LOCALAPPDATA%\leafcode\build\...` で**整合**、(c) `:223` は旧パス `%LOCALAPPDATA%\opencode-webui\build` のまま（BR-13 の主対象）。→ 修正時は :13 の一文も「LeafCode.exe に変更済み・データ保存先は初回起動時に自動移行」と明確化する |
| **BR-14** | 低 | **`ProfilesSettings.tsx:418` の「OpenCode hostを再起動してください」は rebrand 二重置換の取り違え（「WebUI依存」→「LeafCode依存」の置換と同時に「LeafCode host」が誤って「OpenCode host」へ戻された）** | 経緯: `5f260f65` で `"WebUI依存を追加しました。LeafCode hostを再起動してください。"` に変更 → `2dd9bbeb` で「WebUI依存」を「LeafCode依存」に置換した際、**同じ行の「LeafCode host」も「OpenCode host」へ戻された**（`git show 2dd9bbeb -- ProfilesSettings.tsx` で確認・現 HEAD の `:418` に確定）。意味は「LeafCode 連携依存を追加したので**トレイホスト**（ログ prefix `[leafcode-host]`、`host/src/index.js:483`）を再起動して反映」であり、host はエンジン（OpenCode）ではない。2dd9bbeb の他ファイルの「戻し」（OpenCode の再起動 / OpenCode CLI 等）はエンジン参照で正しく、**「OpenCode host」のみが唯一の取り違え**（`git grep "OpenCode host|LeafCode host|WebUI host"` で 1 箇所のみ）。**ターン 8 の全数検証で確定**: 5f260f65 の「OpenCode→LeafCode」全置換行を現 HEAD と突き合わせた結果、(a) 現 HEAD の「LeafCode の再起動」3 箇所（`EngineSettingsTab.tsx:41,222,275`）は全てフロントエンド（webui ターゲット）参照で正しい、(b) CommandCodeCliProxyAuth / CursorCliProxyAuth（反映には OpenCode の再起動＝CLI Proxy 設定はエンジン側）/ ModelRankingSettings（コストは OpenCode の報告費用）/ HostLogPanel（OpenCode / LeafCode / Caddy の 3 元）/ VisionSettings（OpenCode 登録モデル・OpenCode へのプロバイダー登録＝qwen-native 実装と一致）/ ProfileAgentsSyncSettings（マスターは `~/.config/opencode/AGENTS.md` なので「(OpenCode)」が正）の戻しは**全て正しい**。→ 誤りは BR-14 の 1 箇所のみ。表示メッセージのみ・機能影響なし。修正時は「OpenCode host」→「LeafCode host」（再起動対象はトレイ host） |
| **BR-15** | 中 | **`migrateLegacyDataDir()` の rename 経路で、プロファイルレジストリ（`profiles.json`）内の内部プロファイル path が旧名 `%APPDATA%\opencode-webui\profiles\...` のまま残り、移行後にプロファイル切替が「設定ディレクトリとして認識できません」で失敗する** | レジストリはプロファイル path を**絶対パス**で記録する（`web/src/lib/profiles/registry.ts:94` の `makeProfile` → `path.resolve(profilePath)`）。`migrateLegacyDataDir()`（`scripts/lib/data-dir.mjs:53-67`）は `%APPDATA%\opencode-webui` → `%APPDATA%\leafcode` の **rename のみ**で、レジストリファイルの中身（絶対パス）を書き換えない。移行後、`readState()` は leafcode\profiles.json を読むが、path は旧パス（存在しない）のまま。`activate()`（`web/src/lib/profiles/service.ts:165-170`）は `dirExists(profile.path)` が false なら 409「設定ディレクトリとして認識できません」を返すため、**rename 経路の移行直後に内部プロファイルの切替・編集・削除が失敗する**（データは leafcode\profiles に移動済みなので消失なし・再登録で回復）。**実測（ターン 9・br15-probe 一時テスト 1 本 PASS、実行後に削除）**: 旧ディレクトリ構成（opencode-webui\profiles\main + 旧パスを記録した profiles.json）を作成 → `migrateLegacyDataDir()` = true → ディレクトリは leafcode\profiles\main に移動成功 → `readState()` の profiles[0].path は**旧パスのまま**で `existsSync` = false を確認。**この環境では未顕在化**: ユーザーの「default」プロファイルは移行後に新規作成されたため新パス（`leafcode\profiles\default`）、旧パスは「default（移行前バックアップ）」として `external: true` で意図的に登録されている（opencode-webui ディレクトリも残存）。→ 修正方針: `migrateLegacyDataDir()` 成功時にレジストリの profiles[].path のうち旧 dataDir 配下のものを新名へ置換して保存する（external は対象外）、または readState / プロファイル解決時に「存在しない path が旧 dataDir 配下なら新 dataDir へ置換」するフォールバックを追加。**同種の影響範囲（ターン 10 精査）**: dataDir 基準の絶対パスを保存するデータは profiles.json 以外にもある。(a) **`webui.db` の `workspaces.worktree_path`**: `resolveWorktreeDir`（`workspace-service.ts:45`）が `dataDir()/worktrees/<projectId>/<slug>` の絶対パスを DB に保存。rename 後に旧パスになり、ワークスペース削除時（`:337-349`）は `removeWorktree` 失敗 → `existsSync` false → `git worktree prune` のフォールバックで概ね機能（実害軽減あり）。(b) セッション復元（`project-session-sync.ts:127-216`）は worktreePath を allowlist 検証後に使用しており、旧パスが存在しない場合の挙動は**未確認**（復元失敗 or 再生成の分岐が要確認）。(c) `project-session-store` の manifest は `dataDir()/projects/<sha1>/sessions.json` の sha1 キーで rename 影響なし。(d) 実データ（この環境）の DB は移行後に新規作成されており worktree_path の旧パス残存は未顕在化。→ **BR-15 の修正は profiles.json だけでなく DB の worktree_path も対象**（migrate 時に DB の `UPDATE workspaces SET worktree_path = ...` で旧 dataDir 配下を置換、または解決層のフォールバック）。**実害の確定（ターン 11）**: (1) 通常運用（DB 正本）では `workspaces.absolute_path`（worktree 分離では worktree パスが入る）が `POST /api/tasks`（`route.ts:510-670` の 10+ 箇所）と `task-service.ts`（`:171,221,296` 等）で**エンジンへ directory として渡される**。rename 後は存在しないディレクトリになり、**worktree 分離・temporary_copy 分離の既存ワークスペースでセッション再開・新規タスクが失敗する**（current_folder はプロジェクトパスのため影響なし。worktree の実体は leafcode\worktrees に移動済みなので、新規ワークスペース作成で回復）。(2) DB リセット時のマニフェスト復元（`project-session-sync.ts:138-150`）は `isInside(worktreeBase, resolvedWorktree)` チェックで旧パスを「escapes root」と判定し**ワークスペースを復元スキップ**する（DB が正本の通常運用には影響しない）。**実データでの裏付け（ターン 18）**: `%APPDATA%\leafcode\webui.db` を better-sqlite3 読み取り専用で開き `workspaces` を全件確認 → 9 件全て `isolation: "current_folder"`・`worktree_path: null`（git_worktree / temporary_copy 分離は**この環境に存在しない**）。absolute_path は全てプロジェクトパス（dataDir 外）で rename の影響を受けない。→ BR-15 の影響対象行がこの環境の DB に無いことをデータで確認（「未顕在化」の根拠）。 |
| **BR-16** | 低 | **docs/specs の 5 仕様書がデータディレクトリを `%APPDATA%\opencode-webui` と記載したまま（実装は `%APPDATA%\leafcode` に移行済み）** | `git grep` で検出した現行仕様としての記載: `docs/opencode/provider-model-management.md:20`（provider-model-state.json）、`docs/specs/agent-enable-toggle.md:28`（agent-state.json）、`docs/specs/host-log-viewer.md:30`（DATA_DIR・**✅ バッジ付き検証済み仕様**）、`docs/specs/opencode-config-profiles.md:76,109,193`（profiles 集約先・レジストリ・移行カード）、`docs/specs/security-remediation-plan.md:197,238`（revoked-sessions.json / audit.log）。**実装は全て `dataDir()`（`%APPDATA%\leafcode`）に移行済み**（auth-store / browser-config / control-server / audit-log / profiles / agent-state を前ラウンドで確認）。README は「改名移行中」節で更新済みだが、docs/specs は更新漏れ。機能影響なし・開発者/ユーザーが旧パスを参照する誤情報。→ 各仕様書のパス記載を `%APPDATA%\leafcode` に更新（`dataDir()` 参照であることも明記） |
| **BR-17** | 低 | **`web/src/lib/opencode-proxy/proxy.ts:133` の 403 エラー文字列が「disabled in WebUI」のまま（同一エラーの `oc-server.ts:53` は「disabled in LeafCode」に更新済み）** | `git grep "writes are disabled"` で検出: `oc-server.ts:53` は 2dd9bbeb で `"OpenCode config/auth/mcp writes are disabled in LeafCode"` に更新済みだが、**`proxy.ts:133` は `"OpenCode config/auth/mcp writes are disabled in WebUI"` のまま**。同一の 403（OpenCode への config/auth/mcp 書き込み禁止）が経路（ocServer 経由 vs opencode-proxy 経由）で文言が異なる。2dd9bbeb「画面の呼称をWebUIからLeafCodeへ統一」の更新漏れ。機能影響なし・表示文言の不統一。**関連（記録のみ）**: `src/app/api/provider/commandcode/auth/route.ts:49` の `writeSecrets({ userId: "webui", userName: "WebUI", keyName: "webui" })` は CommandCode CLI 認証シークレットの内部識別子（外部ツール側に表示される可能性はあるが、既存シークレットとの互換を考慮すると変更は任意・内部用）。→ proxy.ts:133 を「LeafCode」に更新（oc-server.ts と統一） |
| **BR-18** | 低 | **host ログ文言に「WebUI」が 5 箇所残存（2dd9bbeb のログ文言統一からの漏れ）** | `host/src/index.js` のログ文字列で「WebUI」が残る箇所: `:760` / `:1947`（`WebUI failed to become ready after OpenCode port change`）、`:1624`（`WebUI build is stale but :${WEBUI_PORT} is held by an unknown process`）、`:1625`（`Reusing existing WebUI on :${WEBUI_PORT}`）、`:1629`（`Existing WebUI on :${WEBUI_PORT} is stale; stopping it to rebuild`）、`:1633`（`Could not free :${WEBUI_PORT} after stopping the stale WebUI`）。2dd9bbeb は同ファイルの他のログ文言（`Starting LeafCode` / `LeafCode exited` / `Restarting LeafCode` 等）を LeafCode に変更済みで、**上記 5 箇所のみ統一漏れ**。トレイ UI・設定 UI の表示文言は全て LeafCode 済み（ターン 1・8 で確認）。ホストログはトレイのログビューア（HostLogPanel）でユーザーが閲覧するため、rebrand の一貫性の観点で更新が望ましい。機能影響なし。→ 5 箇所の「WebUI」を「LeafCode」に更新（他のログ文言と統一） |
| **BR-19** | 低 | **ブラウザ拡張ポップアップの日本語文言と build guard のコンソールメッセージに「WebUI」残存** | (a) **拡張ポップアップ（ユーザー向け UI）**: `browser-bridge/extension/popup.html:23`（「WebUI の設定 → 拡張機能で、このペアリング要求を承認してください…」）と `popup.mjs:20`（「WebUI での承認を待っています…」）が「WebUI」のまま（テスト `popup.test.mjs:83` も期待値が同文言）。2dd9bbeb「画面の呼称をWebUIからLeafCodeへ統一」の対象外領域（browser-bridge は rebrand コミットで UI 文言の変更対象外だった）。(b) **build guard のコンソールメッセージ**: `scripts/production-webui-build-guard.mjs` の `[LeafCode]` メッセージ 6 箇所（`:216,228,239,246,254` 等・「Production WebUI is running」「Stop the running production WebUI」）が「WebUI」のまま（build.bat 実行時にユーザーが閲覧）。コメント・内部識別子（`WebUIAddon` 型・`userId: "webui"`・FW 規則名の orphan 除去用 delete 行 `allow-firewall-*.bat`）は意図的で対象外。機能影響なし・表示文言の統一漏れ。→ popup の 2 箇所と guard のメッセージを「LeafCode」に更新 |
| **BR-21** | 低 | **`IMPROVEMENT.md:3` / `OPTIMIZATION.md:3` のヘッダ「対象リポジトリ: OpenCodeWebUI」が旧名のまま** | 両インベントリ文書のヘッダ行のみ旧名（BUG.md のヘッダはターン 1 で「OpenCodeWebUI → LeafCode」に更新済み）。**他の旧名出現は全て履歴的言及で変更不要**: IMPROVEMENT.md `:586-587,674,842`（D2 当時のパス・BH-2 修正記録で `opencode-webui-v6` は過去の値として正しい）、OPTIMIZATION.md `:72,73,76`（OR-11/12/15 の当時記録・対応完了済み）、REFACTORING_PLAN.md `:155-160,422,432`（D2 計画当時の決定）。履歴文書は当時の状態を記録するもので、書き換えると記録の正確性を損なう。→ ヘッダ 2 行のみ「LeafCode」に更新 |
| **BR-20** | 低 | **通知のフォールバックタイトル「OpenCode タスク」が残存（`web/src/lib/notify.ts:34`）** | `notificationText()` は `const name = title || "OpenCode タスク"` で、**タスクタイトルが空の場合に通知タイトル（承認が必要です / タスクが完了しました）の body に「OpenCode タスク」を表示**する。rebrand 後は「LeafCode タスク」であるべき（この WebUI のタスクを指すため・2dd9bbeb の統一対象）。呼び出し元 `TaskView.tsx:3716` は `task?.title ?? ""` を渡すため、タイトル無しタスクで発現する。拡張 manifest.json（`browser-bridge/manifest.json`）は「LeafCode Browser Bridge」に rebrand 済み（`a1316e3f`・確認済み）。機能影響なし・表示文言の更新漏れ。→ notify.ts:34 の「OpenCode タスク」を「LeafCode タスク」に更新 |

> **旧 BR-1〜10 の解決確認（2026-08-14 追跡）**: 前回（HEAD `b065870`）の記録は**全て現 HEAD で解決済み**。
> - **BR-1（`removeBrokenWebBuild` 未 import）**: 修正済み（`host/src/index.js:32` に import あり、`git grep` で確認）。
> - **BR-2（TaskView 旧キー期待 2 件）/ BR-3（memory-migration 前提乖離）/ BR-8（api-guard-coverage 検出低下）/ BR-9（host-control export）/ BR-10（workflow mock 未更新）**: 全て vitest 全 PASS（TaskView 149 本 / db.memory-migration 5 本 / api-guard-coverage 7 本 / host-control 18 本 / workflow.integration 2 本）で**テスト不整合は解消済み**。
> - **BR-5（CLI sync の isDistributable 未注入）**: 修正済み（`scripts/sync-profiles.mjs:70` に `isDistributable` 定義・`:78,93` で注入。`sync-engine.mjs` のデフォルトは `() => true` のまま）。
> - **BR-6（`stopOpencodeOnly` の dispose 欠落）**: 修正済み（`host/src/index.js:1700-1706` で `opencodeUrl: OPENCODE_URL` を注入）。
> - **BR-7（lock-file 内部呼び出しの引数欠落）**: 修正済み（`9be2b58c`「fix: lock-file 内部呼び出しの引数伝搬を修正し unlink を復活（BR-7）」）。
> - **BR-4**: ターン 3 で jsdom 実測により反証（上表 BR-4 更新を参照）。通常のタブ切替操作では onBlur → 保存が実行される。

> **rebrand ラウンドの健全確認メモ（本ラウンドで「バグではない」と確認した再調査防止メモ）**
> - **env shim の適用網羅性**: `normalizeWebuiEnv()` は全 entry point（host `index.js:7` / `next.config.ts:9` / `instrumentation.ts:13` / `build-web.mjs:9` / `production-webui-build-guard.mjs:12` / `smoke-browser-bridge.mjs:6` / `host-control.mjs:54` / browser-bridge の server.mjs・memory-server.mjs）で呼ばれ、start-webui.bat も 15 変数の手動 shim を実行。Launcher.cs は LEAFCODE_NONINTERACTIVE 優先 + 旧名フォールバック（コメントで恒久互換を明示）。host の `env-compat.test.js` 3 本 PASS。→ 健全。
> - **データディレクトリ移行**: `migrateLegacyDataDir()` は host `ensureDataDir`（`index.js:1334`）と web `ensureDataDir`（`paths.ts:12`）の両方から実行。rename 失敗時は次回起動で再試行（swallow）。`paths.test.ts`（migrate 4 ケース）PASS。auth-store / auth-config / browser-config / trusted-device-store / control-server（revoked-sessions）/ memory-server は全て共有 `dataDir()` に統一。→ 健全。
> - **ビルドミラー名**: `web-build-mirror.mjs:84` は `%LOCALAPPDATA%\leafcode\build\<slug>` に更新済み（`a1316e3f`）。旧 `%LOCALAPPDATA%\opencode-webui\build` は残存するが stale 扱いで再ビルドされ実害なし（削除処理は無し）。
> - **旧名の残存参照**: `git grep -n "opencode-webui|OpenCodeWebUI|OPENCODE_WEBUI"` の全ヒットは意図的互換のみ（env shim / browser-bridge env 両出し `host/src/browser-bridge.js:40-41` / テスト・テンポラリプレフィックス / project-meta 除外リスト / worktrees 正規表現 `/opencode-webui|leafcode/`）。**書き込みパス・実行パスに旧名が残る箇所はゼロ**。→ 健全。
> - **Provider models グローバル識別子リネーム**（`__opencodeWebui*` → `__leafCode*`）: 全参照が同一コミット内で一括リネーム済み（diff 確認・機械照合）。`provider-models.test.ts` 52 本 PASS。→ 健全。
> - **ロジックファイルの文言置換**: `jsonc-edit.ts` / `access-mode.ts` / `oc-server.ts` / `goal-prompt.ts` / `tasks/route.ts` / `updates/*` / `copy.ts` / `link.ts` の変更は全てユーザー向けエラー/表示文字列のみ（diff 確認）。テストで文字列マッチに影響なし。→ 健全。
> - **SW キャッシュ名**（`opencode-webui-v6` → `leafcode-v6`）: activate ハンドラが CACHE 以外の全キャッシュを削除するため旧キャッシュは自動クリーン。`sw.test.js` PASS。→ 健全。
> - **Firewall 規則名**（'OpenCode WebUI' → 'LeafCode'）: `allow-firewall-*.bat` は旧規則を削除してから新規則を追加（`a1316e3f`）。`windows-integration.js` の `FIREWALL_RULE_NAME` と整合（テスト PASS）。→ 健全。
> - **`LEAFCODE_WORKFLOW_GRAPH_EDIT` のデフォルト非対称**: start-webui.bat は `true`、host `spawnWeb` は `?? 'false'`。**rebrand 前（OPENCODE_WEBUI_ 時代）から存在する非対称で rebrand 起因ではない**（0dc01130^ と同一挙動）。host 直接起動時のみ Graph 編集が off。→ 既存仕様（変更なし）。
> - **未検証事項（解決済み・ターン 2）**: `GITHUB_REPO = "daihaya000/LeafCode"`（`install-root.ts:4`）の実在を **2026-08-14 webfetch で確認（public リポジトリ・README 記載・2053 commits）**。自己更新（`/api/updates/webui`）と git-restore の参照先は妥当。→ 健全。
> - **health の service 名リネーム（ターン 2）**: control-server の `service: 'leafcode-host'`（`control-server.js:408`）は production-webui-build-guard の host 判定（`production-webui-build-guard.mjs:93`）と整合。旧名 `opencode-webui-host` を期待する参照はゼロ（`git grep` で機械確認・テスト追従済み）。→ 健全。
> - **残り rebrand 差分の整合（ターン 2）**: `host/src/control-server.js`・`scripts/caddy-trust.bat`・`web/ConnectivitySettingsTab.tsx`（netsh 規則名 'LeafCode' は windows-integration と整合）・`browser-bridge/scripts/install-mcp.mjs`・`install-memory-mcp.mjs`・`web/playwright.config.ts`・`web/.env.example`・`workflow-feature.ts`・`host-control.ts`・`access/route.ts`・`client-ip.ts`・`opencode/[...path]/route.test.ts` の変更を全て確認。文言・env 名のみでロジック非干渉。→ 健全。
> - **README のビルドミラー記述ずれ（ターン 2）**: BR-13 として記録（README のみ旧パス `%LOCALAPPDATA%\opencode-webui\build` のまま）。実装は leafcode に更新済み。
> - **2dd9bbeb の全差分は表示文字列のみ（ターン 5）**: EngineSettingsTab.tsx（50 行・RESTART_LABELS / 接続状態 / アップデート / API 世代の文言）、goal-prompt.ts（プロンプト文言）、oc-server.ts / jsonc-edit.ts / access-mode.ts / TaskView / HomeView / Sidebar / GoalLoopPanel / opencode-restart / updates 2 ルート / tasks / ollama/setup を全て確認。2dd9bbeb は 5f260f65 の「OpenCode → LeafCode」一括置換のうち**エンジンを指していた文言を OpenCode へ戻す二重リネーム**であり、全てユーザー向け表示・エラー文字列のみでロジック非干渉（テスト追従済み・全 PASS）。→ 健全。
> - **goal-loop プロンプトマーカー `<!-- webui-goal-loop-prompt -->` の残存（ターン 5）**: `goal-util.ts:414` / `useSessionStream.ts:406` の `GOAL_LOOP_PROMPT_MARKER` と `goal-prompt.ts` 5 箇所が**同一文字列のまま整合**しており、プロンプト生成側とターン判定側（goal loop プロンプトかどうかの判定）が機能。HTML コメント（非表示）の内部マーカーで、**rebrand 後も「webui」名が残るのは意図的維持と判断**（既に実行中の履歴ループは旧マーカーで開始されており、変更すると新・旧プロンプトの判定が不整合になる。変更する場合は 7 箇所同時 + 旧セッション互換の考慮が必要）。機能正常・バグではない。→ 変更しないこと（再調査防止メモ）。
> - **e2e セレクタと実 UI の整合（ターン 6）**: `composer.spec.ts` / `smoke.spec.ts` の `getByRole("heading", { name: "LeafCode" })` は `HomeView.tsx:1306-1316` の `<h1><span>LeafCode</span></h1>` に一致、`toHaveTitle(/LeafCode/)` は `layout.tsx:18` の title と一致。manifest.ts の name/short_name も "LeafCode"。e2e の実実行は未実施（AGENTS.md の CI モード制約）だが、コード整合は確認済み。→ 健全。
> - **LoginGate の auth-required イベント整合（ターン 6）**: `AUTH_REQUIRED_EVENT = "leafcode:auth-required"`（`client.ts:23`）は `client.ts:36` の dispatch と `LoginGate.tsx:40` の addEventListener が同一文字列で整合。テスト（LoginGate.test 11 本 PASS）済み。**注意（低・命名不統一）**: 新イベントだけが「leafcode:」接頭辞で、既存イベント・localStorage キー（`webui:hang-timeout` / `webui:side-panel:*` 等）は「webui:」のまま（9-1b で意図的に維持）。機能には影響しない（dispatch/listen が同一なら動作）が、将来の「webui:」名前空間廃止時に混在する。→ バグではない（記録のみ）。
> - **e2e 全 spec の rebrand 網羅（ターン 13）**: `web/e2e` の 8 spec（composer / pty / session-title-refresh / sidebar / smoke / task / workflow-graph / workflow）を確認。UI テキスト・セレクタの旧名残りはゼロ（`git grep "OpenCodeWebUI|OpenCode WebUI|WebUI"` は smoke.spec.ts:3 と task.spec.ts:763 のコメントのみで、いずれもエンジン参照 or 説明文）。composer/smoke の `getByRole("heading", { name: "LeafCode" })` と `toHaveTitle(/LeafCode/)` は実 UI（HomeView h1 / layout.tsx title）と整合（ターン 6 確認済み）。`playwright.config.ts` は `LEAFCODE_WORKFLOW_*` に更新済み（`0dc01130`）。e2e 実実行は未実施（AGENTS.md の CI モード制約）。→ 健全。
> - **BR-11 修正の前提確認（ターン 14）**: (1) `next.config.ts` に instrumentation 設定は無く、Next 15+ では `instrumentation.ts` の `register()` がデフォルト有効（Next 16.3.1 でも同じ）。`web/src/instrumentation.ts` の `export async function register()` 形式は正しい。→ **BR-11 の修正（register() への 7 処理復元）は起動時に確実に実行される**。(2) **依存バージョン不整合（低・記録のみ）**: `web/package.json` は `"next": "^16.3.1"`（5b4f8760 で更新）だが `"eslint-config-next": "15.5.20"` のまま（Next 15 系）。`npx eslint`（eslint 9 + eslint-config-next 15.5.20）は representative 3 ファイルでエラーなし・正常動作を確認 → 実行時・lint とも機能に影響なし。Next 16 の新ルール（e.g. `@next/next` の新 checks）が適用されない可能性のみ。→ バグではないが次回 next 更新時に eslint-config-next も追随すべき。
> - **npm パッケージ名の rebrand 整合（ターン 15）**: root `leafcode` / host `leafcode-host` / browser-bridge `leafcode-browser-bridge` に変更済み（`802bfe8a`）。package-lock.json の name も全て整合（host・browser-bridge を node で実読確認）。root package.json に workspaces 設定なし（npm --prefix フラット構造）で子の name 参照は無し。全て `private: true` で registry への publish 影響なし。MCP エントリのコマンドは `browser-bridge/mcp/server.mjs` へのパス参照で package 名非依存（webui-dependencies.ts 確認済み）。→ 健全。
> - **FW 規則名の rebrand 整合（ターン 16）**: `scripts/allow-firewall-*.bat` / `fix-lan-access.bat` の「旧名 delete」は**意図的な orphan 除去**（a1316e3f のコメント「Remove the pre-rebrand rule name so it cannot linger as an orphan」）。旧 add 名との一致を `git show a1316e3f^` で確認: 3000 系 'OpenCode WebUI' / 8080 'OpenCode WebUI Caddy' / 8443 'OpenCode WebUI Caddy HTTPS' / Node 'OpenCode WebUI Node' — **全て delete 名と一致**し、新名（'LeafCode' / 'LeafCode Caddy' / 'LeafCode Caddy HTTPS' / 'LeafCode Node'）で再 add。`windows-integration.js` の `FIREWALL_RULE_NAME = 'LeafCode'` は allow-firewall-3000 の新名と一致。host テスト（firewallRuleExists / allowFirewallPort）PASS。→ 健全。setup-messages / shortcut-messages に旧名残りゼロ（git grep で確認）。
> - **host テスト残り 5 ファイルの追従（ターン 17）**: `opencode-upgrade.test.js` / `launcher-exe.test.js` / `production-webui-build-guard.test.js` / `start-webui-bat.test.js` / `web-build-mirror.test.js` の変更は全て env 名追従のみ（`LEAFCODE_NONINTERACTIVE` / `LEAFCODE_AUTO_UPDATE_OPENCODE` / `LEAFCODE_HOST_CONTROL_URL` / `LEAFCODE_PORT` / `LEAFCODE_CADDY` / `LEAFCODE_BUILD_DIR`）で**テストの意味不変**。全 474 本 PASS。→ 健全。
> - **名前整合の両方向網羅（ターン 17）**: 「LeafCode がエンジンを指す誤用」は 0 件（`git grep "LeafCode エンジン|LeafCode のプロバイダ|LeafCode CLI|LeafCode 本体|LeafCode の呼び出し"` で非テストコード 0 件）。「OpenCode がフロントエンド/host を指す誤用」は BR-14 の 1 箇所のみ（ターン 8 の全数検証）。→ 名前の取り違えは BR-14 のみで確定。
> - **.gitignore の rebrand 追従（ターン 18）**: `.leafcode/` を追加し旧 `.opencode-webui/` も除外維持（`fe439fc5`、pre-rebrand clone の漏れ防止として正しい）。`OpenCodeWebUI.exe.old` → `LeafCode.exe.old`（`a1316e3f`）。データディレクトリ（%APPDATA% 下）はリポジトリ外なので ignore 不要。→ 健全。
> - **実環境 host.log の確認（ターン 22）**: `%APPDATA%\leafcode\host.log`（524 行）を読み取り専用で確認。rebrand 後の起動は正常（`Production LeafCode build completed` → `Starting LeafCode (production) on 0.0.0.0:3000` → `LeafCode is ready` / `OpenCode is ready`）。「Failed to seed Caddyfile: ENOENT」は host テスト実行時の副作用ログ（`ocw-caddyfile-1M2Ks9` はテスト temp dir）で実運用エラーではない。「Turbopack build encountered 52 warnings」は Dynamic filesystem access 警告（rebrand と無関係・既存）。BR-11 の直接証跡（scheduler 系の起動ログ）は**確認できず**（start 関数群は起動ログを出力しないため・弱い証跡）。→ 起動経路は健全。
> - **ログ source 名「webui」の残存（ターン 22・記録のみ）**: host が子プロセス（next start）のログに付ける source タグは `'webui'` のまま（`host/src/index.js` の spawnWeb の recordLog source・`HostLogPanel.tsx:9` の `LogSource` 型に `"webui"`）。ログビューアの各行に `[webui]` と表示される（HostLogPanel は source をそのまま表示 `:183`）。2dd9bbeb の統一対象外（内部タグ）で、**source 名を変えると旧ログ（webui タグ）の表示・フィルタと不整合になるため変更リスクあり** → goal-loop マーカーと同様に**変更しないこと**（再調査防止メモ）。UI 文言（HostLogPanel.tsx の説明文「OpenCode / LeafCode / Caddy / ビルド」）は更新済み。
> - **「OpenCode」フォールバック名の網羅（ターン 24）**: 「OpenCode」を**デフォルト名・フォールバック名**として使う箇所は `notify.ts:34`（「OpenCode タスク」・BR-20）**のみ**（`git grep "OpenCode タスク|OpenCode プロジェクト|..."` で確認）。他の「OpenCode」出現は全てエンジン参照（AgentsSettings の「OpenCode の既定」（AgentScope 既定・正しい）/ コメントのエンジン説明）で rebrand 対象外。→ BR-20 が唯一のフォールバック名残り。
> - **動的タイトル・エラーページの整合（ターン 25）**: `TaskView.tsx:3627-3643` のタブタイトルは `task?.title ? \`${task.title} · LeafCode\` : "LeafCode"` で **LeafCode 整合**（フォールバック・アンマウント時の復帰も "LeafCode"）。`(app)/error.tsx` / `global-error.tsx` は LeafCode/OpenCode/WebUI の言及なし（汎用エラー文言）。`app/not-found.tsx` は存在せず Next デフォルト（rebrand 無関係）。layout.tsx の metadata は LeafCode（ターン 6 確認済み）。→ 健全。
> - **OR-1〜23 と BR-11〜20 の関連俯瞰（ターン 26）**: OPTIMIZATION.md の OR-1〜23 は**全て対応完了**と記録済み（コミット群 `2e1ea3e`〜`c79ff24`）。BR-11〜20（rebrand 起因）と OR-1〜23（Phase 0-7 リファクタの構造・テスト問題）は**独立した問題群**で、相互に起因関係なし。**BR-11 修正の OR への影響**: 7 処理の復元は OR-1〜23 のいずれにも干渉しない（OR-3 の goal-loop 循環 import は解消済みで、scheduler 再起動で循環が再発する構造ではない。OR-4 の useSessionStream はクライアント側でサーバー scheduler と無関係）。**唯一の関連**: OR-16（docs/specs のバッジ運用が部分的）と BR-16（docs/specs の旧データパス記載）は同じ docs/specs を対象とするため、**BR-16 修正時に OR-16 のバッジ運用とも整合を取ること**（バッジ付き仕様 host-log-viewer.md に BR-16 の誤記載が含まれる）。→ 修正時の相互影響なし（BR-16/OR-16 の整合のみ注意）。
> - **旧名の全領域最終スイープ（ターン 29）**: 旧名（`opencode-webui` / `OpenCodeWebUI` / `OPENCODE_WEBUI` / `OpenCode WebUI`）の残りを全領域で最終確認。`vendor/`・`deploy/`・`.github/` は**ゼロ**。`scripts/` の残りは全て意図的な互換（env shim `env-compat.mjs` / `start-webui.bat` の 15 変数 shim / FW 規則名 orphan 除去 `allow-firewall-*.bat` / `data-dir.mjs` の LEGACY 定数 / `Launcher.cs` の旧名フォールバック）で、ユーザー向け表示は無し。ブラウザ拡張の UI は `popup.html` のみ（options 等は存在せず・BR-19 で記録済み）。→ **ユーザー向け表示の旧名残りは BR-13〜20 で全て捕捉済み**と確定。
> - **LoginGate の強制遷移エッジケース（ターン 30）**: `LoginGate` は `(app)/layout.tsx:23` で**アプリ全体をラップ**し、ゲート表示中は children（アプリ本体）が非マウント → API 呼び出しが発生せず **auth-required イベントの再発・ループは構造的に発生しない**（未ログインで 403 が続いてもログイン画面に留まるだけ）。`fetchAuthRequirement()`（`auth.ts:48-66`）は catch で `loginRequired: true / authenticated: false` を返す**フェイルセーフ**で、API 障害時に無限スピナーにはならない（ログイン画面表示）。ログイン成功後はセッション（HMAC cookie）が有効なら即時失効しない。ログイン画面の h1 は「LeafCode」で整合。テスト（LoginGate.test 11 本 PASS）。→ 健全。
> - **audit IP（x-ocw-client-ip）の偽装耐性（ターン 31）**: `clientIpFromRequest`（`web/src/lib/client-ip.ts`）は X-Forwarded-For の**右端**を audit IP として control-plane へ転送（`auth/login/route.ts:38`）。右端 = 自インフラ（Caddy）が付けた値という前提は、**実物の `deploy/Caddyfile` に `trusted_proxies` 設定が無い**（PowerShell で確認・読み取りのみ）ため成立する: Caddy v2 は非信頼クライアントからの XFF を**置換**（自ら見た実 IP のみにする）し、攻撃者が送った左端値は破棄される → **audit IP の偽装・login-throttle の IP 回避は不可**。`LEAFCODE_HOST=0.0.0.0` 直接アクセス時は XFF なし → null（unknown）で、throttle は username ベースにフォールバック（「does not share one IP bucket among callers with no address」テスト済み）。BR-12 の Host 偽装リスク（isLocalHostRequest の Host ヘッダ依存）とは独立で、client-ip 経路は安全。→ 健全。
> - **実物 Caddyfile の host-only handle（ターン 32）**: README の注意書き「既存の deploy/Caddyfile の host 限定 API の handle に /api/host/logs* と /api/updates/* を追加」のとおり、**実物の `deploy/Caddyfile` に 5 パスが設定済み**（正規表現でパスのみ抽出・認証情報は非出力）: `/api/browse/folder*` / `/api/host/logs*` / `/api/host/restart*` / `/api/host/voice` / `/api/updates/*`。README 推奨の 2 パスは含まれており、ホスト PC 上の Caddy 経由アクセスで「this endpoint is only available from the host machine」が出る状態ではない。→ 健全。
> - **/api/remote と devcontainer（ターン 33）**: `/api/remote` は README 記載どおりの**未実装プレースホルダー**（`route.ts` GET: `supported: false` / POST: 501「remote workspace attach is not implemented」・guard 付き・意図的）。`/api/devcontainer` は実装済み（`5c17cc20` で文言のみ変更済み・ターン 1 で確認）。→ 健全。
> - **「webui」を含む内部識別子の網羅（ターン 36）**: `git ls-files` で「webui」を含むファイル名は 9 件（`docs/specs/setup-start-webui-merge.md` / `host/src/production-webui-build-guard.test.js` / `host/src/start-webui-bat.test.js` / `scripts/production-webui-build-guard.mjs` / `scripts/start-webui.bat` / `web/src/app/api/updates/webui/route.ts` + test / `web/src/lib/profiles/webui-dependencies.ts` + test）で**全て確認済み**（テストは全 PASS・BR 対象は記録済み）。**ファイル名・API パス・関数名の変更は破壊的**（`start-webui.bat` は `Launcher.cs` から参照、`/api/updates/webui` は API パスでクライアント/テストから参照、`webui-dependencies.ts` はビルド経路の import 元）で、機能に影響しない限り変更する価値は低い → **goal-loop マーカー・ログ source 名と同様に内部識別子として維持**（再調査防止メモ）。
> - **leafcode / LeafCode / LEAFCODE の大小文字使い分け（ターン 37）**: 3 種は**意図的な使い分け**で混在なし: (a) ファイルパス・データディレクトリは**小文字 `leafcode`**（`data-dir.mjs` の `DATA_DIR_NAME` 等・全パス参照が小文字で統一）、(b) 表示・識別子（トレイメニュー title / FW 規則名 'LeafCode' / ショートカット説明 / Console.Title / User-Agent / metadata・manifest name / タブタイトル / サービスステータス / waitUntilReady ラベル）は**パスカルケース `LeafCode`**、(c) 環境変数は**大文字 `LEAFCODE_*`**。実コードのパスに大文字 `LeafCode` は無し（テストデータ `C:\one\LeafCode` 等は識別子として使用・問題なし）。→ 健全。
> - **CI ワークフローの rebrand 追従（ターン 38）**: `.github/workflows`（ci.yml / encoding-check.yml）に旧名ヒット**ゼロ**。ci.yml は web（lint/typecheck/vitest・node 24）と e2e（smoke・node 24）で標準コマンド（npm ci / lint / typecheck / test / e2e:smoke・パッケージ名非依存）。encoding-check.yml も **node-version 24 に更新済み**（前ラウンド OR-17 で記録した「encoding-check は node 20、ci.yml は node 24 と不統一」は**解消済み**）・bat エンコードテスト + host テスト。→ 健全。
> - **config.json と docs の残り（ターン 39）**: ルート `config.json` はこのリポジトリ用の OpenCode エージェント設定（build タスクを deny・`$schema: https://opencode.ai/config.json`）で rebrand と無関係・問題なし。docs の残り（ui-components / verification / browser-bridge-setup / opencode/ 配下）の旧名は `provider-model-management.md:20` の**既知の 1 行のみ**（BR-16 で記録済み）。→ 健全。
> - **実データの旧名パス参照ゼロ（ターン 40）**: `%APPDATA%\leafcode\webui.db` の `settings` テーブルを読み取り専用で確認 → `key/value` に「open/webui/opencode-webui」を含む行は **0 件**（設定値にパス参照なし）。`provider-model-state.json` にも `opencode-webui` 参照なし（プロジェクトパスがキー・リポジトリ名 LeafCode とは無関係）。→ BR-15 の影響対象（旧パス残存）はこの環境の実データに**ゼロ**と最終確認。
> - **実データ JSON の一括確認（ターン 41）**: `%APPDATA%\leafcode` 配下の全 JSON 8 ファイルを「opencode-webui」参照の有無で確認（中身は非出力・機微情報保護）→ **`profiles.json` のみ参照あり**（BR-15 で記録済みの「default（移行前バックアップ）」プロファイル・`external: true` で旧パス `opencode-webui\profiles\main` を指す・opencode-webui ディレクトリ残存中は有効）。他 7 ファイル（host-control / login-throttle-ip / login-throttle / provider-model-state / provider-response-cache / trusted-devices / users）は参照**ゼロ**。→ 実データの旧名参照は BR-15 既知の 1 エントリのみと確定。

---

# 2026-08-14 大規模リファクタリング（Phase 0-7）後の追跡調査

> 検証環境: git HEAD `b065870`（クリーン）、Windows / OneDrive
> スコープ: REFACTORING_PLAN 全 Phase（host P6 分割・goal-loop P3 分割・opencode-proxy P4 分割・
> UI P5 分割・共有 .mjs 化 P1/P6-3・Phase 7 小分割）の**分割起因の回帰・テスト不整合・構造リスク**。
> 検証: host `node --test` 417 本 PASS / goal-loop vitest 84 本 PASS / opencode-proxy 65 本 PASS /
> SettingsView 32 本 PASS / profiles+settings-registry+home 191 本 PASS / `tsc --noEmit` クリーン。
> 本ラウンドは**発見記録のみ・修正しない**。

| ID | 優先度 | 要約 | 根拠 / 影響 |
|----|--------|------|-------------|
| **BR-1** | 高 | **Phase 6 分割で `removeBrokenWebBuild` を web-runtime.js へ移設した際、index.js 側の import が追加されず、本番ビルド経路で `ReferenceError` になる** | コミット `9efbba9`（P6-a 第八歩）で `removeBrokenWebBuild` 関数を index.js から削除して `host/src/web-runtime.js` に移設（export あり）したが、**index.js の web-runtime からの import リスト（`:20-29`）に追加し忘れた**。呼び出し箇所 `buildWebProductionInternal`（`host/src/index.js:984`）は `removeBrokenWebBuild(WEB_DIST_DIR, { log })` のまま残り、import が無いため **本番ビルド時に必ず `ReferenceError: removeBrokenWebBuild is not defined`** になる。実行経路: `spawnWeb()`（`:1041`）→ `plan.needsBuild` 成立時（`LEAFCODE_MODE=production` で BUILD_ID 無し/stale）→ `buildWebProduction`（`:1054`）→ `buildWebProductionInternal`（`:984`）。**dev モードでは発現しない**。直近 2 件（`531ffd7` isProcessAlive 未 import / `b065870` httpWaiter 未構築）と同種の「分割時の import 漏れ」で、`web-runtime.js` の `removeBrokenWebBuild` export と index.js の使用名を機械照合して唯一検出。→ index.js の import に `removeBrokenWebBuild` を追加するか、`buildWebProductionInternal` の呼び出しを削除する |
| **BR-2** | 中 | **Phase 7 の 9-1b（localStorage キー統一）で、TaskView テスト 2 件が旧キー `webui:side-tab` を期待したまま（CI が赤になる）** | コミット `d954375` で side-panel の localStorage キーを `webui:side-tab` 等のフラットキー → `webui:side-panel:tab` 等に統一した（`web/src/lib/side-panel-state.ts`、新テスト `side-panel-state.test.ts` は新キー仕様）。しかし `web/src/components/task/TaskView.test.tsx:436` と `:454` の 2 テストは **旧キー `webui:side-tab` への書き込みを期待したまま**で、キー統一コミットで更新されていない（最後に触れたのは `9875ca9`）。実装は新キーで書き旧キーはフォールバック読みのみのため **アプリ動作は正常**だが、`vitest run` で `TaskView` スイートが 2 件失敗する（実測: 146 中 2 failed）。→ テストの期待値を `webui:side-panel:tab` に更新する |
| **BR-3** | 中 | **Phase 2 のマイグレーション基盤（P2-e）で、`db.memory-migration.test.ts` の「再オープン時の scope/norm_key バックフィル」テストが失敗する（実害なし・テスト前提と実装の乖離）** | `web/src/lib/db.memory-migration.test.ts:119,129` の "backfills scope and norm_key for rows written before the columns existed" が失敗（実測: 5 中 1 failed、`scope_kind` / `scope_key` が期待値 `project`/`proj-legacy` ではなく `null` のまま）。原因: テストは「v1 行（scope 列を人為的に NULL 化）を close → 再 open するとアップグレード相当のバックフィルで修復される」ことを期待するが、`db.ts` の `getDb()` は **`isFreshDb`（初回作成）なら `user_version = CURRENT(1)` にスタンプするだけで `runSchemaMigrations` を呼ばず**、v1→v1 の再オープンでも `schemaVersion < CURRENT` が偽のためバックフィル（`migration.sql` の scope 更新 + `backfillMemoryNormKeys`）は実行されない（`web/src/lib/db.ts:161-169`、`db-schema.ts` MIGRATIONS[0].sql）。**実運用では無害**: `memory.ts` の `createMemory`（`:191`）は常に `scope_kind`/`scope_key`/`norm_key` を設定して INSERT するため、scope 列 NULL の行は発生しない。v0→v1 の真のアップグレード経路ではマイグレーションが走りバックフィルされる。つまりテストのシナリオが「fresh DB は完全スキーマ前提」という実装の設計と矛盾しており、**実装は正しいがテストの前提が古い**（3ab5661 の前後で実装は同一であり、以前から失敗していた可能性が高い）。ただし `vitest run` で CI を赤くする。→ テストを「v0 相当の DB（scope 列なし）を作って v1 へ上げる」シナリオに書き直す、または fresh 時の再バックフィルを実装する |
| **BR-4** | 低 | **SettingsView の上位タブ（engine/general/project/git/connectivity）切替で、フォームの未保存編集が無警告で失われ得る（P5-c で顕在化しやすい構造）** | `web/src/components/settings/SettingsView.tsx` はタブを全て `{activeTab === "engine" && <EngineSettingsTab/>}` 形式の**条件付きレンダリング**でマウントする（`:389` / `:403` / `:427` / `:441` / `:443`）。タブ切替（`setActiveTab`）で現在のタブが**アンマウント → 次タブ表示**、戻ると**新規マウント**され、タブ内部の useState（例: `GeneralSettingsTab.tsx` の `rateDraft` / `hangTimeoutMinutes` / `tokenSavingThreshold`、`ProjectSettingsTab` の `draft` 等）は**再初期化**される。GeneralSettingsTab は `onBlur` 保存設計（`onBlur={commitHangTimeout}` 等）のため通常操作では保存されるが、**フォーカス中の input がタブ切替の onClick で即アンマウントされると onBlur が合成イベントとして発火しない可能性**があり、その場合編集内容が失われる。**重要**: 分離前（`867106a^`、2083 行）も既に `{activeTab === "engine" && ...}` の条件付きマウントであり、**P5-c が原因の新規バグではない**（挙動不変）。ただし BU-2（ProjectSettingsView のタブ内 draft 混線）とは別の「上位タブ切替での無警告編集喪失」が存在し、`SettingsView.test.tsx` にこのケースの検証テストが無い（タブ切替の保存テスト未カバー）。→ フォーカス退避時に強制 commit（onBlur の合成イベント依存を避ける）またはタブ切替前に未保存の確認を入れる。**※実測なしの構造推測（ターン 36）**: BR-4 は「アンマウント時に onBlur 合成イベントが発火しない」という React の一般的挙動に基づく推測で、このリポジトリでは未実測。BR-1〜3 / BR-5〜7 は実測済み（ReferenceError 確認・vitest 失敗・planSync 出力・probe）だが、**BR-4 のみ実測なし**。修正時は jsdom テストで「タブ切替時に onBlur が呼ばれるか」を先に検証すること |
| **BR-5** | 中 | **P1-b の単一ソース化で、CLI（`sync-profiles.mjs`）が `isDistributable` を注入せず、WebUI 専用 MCP（browser-bridge）を外部プロファイルへ同期してしまう（web と非対称）** | 共有 `scripts/lib/sync-engine.mjs` の `planSync` / `applySync` は `isDistributable = () => true` をデフォルトにする（`:16` / `:135`）。web 側 `web/src/lib/profiles/sync-engine.ts` は `isDistributableMcpServer`（`NON_DISTRIBUTABLE_MCP_SERVERS = Set(["browser-bridge"])` を除外）を**注入して委譲**するが、**CLI `scripts/sync-profiles.mjs` は `planSync({ paths: CLI_PATHS })` / `applySync({ paths: CLI_PATHS })` と isDistributable を渡さない**ためデフォルト `() => true` が使われ、**browser-bridge も同期対象になる**。実測: この環境の `opencode.jsonc` master に browser-bridge MCP が含まれており、`node scripts/sync-profiles.mjs --check` は `[codex] 3 servers / [claude] 3 servers / [cursor] 3 servers`（browser-bridge を含む）と「3 file(s) would change」を報告。一方 web の profiles sync UI は browser-bridge を除外して 2 サーバのみ同期するため、**同一 master に対して web と CLI で同期結果が異なる**。これは P1-b が目指した「単一ソース化」の意図（web と CLI の非対称解消）が、逆方向（CLI が未精緻）で残ったケース。実害は「WebUI 専用の browser-bridge 設定が claude/codex/cursor に漏出する・不要な mcp_servers 追記」で、外部プロファイルでの動作はしないためデータ破壊には至らないが、意図しない設定汚染。→ CLI も `isDistributable`（`browser-bridge` 除外）を注入するか、共有 .mjs のデフォルトを `browser-bridge` 除外に変更する。**実現性注記（ターン 30）**: `NON_DISTRIBUTABLE_MCP_SERVERS = Set(["browser-bridge"])` は現在 `web/src/lib/profiles/sync-engine.ts:30` にのみ存在し、共有 `scripts/lib/sync-engine.mjs` / `sync-utils.mjs` には無い。したがって修正には**この定義を共有 .mjs に移す**（web は共有から import、CLI は共有のデフォルトで除外）前提が必要。CLI からの `isDistributable` 注入だけでは CLI が `browser-bridge` を知る手段が無い（web 側の定義を CLI が参照できない） |
| **BR-6** | 中 | **P6-a 分割で `stopOpencodeOnly`（OpenCode 単独再起動）経路の `/global/dispose` 実行が失われた（分割前からの回帰）** | 分割前（`bdbfb0b^`、P6-a 前）の `stopOpencodeProcessTree(pids)` は関数内部で `disposeOpencodeServer(OPENCODE_URL)` を**常に実行**していた（`git show bdbfb0b^:host/src/index.js` の `:865` で確認）。分割後の `process-stop.js` の `stopOpencodeProcessTree(pids, deps)` は `opencodeUrl = deps.opencodeUrl ?? ''` と注入化され、`disposeOpencodeServer` は `if (!baseUrl) return false` で空 URL をスキップする。**`stopOpencodeOnly`（`host/src/index.js:1623`）は `stopOpencodeProcessTree(pids)` と deps を渡さない**ため `opencodeUrl=''` になり dispose がスキップされる一方、`stopChildren`（`:1647`）は `{ opencodeUrl: OPENCODE_URL, ... }` を渡すため dispose が実行される。`stopOpencodeOnly` は `restartOpencode`（`:1794`、設定変更後の OpenCode 再起動）から呼ばれる。結果: **分割前は OpenCode 再起動時に `/global/dispose`（graceful クリーンアップ・セッション保存）が実行されていたが、分割後は実行されず即時停止だけになる**。実害は「再起動時の OpenCode クリーンアップ欠如」で、即時停止でも再起動は機能するため限定的だが、分割による挙動回帰。→ `stopOpencodeOnly` の呼び出しに `{ opencodeUrl: OPENCODE_URL }` を追加するか、`stopOpencodeProcessTree` のデフォルトを OPENCODE_URL 基準に戻す |
| **BR-7** | 中 | **P6-a 分割で lock-file.js の内部呼び出しが `lockFile` 引数を渡さず、`removeLock` / `backfillLockCreationTime` が機能しない（正常シャットダウン時に host.lock が残る）** | 分割後 `host/src/lock-file.js` の各関数は `lockFile` パラメータを受け取るが、**内部呼び出しが引数なしのまま**: `writeLock` は `backfillLockCreationTime()`（`:49`、引数なし）、`backfillLockCreationTime` 内で `readLock()?.pid`（`:56`、引数なし）、`removeLock` 内で `readLockPid()`（`:90`、引数なし）。`readLock(undefined)` は `existsSync(undefined)` = false で `null` を返す（実測確認）。したがって **(1) `backfillLockCreationTime` は `readLock()` が null → `null !== process.pid` が true で writeFileSync（created バックフィル）をスキップ**し `createdPending: true` のまま残る（安全側）、**(2) `removeLock` は `readLockPid()` が null → `null === process.pid` が false で unlinkSync をスキップ**し**ロックファイルを削除しない**。`removeLock(LOCK_FILE, ...)` は正常シャットダウン経路（`onHostExit` `:2019` / quit 系 `:2242,2275,2299`）から呼ばれ、**host.lock が残存**する。分割前（`bdbfb0b^`）の lock 関数は全て引数なしでモジュール定数 `LOCK_FILE` を内部参照しており、`readLockPid()` → `readLock()` が正しく pid を返し unlink を実行していた。実害は「次回起動時に `handleExistingInstance` が `isProcessAlive` で stale lock を検出・`removeStaleLock` で自動回収する（`:1320-1326`）」ため限定的だが、分割による回帰。→ `writeLock` / `backfillLockCreationTime` / `removeLock` の内部呼び出しに `lockFile` を渡す |
| **BR-8** | 中 | **P4-a 分割後に `api-guard-coverage.test.ts` が `/api/opencode/[...path]` のガードを検出できず失敗する（セキュリティ網羅テストの検出力低下）** | `web/src/lib/api-guard-coverage.test.ts` の 3 テストが失敗（実測: 7 中 3 failed）。全て `/api/opencode/[...path]` のガード検出に関わる: (1) "guards every route that is not explicitly public" が `['/api/opencode/[...path]']` を「ガードなし」と報告、(2) "declares a guard call for each exported handler" が「0 guard call(s) for 1 implementation(s)」、(3) "routes the OpenCode catch-all proxy through the guard" が route.ts に `requireAuthorized` 不在を報告。**原因**: テストの `withReExportedSources`（`:45-63`）は **`@addons/...` からの re-export のみ追跡**し、`@/lib/` からの re-export は追跡しない。P4-a で route.ts が 18 行の薄い配線になり `export { proxy } from "@/lib/opencode-proxy/proxy"`（`:3`）となったが、テストがこの `@/lib/` re-export を解決しないため、proxy.ts 内の `requireAuthorized`（proxy.ts:115 で呼ばれる）を検出できない。**分割前後の裏付け（ターン 45）**: 分割前（`a6045e2`）の route.ts は **773 行で requireAuthorized を直接含み**（テストが検出でき PASS）、`784e89a`（proxy 本体を `@/lib/opencode-proxy/proxy.ts` へ移動）で 18 行の薄い配線になり失敗した。**セキュリティ実装は健全**（proxy.ts 先頭で guard が機能・コメント「Guard first, before any request parsing」明示）で、実害は CI 赤化 + **「将来 proxy.ts から guard が誤って削除されてもテストが検出できない」検出力低下**。→ `withReExportedSources` を `@/lib/` からの re-export（`export { proxy } from "@/lib/opencode-proxy/proxy"` 等）も追跡するよう拡張するか、guard 呼び出しを route.ts 側に明示する |
| **BR-9** | 低 | **P1-c（`c2e2628`）で `production-webui-build-guard.mjs` が `resolveHostControlUrl` を export するようになり、`host-control.test.ts` の 1 テストが失敗（CI 赤化）** | `web/src/lib/host-control.test.ts` の "production-webui-build-guard loads and uses the shared implementation" が失敗（実測: 18 中 1 failed）。テストは `expect("resolveHostControlUrl" in buildGuard).toBe(false)`（`:81`）で「guard が共有実装を import して**使うが、自身の export としては公開しない**」ことを検証する。しかし `c2e2628`（parseListeningPids を `host/src/port-plan.js` へ一本化、guard は import+re-export）で `export { parseListeningPids, resolveHostControlUrl }`（`:8`）が追加され、resolveHostControlUrl が guard の export に含まれるようになった。2cd13e1（共有化時）の guard は resolveHostControlUrl を export しておらず、テストはその前提で作成された。**実害は極めて低い**: guard の resolveHostControlUrl export は外部から import されていない（機械確認: guard 内の使用 `:131` / `:193` と再 export `:8` のみ）ため、**不要な export の公開**で機能上の問題なし。ただし CI 赤化はある。→ guard の export から `resolveHostControlUrl` を外す（parseListeningPids のみ再 export）か、テストの期待を更新する |
| **BR-10** | 中 | **P3-a（`bb71386` unwrap 一元化）で `workflow-scheduler.ts` が `unwrapOcData` を import するようになったが、`workflow.integration.test.ts` の `vi.mock("./oc-server")` が `unwrapOcData` を提供せず 2 件失敗（リファクタリング起因の回帰）** | `workflow.integration.test.ts` の 2 テストが失敗（実測: `TypeError: Cannot read properties of undefined (reading 'id')`、`:104` / `:153`）。原因: `bb71386`（P3-a）で `workflow-scheduler.ts` の `normalizeOcList`（attention.ts）を `unwrapOcData`（oc-server.ts）に置き換えた（`:368` / `:528`）。しかし `workflow.integration.test.ts:8` の `vi.mock("./oc-server", () => ({ ocServer }))` は **`unwrapOcData` を提供しない**ため、scheduler 内の `unwrapOcData(...)` が undefined で TypeError → catch で握りつぶされ `messages` が undefined → 後続で `reading 'id'` エラー。**リファクタリング前（`eada365^`）で 2 本 PASS を git worktree で実測確認**（回帰であることを確定）。同種の mock を使う他テストのうち、`workflow-scheduler.test.ts` は mock に `unwrapOcData` を含み PASS、`task-service.test.ts` / `workspace-service.test.ts` は unwrapOcData 経路に到達せず PASS。**workflow.integration.test.ts のみ mock 未更新**。→ `vi.mock("./oc-server")` に `unwrapOcData` を追加（`goal-loop.integration.test.ts` が `unwrapOcData: actual.unwrapOcData` で解決済みのパターンに倣う） |


> **2026-08-14 リファクタリング追跡（ターン 2-3）の健全確認メモ**（BR-1〜3 は上記。以下は「バグではない」と確認した再調査防止メモ）
> - **共有 .mjs の型安全性**: `scripts/lib/*.mjs` の全 export が対応 `.d.mts` で宣言されている（`_probe_dts` で 8 モジュール全て確認）。web 側ラッパー（`host-control.ts` / `paths.ts` / `profiles/*`）は `.mjs` を再エクスポートしつつ web 独自の path ヘルパーを保持。`tsc --noEmit` クリーン。→ 健全。
> - **settings-registry（P4-d）**: `ALLOWED_KEYS` に含まれるキー（auto-optimize / auto-show-model / auto-route-overrides / default-model / default-model-effort / sidebar / sidepanel-width / hang-timeout 等）は全てクライアント/サーバーで実際に使用され、デッドキーなし。`/api/settings/[key]` ルートは `isAllowedKey` + `normalizeSettingValue` を正しく参照（薄い配線）。`BOOLEAN_SETTING_KEYS` の扱い分け（MEMORY_ENABLED は tri-state / MEMORY_WRITE_APPROVAL は boolean）は仕様どおり。ルートテスト 73 本 PASS。→ 健全。
> - **useSessionStream（P5-a）**: 1-797 行（純関数・テスト済み）と 798 行以降の `useSessionStream` フック（799-2290 行・約 1491 行）に明確に分かれる。フック内は多数の ref（preferRestHold / abortingScopes / safetyNetTimer 等）を持つ複雑な状態機械で、`session-sse.ts` / `session-actions.ts` への分割は容易でない（OR-4 参照・P5-a 未実施）。→ 健全（分割が未実施なだけで機能は正常）。
> - **9-1b localStorage キー整理の完全性（ターン 4）**: `git grep` で全 `webui:` キーの使用を網羅調査。旧フラットキー（`webui:side-tab` / `webui:side-show` / `webui:side-panel` / `webui:plugins`）は **読み取りフォールバック専用**（`side-panel-state.ts` の `LEGACY_*_KEY` 定数 / `addons/state.ts` の `LEGACY_STORAGE_KEY`）で、**書き込みパスはゼロ**（`setItem` / `removeItem` / `writeItem` / `writeRaw` が旧キーを使う箇所なし・機械確認）。新キー `webui:side-panel:*` は `SIDE_PANEL_PREFIX` 定数からの文字列生成で一元化され、散在ハードコードなし。つまり **BR-2 は「実装は完全に正しく、テスト 2 件が旧キー期待のまま残った」だけ**であり、9-1b の実装自体に欠陥はない。→ 健全。
> - **Phase 7 2-4/2-5 route テスト・共通化（ターン 5-6）**: Git 11 ルート（branches/commit/commit-message/init/log/merge/pr/push/repositories/rm/show）とメモリ 5 ルート（consolidate/extract/extractions/purge/\[id\]）の全てに `route.ts` + テストが揃っている（機械確認）。2-5 の `open.ts` 共通化は両 open ルート（`profiles/[id]/open` / `profiles/open-target`）で `parseOpenAction` / `runOpenAction` を正しく使用。→ 健全。
> - **P6-b routeHandlers テーブル化の完全性（ターン 6）**: `host/src/control-server.js` の ROUTE_TABLE 18 ルートの route id は routeHandlers の全 13 キー（health/stop-webui/allow-firewall/logs/voice-input/users/auth-config/browser-config/auth/ip/webui/opencode/all）と完全一致。restart 3 ルート（`webui:` / `opencode:` / `all:`）は `onRestartWebui` 等を setImmediate で呼び、`routeHandlers[route]` の dispatch で正しく解決される。control-server テスト 88 本 PASS。**注意**: routeHandlers キーの一部は `webui: async` のようにクォート無しで宣言されており、`^  'キー': async` の正規表現では検出できない（誤検出で「404 になる」と誤推測しやすい。実装は健全）。→ 健全。
> - **Phase 4 キャッシュ TTL と goal-util 定数一元化（ターン 7）**: `opencode-proxy/cache.ts` の `readBoundedCapabilityCache` は TTL 60 秒（`CAPABILITY_CACHE_TTL_MS`）超過でエントリを削除、`GET_RESPONSE_CACHE` は 5 秒 TTL + 32 上限 LRU。`proxy.ts:451-452` の読み取りも TTL チェック付き。`cachedProvidersByDir` / `cachedAgentsByDir` は directory キー + TTL + 64 上限 LRU（`setBoundedCapabilityCache`）で、directory 無しの場合はキャッシュを読み書きしない安全設計。capability cache TTL は `cache.test.ts` の fake timers テスト（ローカル `new Map()` 使用でグローバル汚染なし）で検証済み。goal-util.ts の 16 定数（`TERMINAL_STATUSES` / `TURN_TIMEOUT_MS` / `MESSAGE_TIMEOUT_MS` 等）は goal-scheduler / goal-state / goal-prompt / goal-db の全分割モジュールから import され、二重定義なし（`git grep` で goal 系 3 モジュールのみが `TERMINAL_STATUSES` を参照）。BH-8 の LRU/TTL 構造解消は完了・検証済み。→ 健全。
> - **Phase 1 共有 .mjs の単一ソース化の完全性（ターン 10-11）**: `web/src/lib/profiles/sync-engine.ts` の `planSync` / `applySync` は共有 `scripts/lib/sync-engine.mjs` を `applySync as sharedApplySync` / `planSync as sharedPlanSync` で import し、`isDistributable`（`NON_DISTRIBUTABLE_MCP_SERVERS = Set(["browser-bridge"])` 除外）を注入して委譲する**薄いラッパー**（`getDb()` 等の独自実装ではない）。agents-sync 側も `web/src/lib/profiles/agents-sync-engine.ts` と `scripts/agents-sync.mjs` の両方が共有 `agents-sync-engine.mjs`（5 関数）を参照し、**完全な単一ソース化**。**注意**: `planSync` / `applySync` の関数名は web ラッパーと共有 .mjs の両方に現れるため、`export function` の機械照合だけで「二重実装」と誤判定しやすい（実装は委譲のみ）。→ 健全（BR-5 は CLI 側の `isDistributable` 未注入のみが非対称で、共有 .mjs 自体の単一ソース化は完了）。
> - **Phase 6 health.js / process-stop.js 分割境界（ターン 12）**: `health.js`（89 行）は `procRunning` / `createHttpWaiter` を export し、sleep / log / isPortInUse を注入する純待機ロジック。`createHttpWaiter` は index.js で構築済み（b065870 で修正）。`waitUntilReady` は attempts（秒）を 250ms ポーリングの反復回数に変換（`Math.ceil((attempts*1000)/pollMs)`）し、proc 死亡時は fail-fast。`process-stop.js`（358 行）は 12 関数を export し、`reapOpencodePortHolders` は `deps.listChildren ?? listChildPids` で 531ffd7 の修正を正しく統合。`disposeOpencodeServer` は `if (!baseUrl) return false` で空 URL を安全処理。health + process-stop テスト 24 本 PASS。**注意点**: `stopOpencodeOnly`（`index.js:1623`）は `stopOpencodeProcessTree(pids)` と deps なし（`opencodeUrl=''`）で呼ぶため dispose がスキップされる一方、`stopChildren`（`:1647`）は deps 付きで dispose を実行する。`restartOpencode`（`:1794`）経由の単独再起動では即時停止が意図的設計と推測されるが、dispose スキップによる OpenCode のクリーンアップ未実行は潜在的な注意点（`resolveOccupiedPort` が ghost を吸収するため実害は限定的・要実測）。→ 分割自体は健全、dispose の非対称のみ要確認。**→ ターン 13 で確定: 分割前（`bdbfb0b^`）は `stopOpencodeProcessTree` 内部で常に dispose を実行していたため、分割後に `stopOpencodeOnly` 経路の dispose が失われた回帰。BR-6 として BUG.md に記録済み。**
> - **Phase 5 hooks 抽出（P5-b）のタブ同期整合（ターン 14）**: `use-task-panels.ts`（63 行）は 13 個の表示 state（tab / viewTab / showDiff / sidePanel / sideWidth / viewportIsLg / isMd 等）を集約する純粋な state ラッパーで、副作用は TaskView 側に残る。TaskView は `readChatTab()` / `readShowDiff()` / `readSidePanel()`（`:1085-1087`）で初期化し、`changeTab` / `changeShowDiff` / `changeSidePanel`（`:1475-1483`）で `writeChatTab` 等を呼び永続化する。split ビュー進入時は `changeTab("chat")`（`:1560`）で両ペインを chat 固定にし、`webui:side-panel:tab` への書き込み競合は実質なし（「split ペインは会話固定」の意図的設計）。`viewportIsLg` / `isMd` は matchMedia + resize リスナー（`:1102-1118`）で hooks 抽出後の setter を正しく更新。useAutoTask / useGoalLoop / useTaskModelConfig / useComposerSend も全て副作用なしの state ラッパーで、fetch 等は TaskView 側に残る正しい分離。→ 健全。
> - **P2-b スキーマ一致テストの実効性（ターン 15）**: `web/src/lib/db-schema.test.ts`（262 行）の 5 テストを実行し**全て PASS**。中核の「legacy database upgraded by getDb() has the same shape as a fresh one」は、`LEGACY_SQL`（過去に ALTER TABLE を受けた 6 テーブル: projects / workspaces / session_bindings / goal_loops / workflow_node_attempts / memories）を入れた DB を `getDb()` で開き、`PRAGMA table_info` が fresh DB と**完全一致**することを検証する。MIGRATIONS[0].columns の 6 テーブルは LEGACY_SQL に全て含まれ**網羅性完全**。他にも「user_version スタンプ」「再オープン冪等」「version 0 の完全スキーマがアップグレードされる」「バックアップ保持」を検証。**BR-3 との関係**: db-schema.test.ts（P2-b）は「legacy→fresh のアップグレード経路」で形状一致を検証しており PASS。一方 db.memory-migration.test.ts（BR-3）は「fresh DB の再オープンで scope 列 NULL 行が修復される」前提で失敗。両者は**別シナリオ**（upgrade 経路 vs fresh 再オープン）で矛盾しない。P2-b の健全性は BR-3 の実在を否定しない。→ 健全。
> - **browser-bridge 分割と goal-prompt 分割（ターン 16-17）**: browser-bridge は 91 テスト PASS。host 側 `browser-bridge.js` と `broker/server.mjs` の `createBrowserBridgeBroker` が整合（b065870 で TDZ 修正済み）。P1-d のメモリロジック共有化（`shared/memory-schema.mjs` の `isMemoryKind` / `memoryContentError` / `inspectMemoryContent` / `toFtsPhrase` を memory-server が使用）は完了。goal-prompt.ts（201 行）は `buildGoalPromptWithMemory` / `buildGoalPrompt` / `buildGoalContinuationPrompt` / `buildVerificationPrompt` の 4 関数を export し、`buildGoalPromptWithMemory` は turnNumber で buildGoalPrompt / buildGoalContinuationPrompt を切り替え + memoryInjectionFor 前置。`buildGoalPrompt` の実使用は goal-scheduler のみ（useSessionStream はコメント言及のみ）。ce439ef の diff を確認し、goal-prompt 分割は**移動のみ**（goal-loop.ts -198 行 / goal-prompt.ts +200 行、integration.test.ts の変更は unwrapOcData の mock 調整でプロンプト本文の変更なし）。memory への依存（`memoryInjectionFor`）も分割前（ce439ef^）から存在し、新たな依存増加なし。現在実行中の goal-loop プロンプト（turn 1 of 50）が buildGoalPrompt の full-run 出力と完全一致することも実証。→ 健全（goal-prompt.test.ts 単体は不在だが OR-14 の範囲）。
> - **caddy-sites.js 分割の検証（ターン 18）**: `host/src/caddy-sites.js`（250 行）は 8 関数（isPlaceholderHost / isIpv4Literal / syncCaddySiteAddresses / CADDY_LOOPBACK_URL_RE / parseCaddySiteUrls / parseCaddyPublicUrl / parseCaddyLoopbackUrl / pickBrowserUrl）を export。ab7ff30 はマージコミット（第一親 ab7ff30 + 第二親 9efbba9）で、分割前の index.js には caddy-sites.js からの import と **index.js 内の古いローカル `export function parseCaddySiteUrls` が共存**する重複状態があったが、ab7ff30 でローカル定義を削除し caddy-sites.js に一本化（現 index.js は import + re-export のみで重複なし）。caddy-sites / caddy-setup / caddyfile の 3 テスト 20 本 PASS。Caddy URL 解析（HTTPS origin 抽出・placeholder 除外・loopback 判定）はテストで検証済み。→ 健全。
> - **proxy 分割の SSE/ストリーミング経路（ターン 19）**: `proxy.ts`（716 行）の SSE 処理を確認。`isSse` 判定は content-type `text/event-stream` または wantsSse（`:566`）。SSE は「クライアント切断（req.signal）追従 + ヘッダ待ちのみ `SSE_UPSTREAM_CONNECT_TIMEOUT_MS` で timeout、確立後のストリームはタイムアウトしない」（`:401-412`）。SSE ストリームは初期コメントハートビート `: connected\n\n` + `setInterval` で `encodeSseHeartbeat()` 定期送信 + reader→controller 転送、finally で heartbeat 停止 / reader.cancel / controller.close（`:658-698`）。SSE 定数/関数は `sse-health.ts`（66 行・8 export）に単一ソース化され、`encodeSseHeartbeat` / `SSE_HEARTBEAT_MS` を proxy が参照。`route.test.ts` に「504 when the engine never sends SSE response headers」（`:1080`）と「does not time out an SSE stream once headers have arrived」（`:1115`）が含まれ、65 テスト PASS で SSE 挙動を検証済み。→ 健全。
> - **Phase 7 8-3 追加テストの実装整合（ターン 20）**: Phase 7 で追加された 8-3 コンポーネントテスト 8 ファイル（WorkflowGraphCanvas 48c9dad / ProfileAgentsSyncSettings d92cca2 / ProfileSyncSettings a04fe2c / VisionSettings a0bbf82 / TaskSplitContext 97f1aa3 / AutoRouteOverridesEditor 6975ea1 / model-pricing-registry f94ea1b / addons/state 09a7da3）を全て実行し**PASS**（一括 34 本 + model-pricing/addons 11 本、合計 45 本）。`addons/state.ts` と `model-pricing-registry.ts` はテスト追加コミット（09a7da3 / f94ea1b）以降に変更されておらず（`git log <testcommit>..HEAD` で確認）、**テストと実装が整合したまま**。既存の TaskView / SettingsView テスト（BR-2 / BR-4 の 2 件以外）も含め、Phase 7 のテスト補完は実装と矛盾していない。→ 健全。
> - **P2-e マイグレーションの冪等性とスキーマ整合（ターン 21）**: `runSchemaMigrations`（`db.ts`）は各 step で `PRAGMA table_info` のカラム存在チェック（存在すればスキップ）→ `migration.sql`（index/backfill）実行 → user_version スタンプ、の冪等構造。MIGRATIONS[0].columns の 17 カラム追加が参照する 6 テーブル（workspaces / memories / goal_loops / session_bindings / workflow_node_attempts / projects）は全て SCHEMA_SQL に存在し、**17 カラム全てが SCHEMA_SQL の最新定義に含まれる**ことを確認（memories の scope_kind / scope_key / norm_key / revision を含む）。これは db-schema.test.ts（P2-b、前ターン 5 本 PASS）の「legacy upgrade = fresh 形状」で検証済み。**注意**: CREATE TABLE ブロックを正規表現で切り取る probe は、`;` を含む定義や複雑なテーブルで誤検出しやすい（memories の scope_kind 等を「無い」と誤判定した）。実装は整合。→ 健全。
> - **goal-db 分割の DB 関数整合（ターン 22）**: `goal-db.ts`（406 行）は 6 関数（getGoalLoop / listRunnableGoalLoops / createGoalLoop / updateGoalLoopStatus / updateGoalLoopMaxTurns / pauseGoalLoopForManualSend）を export。呼び出し元は `app/api/tasks/[id]/goal-loop/route.ts` / `app/api/tasks/[id]/route.ts` / `goal-loop.ts` / `goal-scheduler.ts` / `opencode-proxy/proxy.ts` で、全て正しく使用。goal-db.ts は goal-loop.ts から `applyAssistantResult` / `deliveredGoalResultAfterUnknownPrompt` / `isUnknownPromptDeliveryPause` / `runGoalLoopSchedulerTick` を実行時 import する（goal-loop ↔ goal-db の実行時循環）が、**全て関数内参照のみ**（`:160` / `:226-253` / `:289`、トップレベル呼び出しなし）で TDZ を起こさない（ESM の関数呼び出し時解決）。OR-3 はこの goal-db → goal-loop 循環を含めて記録済み。goal-loop テスト 84 本 PASS。→ 健全。
> - **Phase 7 8-4 E2E CI 設定（ターン 23）**: `.github/workflows/ci.yml`（68 行）は `web`（lint/typecheck/vitest）と `e2e-smoke` の 2 ジョブ。e2e-smoke は `npm run e2e:smoke`（`node scripts/e2e.mjs --project=chromium smoke.spec.ts`）を実行し、smoke.spec.ts が存在。`playwright.config.ts` の webServer.command は `npm run sync:addons && npx next build && npm run start --port 3100` で、`NEXT_DIST_DIR: ".next-e2e"` を env 設定して**ビルド出力を分離**。`next.config.ts` の `resolveNextDistDir`（`src/lib/dist-dir.ts`）が NEXT_DIST_DIR を web ディレクトリ内に検証し適用するため、E2E の `next build` がローカル稼働中 WebUI（`.next`）を壊さない（ci.yml コメントの「NEXT_DIST_DIR 分離でローカル稼働中の WebUI には影響しない」と実装が一致）。AGENTS.md の「build はユーザー指示時のみ」制約とも整合（E2E は `.next-e2e` 分離のため稼働中 WebUI に非干渉）。→ 健全。
> - **Phase 6 windows-integration.js 分割（ターン 24）**: `host/src/windows-integration.js`（76 行）は 3 関数（launchWindowsVoiceInput / firewallRuleExists / allowFirewallPort）。`launchWindowsVoiceInput` は PowerShell の `keybd_event` で Win+Shift+H を送信、`allowFirewallPort` は UAC 昇格 subprocess で netsh ルール追加、`firewallRuleExists` は読み取り専用の netsh show（昇格不要）。`firewallRuleExists` は `allowFirewallPort` 内で自己参照（`:if (firewallRuleExists())`）され、index.js からは未使用 → **export は不要な公開 API**（OR-6 のテスト専用 export の範疇）。index.js は `onVoiceInput: () => launchWindowsVoiceInput()`（`:1895`）と `onAllowFirewall: () => allowFirewallPort(WEBUI_PORT)`（`:1897`）として control-server に渡し、非 Windows での throw は control-server が 500 に変換。分割は健全。専用テストは無し（OR-2 で記録済み）。→ 健全。
> - **Phase 6 分割モジュールの引数伝搬・依存整合（ターン 25-26）**: lock-file.js で **BR-7**（内部呼び出しが lockFile 引数を渡さない）を発見・記録。その他の分割モジュールを引数伝搬の観点で精査し、**BR-7 が唯一の該当**であることを確認: `port-scanner.js` の `getListeningPids` → `runNetstat()` / `findFreePort` → `captureNetstat()` は**引数なし関数の正しい呼び出し**（`runNetstat` / `captureNetstat` は引数 0 個）。`opencode-upgrade.js` の `process.cwd()` はメソッド呼び出しで正常。`process-info.js`（5 関数）は `runPowerShell`（port-scanner）を import し、port-scanner は process-info に依存せず循環なし。`port-scanner.js` の `isThisWebUiNextStart`（web の build guard）依存は**分割前から存在**（分割前 index.js:767 が直接 import）し、意図的な共有（コメント明示）。opencode-upgrade.js の import は使用関数を全て含む。→ BR-7 以外は健全。
> - **Phase 7 9-3 ログ集約方針と実装の整合（ターン 27）**: README「ログとデバッグファイルの扱い」（`:233-244`）は「host の `log-buffer` / `log-file` に集約、`log-buffer.js` はリングバッファ、`log-file.js` はディスク書き込み、子プロセス出力は `recordLog` 経由、`console.log` 直書きではなく `log` / `error` 経由」と明記。実装は `recordLog`（`index.js:471-476`）が `pushLogEntry`（リングバッファ）+ `getLogFileWriter().write`（ディスク）の両方に書き、`log` / `error` 関数（`:479-487`）が `console.log` + `recordLog` を行う。`pushLogEntry` の直接呼び出しは recordLog 内（`:473`）のみで、**index.js 内に recordLog バイパスなし**。host/src の分割モジュールに `console.log` / `console.error` の直書きは無い（機械確認、index.js の log/error 関数のみ）。README の方針と実装は完全一致。→ 健全。
> - **Phase 7 8-5 specs バッジ運用（ターン 28）**: `docs/specs/README.md`（59 行・ba5664b）はバッジルール（✅/🔶/⬜）と 28 本の仕様一覧を整理。バッジ付与は **2 本のみ**（`bat-encoding-safety.md` ✅ / `host-log-viewer.md` ✅）で、参照先（`bat-encoding.test.js` / `HostLogPanel.tsx` / `log-buffer.js` / `log-file.js`）は全て実在し判定は正確。残り 26 本はバッジなし（機械確認: `実装ステータス: [✅🔶⬜]` パターンで 28 本中 2 本のみ一致）→ **OR-16** として OPTIMIZATION.md に記録（運用ルールが部分的適用のまま）。バグではない。
> - **Phase 0 encoding-check CI と Node バージョン（ターン 29）**: `.github/workflows/encoding-check.yml` は windows-latest の bat-encoding ジョブで `npm run test:encoding`（= `npm --prefix host run test:encoding`）と `npm --prefix host test` を実行。`index.test.js`（451 行・34 テスト）の network/spawn 参照は全て Caddyfile 文字列のパーステストで外部アクセス・プロセス起動なし。host の依存は systray2 / ws のみで engines なし。host/src に Node 20 で未サポートの API は**使用されていない**（`Promise.withResolvers` 等の機械検出は全てコメント内の単語 "using" のみ）。**注意**: encoding-check は node 20、ci.yml は node 24 とジョブ間でバージョンが不統一（現状 host テストは通るが将来の Node 24 専用 API 導入で encoding-check だけ壊れるリスク）→ **OR-17** として OPTIMIZATION.md に記録。→ 健全。
> - **既存 BR/OR の記述整合レビュー（ターン 30）**: 作業ツリーの最終確認（変更は BUG.md / OPTIMIZATION.md のみ、`db.ts` / `useSessionStream.ts` は mtime 同期のみで差分ゼロ）。BR-5 の修正方針に**実現性の注記を追記**: `NON_DISTRIBUTABLE_MCP_SERVERS` は web 側 `sync-engine.ts:30` にのみ存在し共有 .mjs には無いため、修正には定義の共有化が前提（CLI からの注入だけでは CLI が browser-bridge を知る手段がない）。BR-1〜7 の優先度を再評価: BR-1（高）が唯一の高で本番ビルドクラッシュ、BR-2〜3 / BR-5〜7（中）、BR-4（低）で**優先度は妥当**。BR-7 の実害（host.lock 残存）は `deps.removeControlFile?.()` も同時にスキップされるが、次回起動時に stale として処理されるため限定的。
> - **web 側未分割領域の規模再確認と分割可能性（ターン 31）**: TaskView.tsx 5543 行 / useSessionStream.ts 2290 行 / SettingsView.tsx 595 行 を再確認（OR-4 / OR-8 の記述と一致）。`useSessionStream` フック（799-2290 行・約 1492 行）の構造を精査: useRef 20 / useState 1 / useEffect 5 / useCallback 14 / useMemo 1。useCallback 14 個は **SSE/接続系 6**（acquirePreferRest / releasePreferRest / clearMutationTimers / runResync / resync / startMutationElapsed）と **アクション系 8**（sendPrompt / sendCommand / refreshTodos / abort / replyPermission / replyQuestion / rejectQuestion / clearPendingDescendants）に分類可能で、P5-a の `session-sse.ts` / `session-actions.ts` 2 分割計画と構造的に一致。分割の主な障壁は **20 個の ref の共有**（両グループにまたがる）。→ OR-4 に分割可能性の実測を追記（BUG.md のターン 2-3 メモ「分割は容易でない」は ref 共有の観点で正確だが、関数分類は可能）。
> - **workflow 分割領域の write_conflict 整合（ターン 32）**: `workflow-graph-runtime.ts`（106 行）の write_conflict 処理（BH-10 修正）は `writableReady.length > 1` で全 writable node を `blockedNodeIds` に push し `pauseReason = "write_conflict"` を設定（`:87-95`）。`workflow-scheduler.ts:667` は `graphRuntime.blockedNodeIds.includes(node.node_key) && graphRuntime.pauseReason` で `pauseWorkflowForAttempt` を呼ぶ。**実装は健全**（BH-10 の修正が反映）。ただし `workflow-graph-runtime.test.ts` の write_conflict テスト（"prevents parallel writable claims" `:61-67`）は `pauseReason` / `readyNodeIds` のみ検証し **`blockedNodeIds` の内容を検証していない**（テスト 3 本 PASS だが BH-10 の本質が非保護）→ **OR-18** として OPTIMIZATION.md に記録。
> - **opencode-extensions 分割領域（ターン 33）**: 10 モジュール（agent-files / agents / http / jsonc-edit / mcp / paths / plugins / provider-models / safe-move / skills）は**相互 import なし**の独立自己完結モジュール。ただし safe-move（安全なファイル移動）/ paths / jsonc-edit を多数が共有し、責務分離は明確。`agents.ts` が `@/components/settings/agent-utils` から **type-only import**（`AgentDto` / `AgentScope`、`:5-8`）する lib→components 逆依存が 1 箇所あるが、agent-utils は React / JSX 非依存の純ロジックで type-only のため実行時影響なし（分割前から存在）→ **OR-19** として OPTIMIZATION.md に記録。
> - **Phase 5 1-3b OAuth フック共通化（ターン 34）**: `use-subscription-oauth.ts`（234 行・`useSubscriptionOAuth`）は ClaudeSubscriptionAuth / OpenAISubscriptionAuth の状態機械（loading/ready/starting/waiting/connected/error）・ポーリング（2s × 90 回）・認証開始を 1 実装に集約。両カードは config でプロバイダ差を注入（Claude: `findMethodIndex(type === "oauth")` / OpenAI: `findMethodIndex(isBrowserOAuth)`）。共通化は正しく機能し、`OpenAISubscriptionAuth.test.tsx` 5 本 PASS。**注意**: 共通フックの専用テストと ClaudeSubscriptionAuth.test.tsx は存在しない（分割前 a156c48^ でも Claude はテスト無し）ため、Claude 設定経路はテスト非カバー → **OR-20** として OPTIMIZATION.md に記録。
> - **Phase 4 9-1 キャッシュ方針統一（ターン 35）**: `http-cache.ts` は `DEFAULT_MAX_AGE_SECONDS = 30` / `DEFAULT_SWR_SECONDS = 600`、`stale-cache.ts` は `DEFAULT_FRESH_MS = 30_000` / `DEFAULT_STALE_MS = 600_000` を定義し、現在の値は一致（30s / 600s）。ただし P4-c の「値を 1 箇所に集約」に対し、実装は**2 ファイルに別定義 + コメント（"keep them in step"）での同期指示**で部分解消のまま。`sw.js`（134 行）は鮮度 TTL を持たない（ビルド ID ベースの wipe が主）ため実質 2 系統の二重管理。将来のドリフトリスクが残る → **OR-21** として OPTIMIZATION.md に記録。
> - **発見の俯瞰整理（ターン 36）**: BR-1〜7 のうち **BR-1 / BR-2 / BR-3 / BR-5 / BR-6 / BR-7 は実測済み**（ReferenceError 確認・vitest 2/1 failed・`--check` + planSync 出力・git show 分割前後比較・probe 実測）、**BR-4 のみ実測なしの構造推測**（onBlur アンマウント挙動）であることを BR-4 に注記。発見の内訳: **分割起因の回帰バグ 2 件**（BR-6 dispose スキップ / BR-7 lock 引数伝搬）、**テスト不整合 2 件**（BR-2 TaskView 旧キー / BR-3 マイグレーションテスト前提乖離）、**CLI 非対称 1 件**（BR-5 isDistributable）、**構造リスク 1 件**（BR-1 import 漏れ）、**UX リスク 1 件**（BR-4 タブ編集喪失）。修正優先度: BR-1（高・本番ビルドクラッシュ）→ BR-5 / BR-6 / BR-7（中・分割回帰）→ BR-2 / BR-3（中・CI 赤化）→ BR-4（低・推測）。
> - **Phase 5 1-3 GhostSelect 共通化（ターン 37）**: `PermissionGhostSelect.tsx`（54 行）が固定 options の GhostSelect ラッパーを共通化し、AccessModeSelect / SkillPermissionSelect / SubagentPermissionSelect（各 37-40 行）が options + 表示メタデータ（ariaLabel / icon / tone）を渡す。`ACCESS_MODE_OPTIONS` は `{value,label,title}` で `PermissionOption` と型互換。共通化は健全。**注意**: 3 ラッパーと PermissionGhostSelect に専用テストが無い（4 ファイル不在）。`TaskView.test.tsx` が間接参照するが BR-2 で 2 件失敗中のため完全なカバレッジではない → **OR-22** として OPTIMIZATION.md に記録。
> - **Phase 3 P3-c task-cost 分割（ターン 38）**: `task-service.ts`（339 行・listTasks / listArchivedTasks / getTask / getTaskCost）はコスト計算の正本（`estimateSessionCostWithCache` / `SESSION_COST_FETCH_CONCURRENCY`）を `task-cost.ts` から import し、`getTaskCost` は ocServer の報告コスト優先 → 無ければ `estimateSessionCostWithCache` フォールバック。分割は健全。**注意**: `task-cost.test.ts` は存在せず、`task-service.test.ts`（29 本 PASS）は getTaskCost を 5 回テストするが `estimateSessionCostWithCache` / `exactMessageCost` を直接参照しないため、**コスト推定ロジックはフォールバック経路でのみ間接カバー** → **OR-23** として OPTIMIZATION.md に記録。
> - **Phase 6 npm-cli.js 分割（ターン 39）**: `npm-cli.js`（51 行・`spawnNpm` のみ export）は分割前（`bdbfb0b^`）の index.js 内 `spawnNpm` と**同一実装の移動のみ**（`cachedNpmCli` のモジュール変数・where.exe 探索・`spawn(process.execPath, [...], { shell: false })` の quoting 脆弱性回避）。index.js に cachedNpmCli の重複は無く（0 回）、spawnNpm は import + 使用のみ。→ 健全。
> - **Phase 7 2-4 Git route テスト整合（ターン 40）**: Git 11 ルート（branches / log / show / repositories / merge / pr / commit 等）の route.ts はテスト追加コミット（128c855 / 72bb709 / 9c48d24 / b8b4bbe / f41e52c / 0c468d3 等）以降に変更されていない（`git log <commit>..HEAD` で確認）。Git ルートテストは**11 ファイル 77 本 PASS**（実測）。テストと実装は完全に整合。→ 健全。
> - **Phase 7 2-4 メモリ route テスト整合（ターン 41）**: メモリ 5 ルート（extract / purge / consolidate / extractions / [id]）の route.ts はテスト追加コミット（1cb83ed / ec15fc8 / 692ab29 / b12f410 / 62ea87c）以降に変更されていない（`git log` で確認）。メモリ route テストは**6 ファイル 44 本 PASS**（実測）。テストと実装は完全に整合。→ 健全。
> - **リファクタリング追跡調査の俯瞰整理（ターン 42）**: OPTIMIZATION.md の OR-1〜23 は **高 2**（OR-1 index.js 未使用 import 13 個 / OR-2 分割モジュール 6 個にテストなし）、**中 3**（OR-3 goal-loop 循環 import / OR-4 useSessionStream 未分割 / OR-5 useTaskModelConfig 共通化未実施）、**低 18**。BUG.md の BR-1〜7 は **高 1 / 中 5 / 低 1**。Phase 別健全性: **Phase 0-5 / 7 は概ね健全、Phase 6 に分割回帰 2 件**（BR-6 dispose スキップ / BR-7 lock 引数伝搬）、**Phase 5 に未分割の巨大ファイル 2 件**（OR-4 useSessionStream 2290 行 / OR-8 TaskView 5543 行）。リファクタリングの主要目的（単一ソース化・分割・テスト補完）は概ね達成。
> - **workflow 分割の残り（ターン 43）**: `workflow.ts`（396 行・11 関数・検証/型）と `workflow-service.ts`（877 行・16 関数・CRUD/実行）の分割を確認。workflow-service は workflow から `assertValidWorkflowNodeConfig` / `validateWorkflowNodeKind` を import する**一方向依存で循環なし**。`workflow.test.ts` + `workflow-service.test.ts` 19 本 PASS。両ファイルの最後の変更は 2026-08-02 / 08-12 で**リファクタリング（Phase 0-7）より前**の既存構造であり、リファクタリングの影響を受けていない。→ 健全。
> - **api-guard-coverage の失敗（ターン 44）**: `api-guard-coverage.test.ts` を実行し **7 中 3 failed** を発見（実測）。全て `/api/opencode/[...path]` のガード検出失敗で、原因は `withReExportedSources` が `@addons/...` の re-export のみ追跡し **`@/lib/` からの re-export（`export { proxy } from "@/lib/opencode-proxy/proxy"`）を追跡しない**こと（P4-a で route.ts が薄い配線化）。proxy.ts:115 で `requireAuthorized` が機能しているためセキュリティ実装は健全だが、テストの検出力が低下。**BR-8** として BUG.md に記録（実害は CI 赤化 + 検出力低下）。
> - **api-guard-coverage の失敗時期の確定（ターン 45）**: BR-8 の裏付けとして、分割前後を比較。分割前（`a6045e2`）の route.ts は **773 行で requireAuthorized を直接含み**（api-guard-coverage.test.ts が検出でき PASS）、`784e89a`（proxy 本体を `@/lib/opencode-proxy/proxy.ts` へ移動）で **18 行の薄い配線**（`export { proxy } from "@/lib/opencode-proxy/proxy"`）になり、テストが `@/lib/` re-export を追跡しないため失敗した。つまり **P4-a 分割（784e89a）が BR-8 の原因**であることが確定。→ BR-8 に裏付け追記。
> - **web 全体 vitest の失敗俯瞰（ターン 46）**: `npx vitest run`（全ファイル）を実行し **9 failed / 3858 passed / 1 skipped（321 ファイル）**を確認。失敗 9 件は 5 ファイルに分散: **既知 3 系統**（TaskView 2 = BR-2 / db.memory-migration 1 = BR-3 / api-guard-coverage 3 = BR-8）に加え、**新規 2 系統 3 件**を発見: `host-control.test.ts` 1 件（**BR-9** として記録）と `workflow.integration.test.ts` 2 件（未調査）。host-control の失敗は P1-c（`c2e2628`）で guard が `resolveHostControlUrl` を export するようになりテストの期待（export しない）と矛盾（不要 export・実害極小・CI 赤化）。→ **workflow.integration.test.ts の 2 件を次ターンで調査**。CI は web ジョブ（`npm test` 全ファイル）でこれら 9 件を検出するため**既に赤い状態**。
> - **workflow.integration.test.ts の回帰確定（ターン 47）**: 2 件失敗（`TypeError: Cannot read properties of undefined (reading 'id')`、`:104` / `:153`）を **git worktree でリファクタリング前（`eada365^`）と比較し、前は 2 本 PASS・現在 FAIL であることを実測確定**。原因は `bb71386`（P3-a unwrap 一元化）で `workflow-scheduler.ts` が `normalizeOcList` → `unwrapOcData` に置き換えたが、`workflow.integration.test.ts:8` の `vi.mock("./oc-server", () => ({ ocServer }))` が `unwrapOcData` を提供しないため。同種の `workflow-scheduler.test.ts` は mock 更新済み（PASS）、`task-service.test.ts` / `workspace-service.test.ts` は unwrapOcData 経路に非到達（PASS）。→ **BR-10** として記録。
> - **調査総括（ターン 48）**: 2026-08-14 のリファクタリング追跡調査を完了。**web 全体 vitest の 9 件失敗は全て BR で説明可能**（TaskView 2 = BR-2 / db.memory-migration 1 = BR-3 / api-guard-coverage 3 = BR-8 / host-control 1 = BR-9 / workflow.integration 2 = BR-10）。**BR-1〜10 の内訳**: 分割起因の回帰 4 件（BR-1 import 漏れ・BR-6 dispose スキップ・BR-7 lock 引数伝搬・BR-10 unwrap mock 未更新）、テスト未更新 4 件（BR-2 / BR-3 / BR-8 / BR-9）、CLI 非対称 1 件（BR-5）、UX リスク 1 件（BR-4・実測なし推測）。**修正優先度**: BR-1（高・本番ビルドクラッシュ）→ BR-5/6/7/10（中・回帰/非対称）→ BR-2/3/8（中・CI 赤化）→ BR-4/9（低）。OPTIMIZATION.md は OR-1〜23（高 2 / 中 3 / 低 18）。リファクタリングの主要目的（単一ソース化・分割・テスト補完）は達成されたが、**Phase 6 の host 分割（BR-6/7）と Phase 3-3 の unwrap 一元化（BR-10）に回帰が残る**。
> - **2026-08-14 リファクタリング追跡調査・最終（ターン 50）**: 50 ターンで完了。**記録: BUG.md = BR-1〜10（10 件）+ 健全確認メモ + 調査総括 / OPTIMIZATION.md = OR-1〜23（23 件）**。検証実績: host 417 本 / browser-bridge 91 本 / goal-loop 84 本 / opencode-proxy 65 本 / git 77 本 / memory 44 本 / workflow 19 本 / SettingsView 32 本 / P2-b 5 本 / 8-3 45 本 / tsc クリーン / vitest 全 321 ファイルで 3858 PASS・9 FAIL（全て BR 対応済み）。作業ツリーは BUG.md / OPTIMIZATION.md のみ変更（`db.ts` / `useSessionStream.ts` は OneDrive mtime 同期のみ・差分ゼロ）。一時ファイル・git worktree は全て削除済み。**本調査は発見記録のみで、修正は行っていない**（指示どおり）。

> 検証環境: git HEAD `c47cd6f`（クリーン）、Windows / OneDrive / OpenCode 1.18.16
> スコープ: Composer・Goal Loop・メッセージ表示・設定画面の UI/UX 中心。
> 前回（2026-08-12）の 13 件（BH-1〜13）は全て修正済み。本ラウンドは**発見記録のみ・修正しない**。
> 検証: 対象コンポーネントの vitest（Composer/GoalLoopComposer/GoalLoopPanel/PartView/MessageMetaHeader/HomeView）155 テスト全 PASS の上で、コードレビュー + 実データ照合で以下を確認。

| ID | 優先度 | 要約 | 根拠 / 影響 |
|----|--------|------|-------------|
| **BU-1** | 中 | **HomeView で Workflow 開始モード中もループのトグル/承認条件が表示され有効化できるが、ループは開始されない（無言の無効）** | `web/src/components/home/HomeView.tsx`。ツールバーの `GoalLoopToggle`（`:1600`）は常時表示、`goalLoopEnabled` なら `GoalLoopOptions`（`:1487`）も表示される。一方 `submit()` の goal-loop 起動は `goalLoopEnabled && startMode === "task"`（`:1017`）に限定され、Workflow 変換は `startMode === "workflow"`（`:1005`）のみ実行。`startMode` 切替時に `goalLoopEnabled` をリセットする処理が無いため、Task で ON にした後に Workflow へ切り替えてもトグルは青いまま・承認条件欄も出たまま送信され、ループは**静かに**無視される（acceptance / maxTurns / 完走モードが全て無視）。ユーザーはループが動いていると錯覚する。→ 開始モードと排他にする（Workflow 時はトグル非表示 or 切替時に OFF 化）か、送信時に警告する |
| **BU-2** | 中 | **プロジェクト設定のタブ切替で編集内容（`draft`）が他タブの内容のまま残り、誤ったファイルへ保存され得る** | `web/src/components/settings/ProjectSettingsView.tsx`。`files` / `agents` / `skills` 3 タブが単一の `draft` state（`:55`）を共有。`loadFiles` はマウント時のみ（`:125-127`）で、agents/skills への切替では `loadAgents`/`loadSkills` が `draft` を上書き（`:129-132`）。エージェントタブ→設定ファイルタブへ戻しても `loadFiles` は走らず、**AGENTS.md の編集欄に最後に開いたエージェント/スキルの内容が表示されたまま**「保存」で AGENTS.md へ書き込まれる（誤書き込み＝データ破壊リスク）。また未保存の編集内容がタブ切替で無警告に失われる（AgentsSettings.tsx は `baselineRef` で未保存編集を保持しており、非対称）。→ タブ切替時に当該タブの内容を再ロード or 未保存なら確認する |
| **BU-3** | 低 | **GoalLoopComposer の「最大ターン」数値入力がクリア時に即 1 へ強制され、自由に入力・編集できない** | `web/src/components/GoalLoopComposer.tsx:95`。`onChange` が `Math.min(100, Math.max(1, Number(e.target.value) || 1))` を毎キー実行するため、空欄にしようとすると即座に `1` が入り、既存値のクリア→新値入力ができない（例: `10` を消して `20` にしたいとき `1` に戻る）。GoalLoopPanel.tsx は編集時テキスト state に保持してこの問題を回避済みなのに Composer 側（Home/TaskView 共通）は未対応。テストはこの挙動を明示仕様として固定している（`GoalLoopComposer.test.tsx` の "falls back to 1 for junk"）ため、仕様変更が必要。同系: `VisionSettings.tsx` のタイムアウト入力も `Number("")=0` で表示が `0` に張り付く |
| **BU-4** | 低 | **Composer のハイライトミラーが初回出現時に textarea の scrollTop と同期されない** | `web/src/components/Composer.tsx`。ミラーへスクロールを同期する `useEffect`（`:150-157`）の依存が `[textarea.value, textarea.ref]` のみで、`hasHighlight` の出現契機を含まない。スラッシュ/エージェント候補が非同期ロード（`useSlashCommands`/`useAgents`）で後から届き `hasHighlight` が true になるケースでは textarea.value が変わらずミラーが scrollTop=0 で新規マウントされ、長文スクロール中はハイライト位置がズレる（次の scroll イベントまで）。またミラー末尾に常時 `"\n"` を付加するため 1 行分の高さ差が出得る |
| **BU-5** | 低 | **`stripGoalLoopJsonBlock` がゴールループ以外の assistant テキストにも適用され、末尾の ```json {status:...} が表示から消える** | `web/src/components/task/PartView.tsx:599` で role=assistant の**全**テキストパートに適用。`useSessionStream.ts:451` の判定は「末尾の ```json ブロックのパース結果に status=progress/completed/blocked/verified_completed が含まれる」のみで、ループ実行中か否かを考慮しない。通常の（非ループ）会話でモデルが末尾に例示としてそのような JSON を書いた場合も表示から剥がれる。頻度は低いが「ループ専用」の表示ロジックが全 assistant メッセージに掛かる非対称 |
| **BU-6** | 低 | **`/スキル` トークンの末尾句読点の扱いがコンポーザーと送信済みメッセージで非対称（`@エージェント` は揃っている）** | コンポーザーの `findSkillTokens`（`web/src/lib/slash-command.ts:147`、`/(^|[\s])\/([^\s/]+)/g`）は**末尾の句読点を含めて**トークン照合するため、`/bug-hunt,` のように直後に `,` `.` `!` 等が続くと「`bug-hunt,`」で見つからずハイライトされない。一方 `MessageTokenHighlight`（`web/src/components/task/MessageTokenHighlight.tsx:27`、`[A-Za-z0-9._-]+`）は句読点で切り出して照合するため、**送信後の**メッセージでは同じ `/bug-hunt,` が青字ハイライトされる（node で実測確認: composer=`["bug-hunt,"]` / message=`["bug-hunt"]`）。`@agent` は `findAgentTokens`（`agent-mention.ts:118` の `TOKEN_CHARS` trim）と message 側の両方が切り出すため一致している。→ コンポーザー側も `findSkillTokens` に句読点トリムを入れる or message 側と照合ロジックを統一する |
| **BU-7** | 低 | **プロファイル設定の「連携を適用」成功メッセージが危険（danger）アラートで表示される** | `web/src/components/settings/ProfilesSettings.tsx:406` の `applyDependencies` は成功時 `setActionError("WebUI依存を追加しました…")` で**エラー用 state** に書き込む。レンダリング（`:539`）は `border-danger/30 bg-danger-bg text-danger role="alert"` のため、正常完了の通知が赤い危険アラートとして出る（スクリーンリーダーにも alert で読み上げ）。また成功メッセージは次アクション開始まで残る。さらに `installed.length === 0`（適用済み）の場合は何も表示されず操作の応答が無い。→ 成功用の通知チャネル（`hint`/`notice` 等）を分ける |
| **BU-8** | 低 | **ゴールループ開始時の送信ボタン無効化が HomeView と TaskView で非対称（添付ありで無効化されない）** | HomeView は `disabled` に `(goalLoopEnabled && startMode === "task" && (!prompt.trim() || attachments.length > 0))`（`HomeView.tsx:1618-1620`）を含め、ループ ON＋添付ありならボタン自体を無効化する。一方 TaskView の送信ボタンは `!input.trim() || !task.sessionId || goalLoopBusy`（`TaskView.tsx:5274`）のみで添付を考慮しない。ループ ON＋添付＋ゴールテキストの状態でボタンは**有効**のままになり、クリックすると `send()`（`TaskView.tsx:2561`）が初めて `"ループでは添付ファイルを利用できません…"` のエラーを出す。同一操作（ループ開始＋添付）で「ボタン無効（Home）」と「クリック後にエラー（TaskView）」に分かれる。→ TaskView 側も添付ありなら無効化 or 添付ボタンを非表示にする |
| **BU-9** | 低 | **分割ビューで同一コンポーネントが複数マウントされると固定 DOM id が重複する** | `AppShell.tsx` は `splitActive` 時に primary + secondary の 2 つの `TaskView`（`SplitTaskView`）を同時マウントする。この下位で固定文字列の id を持つ要素が重複する: `SlashSuggestMenu.tsx:34` の `id="slash-suggest-listbox"`、`AgentSuggestMenu.tsx:34` の `id="agent-suggest-listbox"`（Composer の `aria-controls` が参照）、`GoalLoopPanel.tsx:348` の `id="goal-loop-detail"` と `:282` の `id="goal-loop-stop-confirm"`、`SessionActions.tsx:398` の `id="message-revert-confirm"`（`aria-controls` が参照）。両ペインで同時にスラッシュ候補を開くと `aria-controls` が同 id の先頭要素にしか解決されず、AT のラベル付けや自動テストのセレクタが曖昧になる。クラッシュはしないが HTML の id 一意性違反。他の場所（`GhostSelect`・`NextAction`・`AutoRouteOverridesEditor` 等）は `useId()` を使っているのに非対称。→ `useId()` 化する |
| **BU-10** | 低 | **メインの会話タイムラインで複数画像が横並びにグループ化されず縦積みになる（NestedAgentPanel と非対称）** | メインタイムライン（`TaskView.tsx:4717-4718`）は `m.parts.filter(...).map((p) => <PartView/>)` で各パートを独立ブロック描画し、`groupImagePartsForRender`（`web/src/lib/message-parts.ts`）を使わない。一方 `NestedAgentPanel.tsx:362` は `groupImagePartsForRender` で連続する画像パートを `flex-wrap` の横並びにグループ化する。ユーザーメッセージに複数画像を添付すると、メイン会話では 112px の `FileImagePreview` が縦に重なり大きな縦空白を消費するのに対し、サブエージェントパネルでは横並びになる。`groupImagePartsForRender` は NestedAgentPanel 専用に導入された形跡があり、メインタイムラインへの適用漏れ。→ メインタイムラインにも同様に適用して表示を統一する |

**2026-08-13 ラウンドの総括**: Composer / ゴールループ / メッセージ / 設定画面の UI/UX を 50 ターンで網羅的に調査し、**新規バグ 10 件（BU-1〜10）** を BUG.md に記録した（修正は行わない方針）。中優先度: BU-1（Workflow 中ループトグル無効）、BU-2（プロジェクト設定のタブ切替で draft 混線・誤保存リスク）。低優先度: BU-3〜10（数値入力クランプ、ハイライトミラー同期、ゴール JSON の過剰除去、スキルトークン句読点の非対称、成功通知の danger 表示、ループ開始ボタンの無効化非対称、分割ビューの重複 DOM id、メインタイムラインの画像未グループ化）。健全確認のため再調査防止メモを多数追記。コミットは全て BUG.md のみ（他エージェント差分は混入なし）。

## 修正状況（2026-08-13 全 10 件対応）

BU-1〜10 を優先度順に修正済み。各修正は `tsc --noEmit` / `eslint`（対象ファイル）で検証し、関連テストを実行してコミット。

| ID | 修正内容 |
|----|----------|
| BU-1 | HomeView: Workflow 開始モードではループトグル/承認条件を非表示にし、Workflow 切替時に `goalLoopEnabled` を OFF 化（`startMode` onChange とトグル/`GoalLoopOptions` の表示条件） |
| BU-2 | ProjectSettingsView: タブ別 draft（`draftByTab`）に分離し、agents/skills は初回訪問時のみ fetch。タブ往復での他タブ内容の誤表示・誤保存と未保存編集の喪失を防止 |
| BU-3 | GoalLoopComposer: 最大ターン入力を string draft 化し、クリア→再入力を可能に。blur/Enter で clamp 確定（テスト更新＋新テスト追加） |
| BU-4 | Composer: ハイライトミラーのスクロール同期を `useLayoutEffect` 化し `hasHighlight` を deps に追加。非同期ロード後の初回出現でも同期 |
| BU-5 | PartView: `stripGoalLoopJson` プロパティを追加し、ゴール JSON 除去をループタスク（`goalLoop !== null`）限定に（テスト追加） |
| BU-6 | `findSkillTokens`: 末尾句読点を `TOKEN_CHARS` でトリムし、送信済みメッセージのハイライトと一致（テスト追加） |
| BU-7 | ProfilesSettings: `actionSuccess` を追加し、連携適用の成功通知を success 表示に分離（適用済み時も通知）。各アクション開始時にリセット |
| BU-8 | TaskView: ループ開始ボタンの disabled 条件に `attachments.length > 0` を追加（HomeView と一致） |
| BU-9 | Composer/SlashSuggestMenu/AgentSuggestMenu/GoalLoopPanel/SessionActions: 固定 DOM id を `useId()` 化（分割ビューでの重複・`aria-controls` 曖昧さを解消。テスト更新） |
| BU-10 | TaskView: メインタイムラインのパーツ描画を `groupImagePartsForRender` でグループ化（NestedAgentPanel と統一。テスト追加） |

検証: 各修正で `npx tsc --noEmit`（web）成功 / `npx eslint`（対象ファイル）警告 0 / 関連 vitest（Composer 8・GoalLoopComposer 10・PartView 20・slash-command 18・ProfilesSettings 11・TaskView 139・メニュー/ループ/revert 69 等）全 PASS。

## 修正状況（2026-08-12 全件対応）

以下の全 13 件を修正済み。検証: `tsc --noEmit` / `eslint .`（既存警告 1 件のみ）/
vitest 287 ファイル・3561 テスト / host 395 テスト / browser-bridge 91 テスト すべて PASS。

| ID | 修正内容 |
|----|----------|
| BH-1 | `WorkflowGraphCanvas.tsx` のデバッグ fetch 3 箇所を削除 |
| BH-2 | `sw.js` を v6 化: ビルドID 変更時に実キャッシュ（`CACHE`）を wipe、`/_next/` をキャッシュしてオフラインシェルを成立。`sw.test.js` / `service-worker.test.js` を新挙動に更新 |
| BH-3 | `filterRevertedMessages` を message ID の文字列比較から配列 index 境界方式に変更 |
| BH-4 | `sync-profiles.mjs` の Claude settings JSON パースを try/catch 化（壊れた JSON でもクラッシュしない） |
| BH-5 | `path-validation.ts`: 他ユーザーのプロファイルを `includesDescendants: true` で保護 |
| BH-6 | `api-guard.ts` の `Origin: null` コメントを実装と一致する内容に訂正 |
| BH-7 | `pty-session/input/route.ts` の重複コメントを削除 |
| BH-8 | プロキシの `cachedProvidersByDir` / `cachedAgentsByDir` に 64 エントリ上限の LRU 風 eviction を追加 |
| BH-9 | サーバー側トランスクリプト読取に `normalizeOcList`（`{data:[...]}` 吸収）を適用（goal-loop / workflow-scheduler / collaboration-context / memory-extract / task-service）。画像事前解析は v1 `sessionMessagePath` に固定＋応答の `{data}` ラップ防御 |
| BH-10 | `evaluateWorkflowGraphRuntime` の write_conflict 対象 node を `blockedNodeIds` に追加（無言停滞を pause 化） |
| BH-11 | `agents-sync.mjs` の `symlinkDir` を web エンジンと同型に: 実体ディレクトリ/ファイルは throw（再帰削除しない） |
| BH-12 | `stopProcessTreeGracefully` のデッドコード `isAlive(pid) ? 'hard' : 'hard'` を `return 'hard'`＋文書化に整理 |
| BH-13 | `browse/dirs` の files=0（ホーム配下の列挙）を loopback 限定に（`isLocalHostRequest`、api-guard-coverage の制約に適合） |

---

## 検証で実施したこと

- `tsc --noEmit`（web）: **成功**（型エラーなし）
- `eslint .`（web）: 成功（警告 1 件のみ: `opencode-generation.settings.test.ts:13` の未使用変数）
- `vitest run`（web）: **287 ファイル / 3561 テスト PASS（1 skip）**
- `node --test`（host）: **全 PASS（fail 0）**
- `node --test`（browser-bridge）: **91 テスト PASS（fail 0）**
- 手動レビュー: `web/src/lib` / `app/api` / `components` / `host/src` / `browser-bridge` / `scripts`

自動テストは全て通っており、以下は**テストで検出されない**ロジック / 実装上の問題を
手動レビュー + 実データ照合で確認したもの。

---

## 優先度基準

| 優先度 | 基準 |
|--------|------|
| **高** | セキュリティ侵害・データ破壊・コア導線が壊れる・本番コードに調査用残骸 |
| **中** | 実害あり（条件付き / 頻度限定 / 機能の意図未達） |
| **低** | 文言ズレ・レア edge ケース・堅牢性のみ |

---

## 高（すぐ直す）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **BH-1** | `WorkflowGraphCanvas.tsx` にデバッグ用 `fetch` が **3 箇所**残存（コミット済み） | `web/src/components/task/workflow-graph/WorkflowGraphCanvas.tsx:92-96`（レンダリング毎）、`:128-130`（viewport move 終了）、`:174-176`（move 開始）。毎回 `http://127.0.0.1:52338/ingest/8d185c` へ `{graphRevision, viewport, nodePositions, direction}` を POST。ハードコードされた調査用エンドポイント（`hypothesisId:'A,B,C'` 等はデバッグ痕跡）。ポート 52338 に常駐がない環境でも毎レンダリング fetch が走り、グラフ内部状態を外部へ送る。リポジトリルートの `.debug-agent-start.log`（`/ingest/1c340a`）も同じ調査セッションの残骸。→ 削除すべき |
| **BH-11** | `scripts/agents-sync.mjs` の `symlinkDir` が、ミラー先に**実体ディレクトリ/ファイルが存在する場合に再帰削除**してジャンクションで置き換える（データ破壊） | `scripts/agents-sync.mjs` の `symlinkDir`: `if (fs.existsSync(linkPath) || fs.lstatSync(linkPath, { throwIfNoEntry: false })) { fs.rmSync(linkPath, { recursive: true, force: true }); }` は存在するものを**無条件に再帰削除**。ユーザーが `~/.claude/skills/<name>`（または codex/agents 側）に自分で作った実体ディレクトリを、`agents-sync` 実行時に消してジャンクションに置き換える。さらに `plan()`（`--check`）は同一ケースを `exists but is not the correct symlink (blocked)` かつ `wouldChange: false` と報告して「変更しない」と言うのに、`apply()` は破壊する（チェックモードが誤報）。**web 側エンジン `web/src/lib/profiles/agents-sync-engine.ts` の `symlinkDir` は `existing.isSymbolicLink()` でなければ `throw` して保護しており、CLI と非対称**。→ CLI も throw する（または `plan()` の blocked と一致させる）こと | 
| **BH-9** | **OpenCode API 世代を v2 に切り替えると、サーバー側のトランスクリプト読み取りが複数機能で壊れる**（v2 のメッセージ応答が `{data:[...]}` ラップのため） | OpenCode v2 の `/api/session/{id}/message`（`SessionMessagesResponse`）は `{ data: Message[], cursor }` 形式。`ocServer()` はラップを解除しない（BFF プロキシ経由は `maybeUnwrapV2Data` が解除するが、サーバー内部の `ocServer` 直呼びは非対象）。`activeSessionMessagePath()` が v2 に解決される設定（Settings→Engine で v2 選択、`isV2ApiGeneration()` がサーバーでも true）で以下が発現: (1) **goal-loop** `goal-loop.ts` `boundaryStartIndex` が `{data:[...]}` に `.findIndex` を呼び TypeError → スケジューラが loop を `scheduler_error` で pause（ループ機能停止）。(2) **workflow** `workflow-scheduler.ts:372` `if (!Array.isArray(messages)) return;` → attempt が `running` のまま**永遠に進行しない**（無言ハング）。(3) **collaboration-context** `collaboration-context.ts` が TypeError を catch して空ブロックを返す（機能喪失は無言）。(4) **memory-extract** `memory-extract.ts` が `.filter`/iteration で TypeError（抽出がタイムアウト/失敗）。(5) **cost 推定** `task-service.ts` `exactMessageCost` が iterate で TypeError（コスト表示が degraded、`/api/tasks/:id/cost` が 500 になり得る）。(6) **画像事前解析** `qwen-native-vision.ts:223` が `activeSessionMessagePath`（v2 では GET-only の message パス）に **POST** する v1 依存 → v2 では 405 で失敗。`hang-watchdog.ts` だけは `normalizeMessageList` で両形式を吸収済み（非対称）。→ 修正は `ocServer` 側か各 consumer に `normalizeMessageList` 相当の吸収を入れること。**実機確認（2026-08-12、OpenCode 1.18.16）**: `GET http://127.0.0.1:4096/api/session/<id>/message?limit=2` → `{"data":[],"cursor":{"previous":null,"next":null}}`（`{data,cursor}` ラップを確認）。`POST /api/session/<id>/message` → 200 だが **SPA の HTML が返る**（v2 message は GET-only のため POST はフォールスルー）→ 画像事前解析の `activeSessionMessagePath` への POST（qwen-native-vision.ts）は v2 で必ず失敗する |

## 中（次に直す）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **BH-2** | Service Worker の「ビルドID によるキャッシュ無効化」が実質 No-op＋オフラインシェル不成立 | `web/public/sw.js`。`wipeBuildCache()` は `opencode-webui-build`（`BUILD_CACHE`）だけを削除するが、navigation / 静的アセットを実際に書き込むのは `opencode-webui-v5`（`CACHE`）。`activate` ハンドラが `CACHE` 以外を全削除するため `BUILD_CACHE` は常に空で、誰も書き込まない。結果、コメントが意図する「デプロイ後の古いキャッシュ HTML（削除済み `_next` chunk 参照）による白画面防止」は未達（古い `/` が `CACHE` に残る）。さらに `if (url.pathname.startsWith("/_next/")) return;` で `/_next/static/*.js` を一切キャッシュしないため、オフライン時はキャッシュ済み HTML だけが返り JS が読めず白画面になる＝「オフラインアプリシェル」機能が実質動作しない。 |

## 低（後でよい）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **BH-3** | `filterRevertedMessages` が message ID の文字列大小比較に依存（テストなし） | `web/src/lib/useSessionStream.ts:379-398`。`m.info.id > revert.messageID` の `<`/`>` 比較は、OpenCode の message ID（`msg_<base36タイムスタンプ>…`）では**現行は**時系列順と一致することを実 DB（`opencode.db`）で確認済み（タイムスタンプ部が固定長 base36 のため）。ただし ID 形式への暗黙依存で、単体テストが存在しない。形式が変わると revert の非表示が静かに壊れる。 → テスト追加 or 配列 index 境界方式への変更を検討 |
| **BH-12** | `stopProcessTreeGracefully` の最終 return がデッドコード（ハードキル後も生存判定を破棄） | `host/src/process-stop.js` の `return isAlive(pid) ? 'hard' : 'hard'` は両分岐とも `'hard'` を返し、ハードキル後もプロセスが生存しているかを呼び出し元へ伝えない（生存なら `'hard'` でなく別値を返すべき）。現状の呼び出し元 `host/src/index.js` は `'hard'` をログ文言にしか使わないため実害なし。→ `return isAlive(pid) ? 'hard' : 'hard'` を生存判定で分岐させるか、戻り値の意味を文書化する |
| **BH-13** | `GET /api/browse/dirs`（files=0）が「host-only」コメントに反し、認証済みリモートからホストのホーム配下ディレクトリ名を列挙できる | `web/src/app/api/browse/dirs/route.ts`。コメントは「Directory enumeration is host-only … require a loopback caller」と述べるが、実際は `requireAuthorized(req)`（loopback または有効セッション）。リモートでログイン中の利用者は `files=0`（プロジェクトピッカー）でホストの `os.homedir()` 以下のディレクトリ名一覧を読める。`files=1`（in-task FileTree）は `assertAllowedDirectory` で制限済み。ディレクトリ名のみ・認証必須のため実害は低いが、コメントの意図（loopback 限定）と実装が不一致。→ `rejectUnlessLocal` にすべきか、コメントを訂正するか整理する |
| **BH-10** | workflow graph の `write_conflict` 時、writable node が「ready から外れるだけ」で `blockedNodeIds` にも入らず、ワークフローが pause もせず無言で停滞し得る | `web/src/lib/workflow-graph-runtime.ts` の `evaluateWorkflowGraphRuntime`。`writableReady.length > 1` のとき `readyNodeIds` から除外し `pauseReason = "write_conflict"` を立てるが、対象 node を `blockedNodeIds` に**追加しない**。`workflow-scheduler.ts` は `blockedNodeIds.includes(node.node_key) && graphRuntime.pauseReason` のときだけ `pauseWorkflowForAttempt` するため、write 競合 node はスキップされ続け UI に理由が出ない。**ただし** `validateWorkflowGraph` が `parallel_write_nodes` エラーで事前に弾くため、現状は検証を通過した正常 graph では到達しない防御レイヤのみ（実害は低いが、例外経路の検査漏れ）。 |
| **BH-4** | `scripts/sync-profiles.mjs` が壊れた Claude settings JSON でクラッシュ | `planSync()` / `applySync()` の `JSON.parse(readFileSync(CLAUDE_SETTINGS))` が uncaught exception。マスター `opencode.jsonc` は `readJsonc()`（コメント除去・trailing comma 許容）で読むのに、`~/.claude/settings.json` が壊れているとスタックトレースで死ぬ。→ try/catch でエラーメッセージ化すべき |
| **BH-5** | `resolveValidatedAllowlistPath` が他ユーザーのプロファイル子孫を非保護 | `web/src/lib/path-validation.ts`。`getProtectedPaths()` は自ユーザー以外のプロファイル（`C:\Users\OtherUser`）も `includesDescendants: false` で追加するが、その**子孫**（例: `C:\Users\OtherUser\AppData`）は保護されない。自ユーザーのプロファイル子孫を許可するのは意図的（コメント明記）だが、他ユーザーのプロファイル子孫まで許可するのは深層防御の穴。他ユーザー配下を `includesDescendants: true` にする等を検討 |
| **BH-6** | `api-guard.ts` の `Origin: null` コメントが実装と逆 | `web/src/lib/api-guard.ts` `rejectCrossSite`。実装は `Origin: null`（sandboxed/opaque origin）を `403` で拒否しているのに、コメントは「absent と同様に扱い auth に委ねる」と書いてある。挙動は安全側なので実害なし。コメント修正のみ |
| **BH-7** | `pty-session/input/route.ts` に同一コメントが 2 重 | `web/src/app/api/pty-session/input/route.ts`。「Measure UTF-8 bytes…」のコメントが 2 回並ぶ。実害なし。削除のみ |
| **BH-8** | `opencode/[...path]` プロキシの `cachedProvidersByDir` / `cachedAgentsByDir` が無期限に成長 | `web/src/app/api/opencode/[...path]/route.ts`。directory キーでキャッシュを SET するのみで削除/有効期限なし。多数のプロジェクトを開くとメモリ増加＋古い capability（モデルの画像対応が落ちても stale 判定で fail-open 側に残る）リスク。`GET_RESPONSE_CACHE`（5s TTL）と非対称 |

---

## 調査して「バグではない」と確認した候補（再調査防止メモ）

- **BU ラウンド（2026-08-13）追加レビュー（健全・新規確定なし）**: ターン1 `AccessModeSelect` / `SkillPermissionSelect` / `IntelligenceSelect` / `AutoOptimizeSelect` / `AgentSuggestMenu`（GhostSelect ラッパーで親が状態制御）・`ProviderModelsSettings` の並び替え（`saveOrder` はキュー直列化、`moveItem` はコピー返却で state を変異させない）。ターン2 `NestedAgentPanel`（子セッション照合・sticky match・BFF unwrap で v2 `{data}` は解決済み）・`PermissionCard` / `QuestionCard` / `TurnNoticeBanner`（busy/世代ガード）・`message-parts.ts`。ターン3 `AutoRouteOverridesEditor`（explicit/preset 切替、empty override 防止）・`ExtensionsSettings`（`buildSkillTree` の group→skill 昇格は順序依存なし、プラグイン options はサーバへ送らず維持）・`BrowserBridgeApprovals`・SettingsView「接続」タブ（VPN/CA証明書/ファイアウォール）。ターン4 **goal-loop の進捗/一時停止 UI を実データで照合**（稼働中ループへ `GET /api/tasks/<id>/goal-loop`）: `status=running / turnCount=4 / maxTurns=50 / forceFullRun=true / progress=3件` で、`progress[].time` は ISO 文字列（`normalizeStructured` が必ず `new Date().toISOString()` を付与）、`status/summary/next/evidence` がクライアント `GoalLoopDto` と完全一致。`turnCount`(4) と progress 件数(3) の差は実行中ターン未確定の正常差。`normalizeStructured`（`goal-loop.ts:1107`）と pause 理由の列挙は UI の `PAUSE_REASON_HINT` / `STATUS_LABEL` と一致。ターン5 **Composer 送信経路**（TaskView `send()` のキュー/割り込み・`queuedAutoSend` 二段 effect・`queueHoldScopesRef`・`sendingScopeRef` の二重送信ガード・失敗時 `rememberComposerDraft` 復元・auto-compact の 409/曖昧失敗時のメッセージ）・`changeAccessMode` / `changeSkillPermission` / `changeSubagentPermission`（saving state で二重 POST 防止、失敗時 sendError）→ 全て健全。`action` ボタンは `working` 中に stop へ置換され、キュー送信は Enter 経由のみになる点は意図的仕様（stop 優先）と判断し記録対象外。ターン6 **SSE 反映/再接続 UI**（connection 状態遷移 connecting/reconnecting/down/live・`message.updated` / `part.updated` / `session.status` / `session.idle` / `session.error` の適用・`sessionError` バナー・Auto 選定バナー・`useSessionActions` compact/revert/unrevert・`lastRevertMessageId` 導出）→ 全て健全。ターン7 **AddProjectButton / HomeView プロジェクトピッカー / SessionSwitcherDialog**（ネイティブピッカーの host-only ゲート、in-app ピッカーの reqId 失効・二重送信ガード・エラー日本語化、`/api/browse/dirs` の応答形状 `{path,parent,entries:[{name,path}],quickAccess:[{name,path}]}` を稼働サーバで実測照合: `path=C:\Users\Daichi / entries=51 / quickAccess=18`）→ 全て健全。ターン8 **PartView の tool 描画**（`humanizeToolOutput` の `<task_result>` 抽出・XML ノイズ除去、`tool-part-summary.ts` の question 要約/フィールド）・SettingsView git タブ（コミット作者 onBlur 保存・空で unset）・goal-loop `PATCH updateMaxTurns` のサーバ clamp（`goal-loop.ts:800` `Math.min(100, Math.max(1, Math.trunc(raw)))`）とクライアント clamp の整合・DiffPane のヘッダ/ロード/コミット（reqId 失効・directory 切替リセット・filter と `selectedPaths` のコミット all 判定ガード）→ 全て健全。ターン9 **DiffPane の side-by-side / ファイル一覧 / セッションフィルタ / baseCompare**（ロード時の reqId・`externalChange` の「セッション外?」バッジ・`MAX_LINES_PER_FILE` トランケーション）・TaskView の **Auto 選定バナー/ワンショット昇格リトライ**（`autoRecord.dismissed` / `autoRetryNotice` / `autoFollowUpNotice` の優先順、`autoRetryFiredRef`＋`record.retried`＋`hasCompletedAssistantText`＋`userMessageCount>1` の一回限りガード）→ 全て健全（side-by-side は削除/追加行を単純 1:1 対にする既知の簡易ペアリングで、hunk が不均衡な場合の見た目ズレは許容）。ターン10 **SettingsView の再起動/更新フロー**（`restartService` のヘルス確認・`restartHealthTimeoutMessage` の分岐、`updateService` の timeout/結果表示）・`skill-catalog-labels.ts`・TaskView の画像添付（`imageInputAvailable` は agent 考慮の `effectiveModelKey` で HomeView と整合、`showImageWarning`）・`MessageMetaHeader` の effort は `task.variant`（session の current/last model variant で per-message ではなく設計どおり）・`filterCompactionContinueMessages` / `visibleMessages` の scope ゲート → 全て健全。ターン11 **HomeView の初回モデル解決と Auto セレクタ連動**（`default-model.ts` の localStorage 高速経路＋server 永続化の write queue、サーバデフォルトの cross-browser 反映は「localStorage はキャッシュ・server が耐久源」の設計どおりで、hydration effect がローカル選択を上書きする挙動も複数ブラウザ同期の意図と整合・バグでない）・`model-options.ts`（`sortModelOptions` の provider バケット/version/intelligence スコア、`shouldDefaultDisableModel` の世代判定、`formatModelLabel` の "(latest)" 除去）→ 関連テスト 245 件（default-model 25 / model-options 26 / auto-model 194）PASS。ターン12 **TaskView の TodoPanel / トークン節約表示 / 完了報告警告**（`todosIncompleteAtFinish`・`gitDirtyAtFinish` のゲート、`forceOpen` のワンウェイ、`tokenSavingNotice` の suggest モード表示）→ TaskView テスト 138 件 PASS（canvas getContext の stderr は favicon-badge 起因の無害ノイズ）。ターン13 **スラッシュ/メンション候補**（キーボード操作とテスト 35 件 PASS）→ **BU-9 発見**（分割ビューで固定 DOM id 重複）。ターン14 **Markdown 描画の `.md` CSS**（コード/テーブル/ブロック引用、`pre` は `overflow-x:auto`・インライン `code` は `overflow-wrap:anywhere`）・**HostLogPanel**（2s ポーリング・`since` seq・`MAX_CLIENT_ENTRIES` 500 キャップ・非表示時 abort）・SettingsView のタブルーティング（users タブの Windows 認証説明等）→ 全て健全。ターン15 **PlanDocumentCard**（`approved` 履歴由来・`submitted` セッション内・`approvingRef`/世代ガード、path 変更時のリセット、エラー時の再試行）・**AgentsSettings**（`baselineRef` による再読込時の未保存編集保持、selectAgent 切替は save 前提の仕様、`AgentListRow`/`ModelLabel`/tone）→ 全て健全。ターン16 **Composer 添付前処理**（`prepare-attached-image.ts` の 1536px 最大辺・`targetBytes` 900KB・段階的品質/縮小ループ・canvas 失敗時は元データ URL へフォールバック・透明 PNG→白 JPEG は文書化）・**HomeView エラーバナー**・**`useSessionStream` の `sendPrompt`/`sendCommand`**（parts 組立、`pendingMutationRef`・`mutationStartedAtRef`・`markLiveScope`、クライアント timeout 時に idle へ落とさず preferRest+resync、800ms safety-net resync）→ 全て健全。ターン17 **TaskView ヘッダ**（resync ボタン `manualResync`・ファイルツリー/グラフ/Diff/Markdown のサイドパネルトグル・分割クローズ・モバイル kebab への収納）・**HeaderKebabMenu**（`pointerDownOpenRef` による pointerdown→click レース回避、blur-close 抑制、Arrow/Enter/Escape/Tab ナビ、`useId` 使用）・**MobileMenuHeader**（ダブルタップで最上段スクロール）→ 全て健全。ターン18 **AddonSettings / addons 状態管理**（`addons/state.ts` の sanitizePrefs・legacy `webui:plugins` 移行・`writeAddonEnabled` の write queue 直列化、hydration 前はスイッチ disabled）→ 全て健全。ターン19 **goal-loop の pause/resume/stop 遷移をサーバ実装と実データで照合**: サーバ `updateGoalLoopStatus`（`goal-loop.ts:650`）の pause は `status IN ('queued','running','verifying_completed')`、stop は `NOT IN ('completed','blocked','stopped')`、resume は `status='paused'` のみ＋未知送達の回復（`deliveredGoalResultAfterUnknownPrompt`）・verification 中の resume は `verifying_completed` へ戻る・rejectedClaims 上限起因の pause は resume でカウンタ clear。クライアント `GoalLoopPanel` の `canPause`/`canResume`/`canStop` と完全一致。稼働中ループ（turn=19/50, running）への実測で `canPause=True / canResume=False / canStop=True` を確認。ターン20 **SettingsView エンジンタブ**（接続状態バッジ・再起動確認/進行表示・アップデート情報）・**todos フロー**（`todo.updated` SSE・REST `sessionTodoPath` の回復・`refreshTodos` の reqId ガード）・**コスト表示 `currency.ts`**（`sanitizeCostDisplayPrefs`・`clampUsdJpyRate`・`useCostDisplayPrefs` の FX auto 取得と `COST_DISPLAY_EVENT`/`storage` 同期・`formatYen` の桁表示）→ 全て健全。ターン21 **Composer のツールバー省略/モバイル**（`overflow-x-auto`＋右端フェード・`action` はスクロール外固定、`tabIndex=0` のフォーカス可視化）・**HomeView のモバイルレイアウト**（`max-w-5xl` 中央・`pt-64` はスラッシュ/メンション表示時の意図的シフト・`pb-safe`、auto-resize 上限 240px vs TaskView 160px）・**添付削除ボタン**（デスクトップ hover 表示・`max-sm:opacity-100`・`focus-visible:opacity-100`、大画面タッチ端末では hover なしでも `focus-visible` で到達可）→ 全て健全。ターン22 **SettingsView プロジェクトタブ**（アーカイブ/復元/完全削除の `guard` busy 直列化・エラー表示・`refresh`、確認ダイアログの `busy` 連動、ルート追加/削除の 404 回復）→ 全て健全。ターン23 **HomeView のプロジェクト切替時ブランチ表示**（`projectId`/`projects` 依存の `cancelled` ガード、`branchProjectId` との `branchReady` ゲート）・**GlobalAttentionProvider**（`useAttentionQueue` の reconcile、バックグラウンド権限の `permissionAutoAction` 自動処理、`wasRecentlyReplied` 除去、イベント/`storage` 同期）・**`match-child-session.ts`**（`matchChildSession` の explicit metadata → sticky → title 一致、単一 child の自動マップ拒否、`collectTaskCallIds`/`messageHasTimelineParts`）→ 全て健全。ターン24 **TaskView の split 時の activeScope 競合**（`shellActive = !splitActive || activeTaskId === taskId`、登録は `setActiveScope({directory,sessionId}, taskId)`＋unmount/切替で `setActiveScope(null, taskId)`、extras も owner id 付きで排他）・**Sidebar のお気に入りトグル**（`beginAction`/`endAction` のキー付き busy ガード、`toggleSessionFavorite` は `sessionId` 必須・`notifyTasksChanged`＋`refresh`、`timeAgo` 表示）→ 全て健全。ターン25 **HomeView のエンジン未接続時の挙動**（`submit()` は `!engineOk` で無言 return・送信ボタンは disabled・警告バナーは `loaded && !engineOk`・3s ポーリングで自己回復）・**稼働中タスクの実データ照合**（`GET /api/tasks` で goal-loop タスクが `working`、他は idle、variant/agent が UI 表示と整合）・**HomeView のモデル選択後の `modelTouchedRef` による hydration 上書き防止** → 全て健全。ターン26 **SessionSwitcher**（`focusSession` の rebind＋`syncSessionPermissions` を `onSwitch` より前に await、切替失敗時は `refresh()`＋`localSelection` 復元、`toggleFavorite` は楽観更新＋失敗時ロールバック、`localSelection` は親追従まで保持）・**DiffPane の `externalChange` バッジ**（`touchedPaths` 提供時のみ・セッション外編集の警告）→ 全て健全。ターン27 **HomeView の isolation/worktree フロー**（`valueLabel` は current_folder で `defaultBranchLabel`（読み込み中/実際のブランチ/現在の HEAD）、submit とボタンは `branchProjectId === projectId` でゲート、失敗時 `baseBranch=""`＝HEAD 使用の安全側）・**プロジェクト追加後のピッカー更新**（GhostSelect `action` の AddProjectButton→`refreshProjects`→`setProjectId(project.id)`、action 領域は pointerdown で閉じない設計）→ 全て健全。ターン28 **ProviderModelsSettings の ModelPricingEditor**（`parse` の input/output 必須＋cachedInput/cacheWrite 任意の検証、エラー文言、open/close 時の最新 pricing への draft 再同期、`onSave(null)` で価格クリア、`saveModelPricing` の楽観更新＋API 失敗時 reload）→ 全て健全。ターン29 **ProviderModelsSettings の Auto 設定連携**（`autoSettingsTouched` ref によるサーバ同期の上書き防止、`readAutoSettingsFromServer` の DB 優先＋localStorage 反映・未設定時マイグレーション、`AutoOptimizeSelect`/`AutoRouteOverridesEditor` の変更フロー）・**defaultModel/generationModel の DB↔localStorage 同期**（`touched` ガード、DB 優先、localStorage→DB マイグレーション）→ 全て健全。ターン30 **GoalLoopPanel とインラインプログレスの二重表示**（`showInlineGoalProgress` は live 状態のみ・展開時は summary が重複表示されるが冗長のみで不整合なし）・**`useSessionStream` の `abort`（停止）フロー**（`abortingScopesRef` による二重 abort ガード、`pendingMutationRef` 即時解除で composer 凍結防止、preferRest を resync まで保持して楽観 idle を REST busy で補正）→ 全て健全。ターン31 **`useAgents`**（directory 変更時に旧一覧が新 fetch 完了まで残る一時的な stale 表示は既知の軽微事項・cancelled ガードで旧 fetch の反映は防止済み）・**`HangTimeoutSync` / `hang-timeout.ts`**（`clampHangTimeoutMs` 10s〜30min・`reconcileHangTimeout` のサーバ値優先＋local 未設定時マイグレーション、イベント/`storage` 購読）→ 全て健全。ターン32 **Sidebar のタスク/プロジェクト一覧**（アーカイブ展開の localStorage＋server settings 同期（`persistSidebar`）、`archivedExpandedRef` による遅延 fetch（展開時のみ `/api/tasks/archived`）、空状態の表示、`archiveTask`/`archiveProjectAction` の `beginAction` ガード、active タスクのプロジェクト自動展開）→ 全て健全。ターン33 **ProviderModelsSettings のプロバイダ追加/編集フォーム**（`saveProvider` の `providerMutationRef` 二重送信ガード、icon-only 編集の PATCH 分岐、モデル行の `id|name` パース、保存後 `resetProviderForm`＋`load`、`editingProviderId` 中の id 入力 disable、再起動反映メッセージ）→ 全て健全。ターン34 **ProviderIcon**（`providerIconSrcForOpencodeId` のバンドルアイコン解決・破損時 CPU グリフへフォールバック、`MessageMetaHeader` の `key={providerID}` によるアイコン再試行）・**アイコン解決の一貫性**（ProviderIcon / ModelSelect / ProviderModelsSettings / ModelRankingSettings が同一 `providerIconSrcForOpencodeId` を使用）→ 全て健全。ターン35 **BU-10 を発見・記録**（メインタイムラインの複数画像が未グループ化で縦積み）。ターン36 **`useBodyScrollLock`**（モジュールレベル `activeLocks` カウンタでネストロック対応、`previousOverflow` は先頭ロック時のみ保持、`active` 切替で正しく復元）・**利用箇所**（AddProjectButton / QuestionCard ライトボックス / ProfilesSettings / SessionSwitcherDialog）→ 全て健全。ターン37 **BU-10 のテスト網羅を確認**（`message-parts.ts` に専用テスト無し、`PartView.test.tsx` は単一画像のサムネイル/ライトボックスのみで複数画像のグループ化は未カバー＝BU-10 がテスト非検出である根拠を確認）→ 記録は BU-10 のまま。ターン38 **StatusBadge**（`STATUS_META` が全 `TaskStatus` を網羅＋未知は `unknown` へフォールバック、pulse は working のみ）・**isolationLabel**（git_worktree/current_folder/temporary_copy/devcontainer の対応表）→ 全て健全。ターン39 **TaskView の `ensureSession` / `restoreSession`**（`taskActionBusy` の単一操作ガード、session 作成→workspace バインド→`refreshTask`、失敗時 `setSendError`、ヘッダ/本体の `aria-busy` 連動）→ 全て健全。ターン40 **ExtensionsSettings の MCP 一覧/認証バッジ**（`mcpStatusBadge` の `engineAvailable`→`pendingRestart`（再起動で反映）→`runtime`（connected/failed/needs_auth/needs_client_registration/disabled）の優先順、`McpDto` の `detail`（認証情報マスク）/`meta`（env 名のみ）表示、トグルは `onToggled`→`restartNeeded` 連動）→ 全て健全。ターン41 **SettingsView のユーザー管理**（`toggleWindowsAuth` の `setWindowsAuthEnabled` 反映、`addUser`/`removeUser` の入力検証・`authBusy` 直列化・成功/エラー通知、users タブ表示時に `refreshAuthUsers`、パスワード 4 文字以上のボタン disabled 連動）→ 全て健全。ターン42 **`events.ts` / Sidebar の変更通知リフレッシュ**（`notifyTasksChanged`→`webui:tasks-changed`→`refresh`、`refreshBusyRef`＋`refreshRequestRef` で連続通知を合流、visibilitychange 時の再取得、`persistSidebar` の DB ミラーは非 fatal）→ 全て健全。ターン43 **TaskView の composer draft キャッシュ**（`composerScopeKey = directory＋sessionId`、`useLayoutEffect` で切替前スコープを保存→新スコープを復元、unmount 時も保存、`COMPOSER_DRAFT_CACHE_MAX` の LRU 上限、送信失敗時は `rememberComposerDraft` で復元）→ 全て健全。ターン44 **HomeView のエラーバナー**（`error` は `role="alert"`＋danger 表示、添付サイズ超過/枚数超過・submit 検証（ループ添付禁止等）・送信失敗で設定、submit 開始時にクリア）・**MessageRevertButton の確認ダイアログ**（`aria-controls`/`aria-describedby`、Escape で閉じる、busy/世代ガード、失敗時 inline エラー）→ 全て健全。ターン45 **TaskView のモバイル kebab メニュー**（`mobileKebabGroups` の session グループ（停止/コンテキスト圧縮/巻き戻す/取消）・task グループ（Workflow 変換等）・panel グループの組み立て、`isMd` 時は空配列、`revertTriggerRef` のフォーカス退避、各 item の disabled/busy 連動）→ 全て健全。ターン46 **`model-variants.ts`**（`getIntelligenceVariants` は `INTELLIGENCE_KEYS` 順で declared かつ非 disabled のみ、`variants[key]` が undefined/`{}` のケースも有効扱い、未知キーは無視・`isIntelligenceVariant` は enum 検証）→ 関連テスト PASS。ターン47 **`sidebar-settings.ts` / `sidepanel-settings.ts`**（サイドバー geometry の JSON 束ね DB 永続化、write queue 直列化、DB＞localStorage 優先、parse の型検証、失敗時は localStorage フォールバック）→ 関連テスト PASS。ターン48 **ClaudeSubscriptionAuth**（OAuth popup 起動→`/api/provider/anthropic/oauth/authorize`、`waiting` 中の 2s ポーリング（最大 90 回・非表示時停止）、`connectionRequestBusyRef`/世代ガード、再認証/確認ボタン、エラー時の状態復元）→ 健全。ターン49 **OpenAISubscriptionAuth**（Claude と同型＋`isBrowserOAuth`（`type=oauth` かつ label に browser/ブラウザ）で方式選択、`/api/provider/openai/oauth/authorize`、接続判定は `connected` に `openai` を含むか）→ 健全。**注記**: `BrowserBridgeSettings` の「この接続を削除」はサーバ側 revoke 未実装（コード内 TODO）で純粋にローカル表示のみのため、再マウントで接続が復活する。既知の未実装として記録対象外。
- `filterRevertedMessages` の文字列比較 → 現行 ID 形式では正しく動作（BH-3 参照）。
- `isBlockedOpencodeWrite` が `/api/config` PATCH を塞がない → OpenCode 1.18.14 の schema には `/config`・`/global/config` のみで `/api/config` は存在せず、現状は塞がっている。
- `task-status.ts` / `dirstat.ts` / `git.ts` / `api-guard.ts` / `memory-*` / `pty-*` / `hang-watchdog.ts` / `goal-loop.ts` / `workflow-scheduler.ts` → いずれも実データ・テストと整合。
- 過去のインベントリ（`docs/bugs/2026-07-23-bug-inventory.md` の R35#1 等）は別ループで**修正済み**（`removeWorktree` 根一致拒否、`maskSecrets`、write ブロック追加、`writeCostDisplayPrefs` の部分更新マージ等を現コードで確認）。
- 本ラウンド追加レビュー: `browser-bridge/extension/*`（content-runtime.js: refs は MutationObserver で常に再構築・generation 照合あり、型イベントは React 制御 input で `.value` 直書きのため controlled component で onChange が効かない可能性はあるが、`InputEvent` を後送りするため実測未確認＝バグ確定せず）、`workflow-graph-runtime/validation/mutations/repository`、`useSessionStream` SSE ハンドラ / reducer / resync、`goal-loop` scheduler、`opencode-extensions`（agent-files/plugins/jsonc-edit/safe-move）全経路、`copy.ts` / `workspace-service` / `profiles/link` / `sw.js`。→ 新規確定は **BH-9**（v2 世代で複数機能破壊）と **BH-10**。
- **BH-9 のテスト非検出理由**: 各サーバーモジュールのテストは `registerServerOpenCodeApiGenerationResolver` を登録せず v1（bare array）前提のモックで通るため、v2 の `{data:[...]}` 形状がテスト網羅されていない（`rg` で確認: goal-loop / workflow-scheduler / memory-extract / collaboration-context / task-service の各テストに v2 参照なし）。`hang-watchdog.test.ts` のみ `{data:[...]}` の正規化テストあり。
- **BH-9 の到達可能性を確認**: `web/src/app/api/settings/[key]/route.ts` の `ALLOWED_KEYS` に `OPENCODE_API_GENERATION_SETTING_KEY` が含まれ、`normalizeSettingValue` が `v2` を受理する。つまり Settings→Engine で v2 を選択するとサーバー側 `isV2ApiGeneration()` が true になり BH-9 が発現する。**修正の参考**: `web/src/app/api/git/commit-message/route.ts` は v1 の `sessionMessagePath()` を明示使用しており、v2 設定でも壊れない（v1 ビルダー固定パターン）。goal-loop / workflow も同様に v1 固定 or `normalizeMessageList` 相当の吸収が候補。
- ターン3 追加レビュー: `host/src/control-server.js`（HMAC セッション・失効・`createLoginThrottle` の username/IP 二段スロットル・admin チェック、trusted-device フロー）→ 全て健全。`api/settings/[key]` ルート（key allowlist + 値正規化）→ 健全。`auto-model.ts`（tier 分類・cost band ・escalation）→ 健全。`useAgents.ts`（directory 変更時に旧一覧を一瞬表示し得るが軽微・バグ確定せず）。`build-web.mjs` / `web-build-mirror.mjs` / `dev.mjs` → 健全（stash/restore/discard と mirror のhard-link/copy 切替）。`opencode-id.ts` → 健全。TaskView の `activeScope`/`extras` は `shellActive`＋owner id で split 時の取り合いを正しく処理。
- ターン4 追加レビュー: `host/src/caddy-sites.js`（IPv4 リテラルのみ管理、loopback/ドメイン/カスタム port 保存、placeholder 除去、idempotent）→ 健全。`web/src/lib/profiles/copy.ts`（concurrency 付き copyTree、symlink 再現、verifyCopy）→ 健全。`scripts/agents-sync.mjs` → **BH-11 を発見**（web エンジン `agents-sync-engine.ts` と非対称で、実体ディレクトリを再帰削除）。`web/e2e/*.spec.ts` / `playwright.config.ts`（`.next-e2e` 分離 distDir）→ 健全。
- ターン5 追加レビュー: `ProviderModelsSettings`（auto-settings write path）・`ProfilesSettings` / `profiles/service.ts`（`switchBlockReason`・`isBusy` ガード・`swapLink`）・`goal-loop.ts` の `applyAssistantResult` / `expireStalledTurn` / turn カウント（fresh re-read、`turn_count < max_turns` の WHERE ガード、verification は turn 消費しない）・`memory/consolidate`（dryRun 既定、トランザクション、use_count マージ）・`agent-utils.ts`（rank パース）・`useSessionStream` の reconnect/resync（preferRest ホールドカウンタ、silenceWatch、CONNECTING stall ガード）→ いずれも健全、新規確定バグなし。
- ターン6 追加レビュー: `memory-key.ts`（trigram Jaccard・polarity 分離・identifier オーバーラップ閾値）→ 健全。`git-restore.ts`（cooldown・`--no-checkout` clone→`.git` move→`reset --hard`、失敗時の progress 記録と再試行）→ 設計妥当（zip インストールのローカル改変は `reset --hard` で失われるが意図的）。`opencode-extensions/mcp.ts`（name 検証・config ロック書き込み）・`addons/codexbar`（usage/tokens/providers ルートは `requireAuthorized` 済み、64MB/300file 上限＋キャッシュ）・`codexbar-auto.ts` → 健全。新規確定バグなし。
- ターン7: **BH-9 を実機で確認**。稼働中エンジン（127.0.0.1:4096, OpenCode 1.18.16）へ直接照会し、`GET /api/session/{id}/message` が `{"data":[],"cursor":{...}}` を返すこと、`POST /api/session/{id}/message` が 200 で SPA の HTML を返すこと（v2 message は GET-only）を確認。BH-9 の前提（v2 ラップ形状と画像解析の POST 失敗）を実証。`provider-models.ts` の書き込み経路（`validateIdentifier` / `{env:NAME}` 保存 / config ロック / `ensureConfigFile`）・TaskView の Todo 完了警告（`looksFinishedReport` ゲート）→ 健全。
- ターン8 追加レビュー: `memory-extract.ts`（`lastJsonBlock` / `parseExtractionJson` / `messagesAfter` の delta 抽出、`ALREADY STORED` 防御）→ 健全。`workflow-service.ts`（`createWorkflow` / `reattachWorkflow` / `updateWorkflowNode` の revision CAS）→ 健全。HomeView の submit 後段（workflow/goal-loop 変換と失敗時の DELETE ロールバック、`autoDecision` のループモデル解決）→ 健全。`NextAction.tsx`（generation ref による古い応答破棄）・`useSessionStream` の `session.next.text/reasoning.*` delta 適用（`upsertPart` の text 置換ロジック）→ 健全。新規確定バグなし。
- ターン9 追加レビュー: `project-agents.ts` / `project-skills.ts`（`SAFE_NAME`・symlink 拒否・`realpath` で root 内検証・2MB 上限）→ 健全。`skill-frontmatter.ts`（block scalar 対応の frontmatter 解析）・`skill-catalog-labels`・`CommandPalette.tsx`（Escape の open 時限定、attention モーダル重なり防止、`find/file` 検索の AbortController）→ 健全。新規確定バグなし。
- ターン10 追加レビュー: `SessionSwitcher.tsx`（既存切替でも `focusSession` → `syncSessionPermissions` → `onSwitch` の順で権限同期、失敗時 `refresh`＋選択復元、generation/requestId ガード）→ 健全。`AttentionQueueModal.tsx`（busyRef・404 は既応答として除去・フォーカス管理・Escape は busy 時無視）→ 健全。`MessageMetaHeader.tsx`（cost/tokens/effort 表示）→ 健全。新規確定バグなし。
- ターン11 追加レビュー: `workflow-graph-react-flow.ts`（disabled/unsupported 状態、edge active アニメーション、TB のみ自動レイアウト）・`workflow-graph-layout.ts`（dagre、feedback edge 除外）→ 健全。`difftint.ts`（キーワード→コメント→文字列の順、`(?<!&)#` の R15#3 対応）→ 健全（文字列内 `#`/`//` の誤 tint は軽量ハイライトの既知トレードオフ）。`git-pathspec.ts`（NUL/`..`/magic/glob/絶対パス拒否、`rejectWebuiMeta`）→ 健全（`..` を含むファイル名は誤拒否され得るが安全側）。`openai-pricing.ts` / `model-options.ts`（価格表・intelligence スコア・`shouldDefaultDisableModel` の世代判定）→ 健全。新規確定バグなし。
- ターン12 追加レビュー: `host/src/port-plan.js`（`netstat` の LISTENING 行・`:port` suffix 照合で `:40960` 等を除外）→ 健全。`host/src/restart-targets.js`（`resolveWebKillPids` の owned PID 優先＋`isOwnedListener` ガード）→ 健全。`host/src/process-stop.js`（dispose→soft→hard、`reapInheritedHolders`、`stopWebTreeSync`）→ **BH-12 を発見**（`stopProcessTreeGracefully` 最終行のデッドコード。実害なし・低）。`browser-bridge/extension/snapshot-safety.mjs`（sensitive 判定・opaque ref・generation 照合）→ 健全。
- ターン13 追加レビュー: `browser-bridge/extension/background.mjs`（ペアリング→authenticate、persisted sharedTabs の re-validate（phantom タブ防止）、command generation 照合・commandId dedupe、auto-share 権限チェック、Broker URL loopback 制限、`pairing_denied`/`NOT_PAIRED` 処理）→ 健全。`browser-bridge/mcp/memory-server.mjs`（web と同型のメモリ重複検査・FTS・scope 解決）→ 健全。`web/src/lib/aborted-resume.ts`（abort/silent ターン再開判定）・`completion-report.ts`（`完了報告` 行アンカー）・`events.ts` → 健全。新規確定バグなし。
- ターン14 追加レビュー: `PtyPanel.tsx`（SSE 再接続の指数バックオフ、`{t:"exit"}` で停止、`onResize` の初回 fit 同期、input は `keepalive`＋タイムアウト）→ 健全。`GoalLoopPanel.tsx`（pause/stop/resume/maxTurns 編集、`PAUSE_REASON_HINT` の未定義 key は hint 非表示になるだけ・クラッシュなし、Escape の confirm ダイアログ）→ 健全。新規確定バグなし。
- ターン15 追加レビュー: `FileTreePanel.tsx`（「上へ」は root 超えを `assertAllowedDirectory` が拒否、reqId ガード）→ 健全。`api/browse/dirs` → **BH-13 を発見**（files=0 が host-only コメントと不一致で認証済みリモートにホーム配下を列挙させ得る。低）。`quickaccess.ts`（Jump List パース・LNK 解決の PowerShell spawn、30s キャッシュ・並列 pending 抑制）→ 健全。
- ターン16 追加レビュー: `workflow-artifacts.ts`（opaque ref の `data:`/base64 拒否・origin 検証・`verifyBrowserBridgeScreenshot` の tab 共有/所有者照合）→ 健全。`workflow-git.ts`（snapshot の SHA-256 fingerprint）→ 健全。`MarkdownViewerPanel.tsx`（`collectMarkdownEntries` の file/text 分類・画像除外）→ 健全。`workflow-feature-client.ts` → 健全。新規確定バグなし。
- ターン17 追加レビュー: `Sidebar.tsx` のドラッグリサイズ（`clampWidth`・pointerup で `saveWidth`＋`persistSidebar`、cleanup で body cursor 復元）・プロジェクト展開永続化 → 健全。`provider-model-state.ts`（状態ファイルの検証済み読み込み・`withStateLock` 直列化・`knownModelKeys` の legacy grandfathered 扱い・`removeProviderState` の全トレース掃除）→ 健全。新規確定バグなし。
- ターン18 追加レビュー: `deploy/Caddyfile.example`（h3 無効化＋`Alt-Svc clear`、`@hostOnlyApis` の loopback 限定・`root.crt` のみ配布、placeholder hostname 除去）→ 健全。`build.bat`（ASCII-only・build guard・mirror distDir 解決・BUILD_ID 検証）→ 健全。`SessionSwitcherDialog.tsx`（フォーカス管理・Tab トラップ・Escape）→ 健全（`onSwitch` の await 漏れは既知・対象外）。新規確定バグなし。
- ターン19 追加レビュー: `goal-loop.ts` の `buildGoalPrompt` / `buildGoalContinuationPrompt`（完走モードは `status: progress|blocked` のみ・早期完了宣言を無視・turn 数を明示、通常モードは `completed`＋検証ターン言及、recent progress は full で最新5件・continuation で最新2件＋evidence）→ プロンプト間の整合性あり。`useSlashCommands.ts`（`/api/opencode/command` の normalize、directory 依存、cancelled ガード）→ 健全。`memory-idle.ts`（`updated_at` 閾値・ledger dedup・in-flight ガード）→ 健全。新規確定バグなし。
- ターン20 追加レビュー: `token-saving-settings.ts`（localStorage 一元＋server バックアップ、clamp、`shouldAutoCompact` ゲート）→ 健全。`goal-memory-hook.ts`（goal 完了時抽出の fire-and-forget・auto_extract ゲート）→ 健全。`workflow-executor-registry.ts`（executor 解決・legacy/snapshot 切替）・`npm-cli.ts`（npm-cli.js 解決の where.exe フォールバック）→ 健全。新規確定バグなし。
- ターン21 追加レビュー: `workflow-control-executor.ts` / `workflow.ts` の `evaluateReviewGate`（skipped/failed/blocked/pass/return_to_implement、blocking severities、`dedupeFindings` の key と並び）・`parseReviewResult` の schema 検証 → 健全。`next-action-text.ts`（transcript 上限・`sanitizeSuggestionCount`/`sanitizePreviousSuggestions`・`normalizeSuggestion` の引用符/番号除去・`extractAssistantText`）→ 健全。新規確定バグなし。
- ターン22 追加レビュー: `memory-extract.ts` の `resolveLightweightModel`（`chooseAutoModel` で light/cost 解決、失敗時 null）→ 健全。`auto-settings.ts`（localStorage 一元＋server バックアップ、設定キー別の write queue 直列化、`hasStoredAutoSetting` で server 値の上書き回避、storage/CustomEvent 購読）→ 健全。新規確定バグなし。
- ターン23 追加レビュー: `sidepanel-settings.ts` / `sidebar-settings.ts`（DB バックアップ・書き込み失敗は非 fatal）→ 健全。`useBodyScrollLock.ts`（ネスト lock カウンタで overflow 復元）→ 健全。`useAttentionQueue.ts`（`reconcileDirectory` の v2 keep セッション・`receivedAt > syncStartedAt`・recently-replied 除去、`shouldQueueAttention` で active セッション除外）→ 健全。新規確定バグなし。
- ターン24 追加レビュー: `sse-health.ts`（heartbeat 15s / silence 45s / upstream connect 20s / client CONNECTING stall 45s の大小関係整合）→ 健全。`MobileMenuHeader.tsx` → 健全。`host/src/auth-store.js`（sha256+salt HMAC・timing-safe 比較・role 正規化・username/password 検証）→ 健全（sha256 は localhost BFF 前提と明記）。新規確定バグなし。
- ターン25 追加レビュー: `useSessionStream` のタイムアウト定数（`SESSION_MUTATION_TIMEOUT_MS` 60s / `SESSION_COMMAND_TIMEOUT_MS` 295s / `mutationTimeoutForSend` は画像時 `IMAGE_ANALYSIS_SEND_TIMEOUT_MS` 以上）→ 整合。`host/src/secure-file.js`（icacls の `/inheritance:r`・owner/SYSTEM/Admin 再付与・失敗キャッシュ）→ 健全。`host/src/log-file.js`（単一行正規化・世代ローテーション・fs エラー吞み込み）→ 健全。新規確定バグなし。
- ターン26 追加レビュー: `dist-dir.ts`（NEXT_DIST_DIR が app 内に収まることの検証・絶対パス拒否）→ 健全。`api/git/merge`（`into=branch` の checkout→merge→元ブランチ復帰、コンフリクト時 `merge --abort` で復旧、`assertNoActiveWorkflowForDirectory` ガード、worktree 使用中 409）→ 健全。`install-root.ts` / `install-state.ts`（git 復元の progress/cooldown 記録）→ 健全。新規確定バグなし。
- ターン27 追加レビュー: `api/fx/usd-jpy`（`requireAuthorized`・失敗 502）→ 健全。`api/git/pr`（gh を argv 直呼び・`--version` 事前チェック・`push -u origin HEAD`→`gh pr create`・base の `assertSafeBranchName`、60s タイムアウト）→ 健全。`devcontainer.ts`（`.devcontainer` 検出）→ 健全。`project-session-store.ts`（セッション manifest の検証付き書き込み）→ 健全。新規確定バグなし。
- ターン28 追加レビュー: `ollama-provider.ts`（`resolveOllamaModelEntries` は能力取得失敗時に名前推定へ fallback、`ollamaProviderConfig` は `attachment`/`modalities` を付与、`upsertProviderEntry` で config 更新）→ 健全。`workflow-feature.ts`（settings 表→env→default の解決順、client/server の分離）→ 健全。新規確定バグなし。
- ターン29 追加レビュー: `fx-usd-jpy.ts`（JST 日付キーで 1 日キャッシュ・レート範囲検証・8s タイムアウト）→ 健全。`ollama-cli.ts`（WinGet Links フォールバック・`/api/show` 能力取得の 5s abort・`/api/tags` 実行判定・pull の JSON 行パース）→ 健全。`api/git/show` / `api/git/log`（`assertSafeCommitHash`・pathspec 検証）→ 健全。新規確定バグなし。
- ターン30 追加レビュー: `opencode-extensions/paths.ts`（`OPENCODE_CONFIG_DIR` override・`configDirStateKey` でプロファイル別の state 分離（realpath＋Windows 小文字正規化）・`opencodeConfigFilePath` の jsonc 優先）→ 健全。`opencode-extensions/http.ts` → 健全。新規確定バグなし。
- ターン31 追加レビュー: `opencode-extensions/skills.ts`（`walkForSkills` の深さ/件数/走査ディレクトリ上限・nested は view-only・`setSkillEnabled` の `resolveContainedPath`＋`moveEntrySafe`・engine `/skill` 説明で上書き）→ 健全。`client-ip.ts`（**右端**の XFF を使用＝攻撃者制御の左端を信用しない、IPv6 bracket/zone/`::ffff:` 正規化）→ 健全。新規確定バグなし。
- ターン32 追加レビュー: `localhost-redirect.ts`（loopback/private 判定・`127.0.0.1:18765/health` への no-cors プローブで到達時のみ redirect・public hostname は対象外・800ms タイムアウト）→ 健全。`api/qwen-native/settings`（`providerID::modelID` 形式検証・timeout 範囲クランプ・enabled 時はモデル必須）→ 健全。新規確定バグなし。
- ターン33 追加レビュー: `qwen-native-vision.ts`（`loadToolDisableMap` の空配列 fail-closed＋5min キャッシュ、`lockAnalysisSessionPermissions` の全 deny、session 作成と tool/id を並列、`DATA_URL_RE` の mime 一致検証、`nativeImageContext` は画像由来を未信頼データとして明示、teardown で session DELETE）→ 健全。新規確定バグなし。
- ターン34 追加レビュー: `SessionActions.tsx`（v2 は stage→commit の 2 段 revert、v1 は partID 対応、compact の 409 ロック競合リトライ（10s 期限）、`revertUserMessageToComposer` の inclusive revert、generation/busyRef ガード）→ 健全。`HeaderKebabMenu.tsx`（キーボードナビ）→ 健全。新規確定バグなし。
- ターン35 追加レビュー: `ollama/status`・`ollama/setup`（`requireAuthorized`・`maxDuration`）→ 健全。`message-parts.ts`（`isImageFilePart`・`groupImagePartsForRender`）→ 健全。`project-session-sync.ts`（manifest からの復元で worktree/copies の root 一致拒否・絶対パスを isolation の trusted ディレクトリへ束縛・他 project 所有 id の束縛拒否・idempotent）→ 健全。新規確定バグなし。
- ターン36 追加レビュー: `context-usage.ts` / `token-usage.ts`（直近 assistant ターンの token で context 使用率、`t.total ??` のフォールバック合計、`cacheHitPct` の 0 除算ガード、`cumulativeTokenUsage` は課金相当の累計と明記）→ 健全。`install-root.ts`（git 判定・インストール root 解決）→ 健全。新規確定バグなし。
- ターン37 追加レビュー: `CommandPalette.tsx` のタスク/ファイル/アクション選択（`find/file` 検索・active index の items 変化時クランプ・onMouseDown 背景で close）→ 健全。`WorkflowGraphInspector.tsx`（node 切替時の draft/selectedAttempt リセット・retry の workflowRevision・Escape close）→ 健全。新規確定バグなし。
- ターン38 追加レビュー: `favicon-badge.ts`（64px canvas の tray アイコン再現・ステータスドット・`toDataURL` の lock-down 例外 swallow）→ 健全。`WorkflowGraphEditor.tsx`（CAS ベースの `mutate`（`expectedGraphRevision`）・キーボード（Delete/Escape/Ctrl+矢印）・自動レイアウト・add_edge の kind 決定）→ 健全。新規確定バグなし。
- ターン39 追加レビュー: `api-guard-coverage.test.ts`（全 `/api` ルートの default-deny 強制・`@addons/*` 再エクスポート解決・ハンドラ数 vs guard 数・public allowlist の縮小維持・proxy の guard が params 解決前）→ 健全（網羅テストが設計として強力）。`AgentSuggestMenu` / `SlashSuggestMenu`（キーボードナビ・aria 属性）→ 健全。新規確定バグなし。
- ターン40 追加レビュー: `opencode-schema-freshness.test.ts`（spec の path 面 vs 生成 `.d.ts` の一致・`OC_PATH_TEMPLATES` が spec に存在・`VERSION` 形式）→ 健全（stale 生成ファイルによる誤検証を防ぐ）。`StatusBadge.tsx` → 健全。新規確定バグなし。
- ターン41 追加レビュー: TaskView の busy→idle 遷移（`playSessionCompleteSound`・`setDiffKey`・`refreshTask`・`refreshTodos`・`resync`・`refreshGoalLoop` を scope 一致時のみ、null は reset プレースホルダと判別）→ 健全。`useVoiceInput` の TaskView/HomeView 統合（`disabled` 時 auto-stop）→ 健全。新規確定バグなし。
- `build.log` の `useSessionStream.ts` 構文エラーは古い失敗記録で、現ファイルは正常（tsc PASS）。

---

## 対応記録（2026-08-14 修正ラウンド）

**BR-1〜10 の全項目を対応完了**（コミット群 `86a84c4` 〜 `3acac90`）:

| ID | 修正内容 | コミット |
|----|---------|---------|
| BR-1 | index.js に `removeBrokenWebBuild` import 追加（本番ビルド ReferenceError 解消） | `86a84c4` |
| BR-2 | TaskView.test.tsx の localStorage キー期待を新キーへ更新 | `b5dba3a` |
| BR-3 | マイグレーション再オープンテストを v0 アップグレード相当に修正 | `d709c75` |
| BR-4 | タブ切替で編集値が失われないことを検証するテスト追加（実害なしを実証） | `cd0c37d` |
| BR-5 | CLI 同期で browser-bridge を外部プロファイルへ漏らさない（isDistributable 注入） | `e1b4e31` |
| BR-6 | stopOpencodeOnly に opencodeUrl 等を注入し /global/dispose を復活 | `1e73334` |
| BR-7 | lock-file 内部呼び出しの引数伝搬 4 箇所を修正 + 専用テスト 6 本 | `9be2b58` |
| BR-8 | ガード網羅テストが @/lib/ re-export を追跡できるよう拡張 | `98c3ccd` |
| BR-9 | guard の不要 export 除去 + host テストは共有実装を参照 | `979b4f0` |
| BR-10 | workflow 統合テストの oc-server mock に unwrapOcData を提供 | `3acac90` |

**検証結果**: web 全体 vitest は 9 件失敗 → **321→325 ファイル / 3918 本 PASS（0 失敗）** に改善。
host 417→462 本 PASS。tsc --noEmit クリーン。修正はすべてテストで保護済み。

