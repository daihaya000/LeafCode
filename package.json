{
  "name": "leafcode",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev -- --hostname 127.0.0.1 --port 3000",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start -- --hostname 127.0.0.1 --port 3000",
    "host": "npm --prefix host start",
    "smoke": "node scripts/smoke-api.mjs",
    "smoke:browser-bridge": "node scripts/smoke-browser-bridge.mjs",
    "install:browser-bridge-mcp": "node browser-bridge/scripts/install-mcp.mjs",
    "test:encoding": "npm --prefix host run test:encoding",
    "sync:profiles": "node scripts/sync-profiles.mjs",
    "sync:profiles:check": "node scripts/sync-profiles.mjs --check",
    "sync:agents": "node scripts/agents-sync.mjs",
    "sync:agents:check": "node scripts/agents-sync.mjs --check"
  }
}
