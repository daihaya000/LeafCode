# OpenCode engine (BFF → serve) — always localhost
OPENCODE_BASE_URL=http://127.0.0.1:4096

# Host (start-webui.bat / host/src/index.js)
# OPENCODE_WEBUI_HOST=0.0.0.0   # default: reachable via VPN/LAN (phone)
# OPENCODE_WEBUI_HOST=127.0.0.1 # local-only
# OPENCODE_WEBUI_PORT=3000
