# UX — UI/UX・レスポンシブ監査インベントリ

監査日: 2026-08-15
監査方法: Playwright (headless Chrome) 実測 + 静的解析。ビューポート 1440 / 768 / 375px。

## 優先度基準

- **高**: 操作不能・操作ミス・重要なフィードバック消失を招く
- **中**: 機能は使えるが発見性・快適性を損なう
- **低**: 環境依存・見た目の問題

## 高（すぐ直す）

### UX-1. 【高】グローバル注目イベントの SSE が恒久的に 400 で失敗し、バッジがリアルタイム更新されない

- **現象**: コンソールに `/api/opencode/event => 400 Bad Request` がリトライ間隔（1〜15秒のバックオフ）で無限に出る。ブラウザで実測、計 14 件以上確認。
- **原因**: `GlobalAttentionProvider.tsx:583` が `new EventSource(apiUrl("/api/opencode/event"))` と directory パラメータを渡さず接続。`proxy.ts:174-176` の `/event` は `x-opencode-directory`（または `?directory=`）必須のため必ず 400 を返す。EventSource はカスタムヘッダを送れないため、query で渡す必要がある（`useSessionStream.ts:1810` は `apiUrl(path, { directory })` と正しく渡しており、こちらは正常）。
- **影響**: グローバル注目キュー（AttentionBadge / AttentionQueueModal）が SSE から更新されない。`syncPendingAttention()` は `onopen` 時にしか呼ばれず、400 では `onopen` が発生しないため、リアルタイムのバッジ更新が機能しない（タスク切替時の scope 変更でのみ同期される）。
- **対処の方向性**: `GlobalAttentionProvider` の接続にも directory を付与する（複数ルートが存在するため、接続元 task の directory を引くか、グローバルとして認められるパス設計を確認する）。

## 中（次に直す）

### UX-2. 【中】モバイルで「タスク開始」ボタンが 36px と小さく、タップターゲット基準 (44px) 未満

- **現象**: 375px・1440px 実測ともタスク開始ボタンは 36×36px（`h-9 w-9`、ui.tsx:351 の `icon` size）。
- **理由**: 最重要操作（送信）がモバイルで最小ターゲット。タップミスで無反応に感じる。
- **対処の方向性**: モバイルのみ `md:h-11 md:w-11` 等で拡大するか、`size="icon"` のベースを 44px に引き上げる。

### UX-2b. 【中】テーマ切替ボタンが UI に存在しない（コマンドパレット経由のみ）

- **現象**: `ThemeToggle`（ui.tsx:452）は定義されているがどの画面からも未使用。ホーム / タスク / 設定（1440px・375px 両方）を実測して `aria-label="テーマ切替"` のボタンは DOM に存在しない。テーマ変更はコマンドパレット（Ctrl+K）の「テーマ切替（ライト/ダーク）」または設定 > 全般タブからのみ可能。
- **影響**: モバイルでコマンドパレットを開くショートカットが使えない場合（物理キーボードなし）、テーマ切替への導線が設定画面に限定される。また `e2e/smoke.spec.ts` の「theme toggle switches the color scheme」テストは `getByRole("button", { name: "テーマ切替" })` を前提としており、現在の UI では失敗する（テストと実装の乖離）。
- **対処の方向性**: サイドバー下部またはヘッダーに ThemeToggle を配置するか、モバイルメニューに組み込む。テスト前提も合わせて更新する。

### UX-3. 【中】コンポーザー設定行がモバイルで大きく隠れる（横スクロール 831px）

- **現象**: 375px で設定グループのスクロール幅 831px / 表示幅 317px。`インテリジェンス` 以降（エージェント・アクセスモード・スキル・サブエージェント・ループ）はスクロールしないと見えない。768px（サイドバーあり時メイン 411px）でもスクロール幅 818px / 表示 309px。
- **理由**: Composer.tsx:320-325 の意図的な `overflow-x-auto` + 右端グラデーション（スクロールヒントあり）だが、主要設定の発見性が低い。特に「ループで継続実行」は最右端 (x=1139) にあり到達しづらい。
- **対処の方向性**: 最重要トグル（ループ）を左寄せにする、または 2 行化（sm 未満で wrap）を検討。

### UX-4. 【中】設定タブがモバイルで 11 個中 6 個スクロール外

- **現象**: 375px でタブリスト 790px / 表示 343px。「メモリ」等 6 タブがスクロール必須。グラデーションはあるが発見性が低い。
- **理由**: SettingsView.tsx:316 の `overflow-x-auto`（sm 以上で wrap）。意図的だが、頻出タブ（メモリ等）が隠れる。
- **対処の方向性**: 初期表示位置を active タブへ scrollIntoView する、またはモバイルでグリッド/ドロップダウン化を検討。

## 低（後でよい）

### UX-5. 【低】タブレット幅でサイドバーがコンテンツを圧迫

- **現象**: 768px でサイドバー 357px（保存済み幅）使用時、メイン領域が 411px に圧縮。コンポーザー設定行がさらに狭くなる。
- **理由**: サイドバーはドラッグで 180〜480px に調整可能（デフォルト 240px）。タブレット (768px) では md ブレークポイントでサイドバーが出るため、広めの保存幅だとメインが狭い。
- **対処の方向性**: タブレット幅ではサイドバーを自動で閉じる/オーバーレイ化する等の検討。

### UX-6. 【低】ログイン判定がヘッドレス環境とサーバ判定で食い違う（要再確認）

- **現象**: `curl /api/auth/session` は `loginRequired: false` を返すが、headless Chrome では初回にログイン画面が表示された（reload 後は非表示）。
- **理由**: ループバック判定がリクエスト元（Host / X-Forwarded-For）で変わるため、ヘッドレス環境特有の可能性が高い。
- **対処の方向性**: 実ブラウザ（トレイ host 経由）での再現確認が必要。再現しなければ環境依存としてクローズ。

## 正常確認済み（回帰防止メモ）

- 1440 / 768 / 375px の全ビューポートでページ全体の横スクロールは発生しない（`scrollWidth <= clientWidth`）。
- モバイルナビ：ドロワー（288px = min(18rem, 85vw)）＋バックドロップ＋Escape で正常に開閉。ドロワー外タップで閉じる。
- 設定タブは sm 以上で折り返し表示（オーバーフローなし）。
- ログイン画面は 375px で崩れなし（フォーム max-w-sm、中央寄せ）。
- Composer の選択ラベルは e2e（smoke.spec.ts）で 1024/500/400px の truncate チェック済み。
- タスク詳細画面（DeepSeek V4 Pro のモデル確認）は 375px で横スクロールなし。タイトル h1 は truncate されず全文字表示（197px）。タブ（会話/変更/ファイル/グラフ）は 224px まででビューポート内に収まる。フォローアップ textarea は 325px で収まる。タスク操作ボタンはモバイルで「再同期」+「メニューを開く」に集約され、メニューから全操作（コンテキスト圧縮・巻き戻し・Workflow変換・セッション切替・削除等）にアクセス可能。
- ダークモード：コマンドパレット経由で切替成功（`html.dark`、body 背景 #111113、テキスト #f4f4f5）。コンポーザー・フォームの表示に崩れなし。
