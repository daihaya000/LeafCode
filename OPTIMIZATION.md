# OPTIMIZATION — 大規模リファクタリング（Phase 0-7）後の最適化余地

> 対象リポジトリ: OpenCodeWebUI（`web/` + `host/` + `browser-bridge/` + `scripts/`）
> 作成日: 2026-08-14（git HEAD `b065870`）
> 目的: REFACTORING_PLAN 全 Phase（host P6 分割・goal-loop P3 分割・opencode-proxy P4 分割・
> UI P5 分割・共有 .mjs 化 P1/P6-3・Phase 7 小分割）の実施後に残る**構造・整理・非効率**の余地を
> 記録する。バグは [BUG.md](./BUG.md)（BR-1〜10）、機能改善計画は [IMPROVEMENT.md](./IMPROVEMENT.md)
> を参照。本ファイルは**発見記録のみ**（修正は別途）。

---

## 調査の要約

リファクタリングは概ね成功しており、`host` 417 本 / browser-bridge 91 本 / goal-loop 84 本 /
opencode-proxy 65 本 / SettingsView 32 本 / git 77 本 / memory 44 本 / workflow 19 本のテストと
`tsc --noEmit` が PASS する。共有 .mjs 化（P1 / P6-3）は web・host・CLI の 3 層で重複解消が
一貫して完了している。残る最適化余地は、**分割の途中で止まった作業**・**分割で生じた残留物**・
**テスト不足の分割モジュール** の 3 系統に分類できる。**OR-1〜23（高 2 / 中 3 / 低 18）**。
バグは BR-1〜10（高 1 / 中 7 / 低 2、うち web 全体 vitest の 9 件失敗を説明）を BUG.md に記録。

## 対応記録（2026-08-14）

**OR-1〜23 の全項目を対応完了**（調査→修正は同日実施、コミット群 `2e1ea3e` 〜 `c79ff24`）:

- **構造・整理**: OR-1（index.js 未使用 import 13 個削除）/ OR-3（goal-loop 循環 import 解消）/
  OR-4（useSessionStream 純ロジック抽出）/ OR-5（モデル選択 state 共通フック化）/
  OR-6（テスト専用 export 明示）/ OR-7（taskkill ヘルパー集約）/ OR-8（TaskView JSX 抽出）/
  OR-9（spawnOpencode 分割 95→36 行）/ OR-11（resolveHostControlUrl の dataDir 追従）/
  OR-12（index.js DATA_DIR 共有化）/ OR-13（createSettingSync 3 モジュール移行）/
  OR-15（memory-server 共有 dataDir 化）/ OR-16（specs バッジ 26 本付与）/
  OR-17（CI Node 24 統一）/ OR-19（lib→components 逆依存解消）/ OR-21（キャッシュ値一元化）/
  OR-10（user_version トランザクション内移動）
- **テスト追加**: OR-2（分割モジュール 44 本）/ OR-14（goal-state 10 本）/ OR-18（write_conflict
  blockedNodeIds 検証）/ OR-20（useSubscriptionOAuth 5 本）/ OR-22（PermissionGhostSelect 6 本）/
  OR-23（task-cost 13 本）
- **検証結果**: web 325 ファイル 3918 本 PASS（1 skipped）/ host 462 本 PASS / browser-bridge 91 本
  PASS / tsc --noEmit クリーン
- **並行セッション対応**: 別エージェントのコミット（`48fa20a` / `c305109`）を検出し、自分が
  作っていない差分は触らず残置。OR-8 は別エージェントが ComposerStatusMessages をインライン化
  したため、キュー待ち表示を別ブロックで抽出して対応

---

## 高（優先的に片付ける）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **OR-1** | **`host/src/index.js` に 13 個の未使用 import が残存（分割の残留物）** | `host/src/index.js` の import のうち、ファイル内で一度も参照されないものが 13 個ある: `webRestartDelay`（`:25`）、`parseListeningPids`（`:30`）、`runNetstat`（`:38`）、`runPowerShell`（`:39`）、`CADDY_LOOPBACK_URL_RE`（`:55`）、`isPlaceholderHost`（`:56`）、`backfillLockCreationTime`（`:72`）、`readLockPid`（`:74`）、`listChildPids`（`:86`）、`reapInheritedHolders`（`:87`）、`isWindowsPeExecutable`（`:117`）、`npmOpencodeSiblingExe`（`:118`）、`pickOpencodePath`（`:119`）。import 行のみで使用箇所ゼロ（`split(name).length===1` で確認）。分割時にコードをモジュールへ移設した際、index.js 側の import を削除し忘れたもの。`host` は lint 設定が無い（`host/package.json` に lint script なし・eslint config なし）ため未検出。→ import 文の整理（lint 導入または手動削除） |
| **OR-2** | **分割モジュール 6 個に専用テストが無い（分割の検証不足）** | Phase 6 で新設された `host/src/` の 6 モジュールに同名の `.test.js` が存在しない: `lock-file.js` / `npm-cli.js` / `opencode-upgrade.js` / `port-scanner.js` / `process-info.js` / `windows-integration.js`。`index.test.js` からの直接 import も無い（実測）。`port-plan.test.js` 等の既存テストから間接的に呼ばれる関数はあるが、分割で新設された関数の大半（`getCommandLineMap` / `firewallRuleExists` / `parseChildPidOutput` 等）はテスト非カバー。特に `removeBrokenWebBuild` の import 漏れ（BUG.md BR-1）がテストで検出されなかった一因は、`index.test.js` が `spawnWeb` / `buildWebProduction` を一切呼ばないため。→ 分割モジュールへ単体テストを追加（少なくとも export 関数の特性検査） |

---

## 中（次に取り組む）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **OR-3** | **goal-loop 分割で実行時循環 import が生じた（goal-loop ↔ goal-scheduler ↔ goal-state ↔ goal-memory-hook）** | `goal-loop.ts` → `./goal-scheduler`（`goalLoopTestSeams` / `processLoop` を re-export）→ `./goal-state` → `./goal-memory-hook`（`scheduleAutoExtractAfterGoalCompleted`）→ `./goal-loop`（type-only）。加えて `goal-scheduler.ts` → `./goal-loop`（`autoCompactGoalLoop`）の実行時参照があり、`goal-db.ts` → `./goal-loop`（`applyAssistantResult` 等 4 関数）も実行時。`import type` を除く**実行時循環**が存在する。現在は関数内参照のみで TDZ にはならず、テスト 84 本 PASS で動作は健全（`e1ec7b1` は「循環 import 許容」と記録）。ただし分割の目的（1 モジュール 1 責務・依存の明確化）を部分的に損ない、将来のモジュール初期化順変更で TDZ の危険が残る。→ `goal-memory-hook.ts` は type-only で済むため実質循環なしだが、`goal-loop`↔`goal-scheduler` の相互参照（`autoCompactGoalLoop` と `processLoop`）を一方の向きに揃えられるか再検討 |
| **OR-4** | **P5-a（`useSessionStream.ts` の分割）が完了記録と実態で乖離（2290 行のまま未分割）** | REFACTORING_PLAN「完了記録」は Phase 5 を「✅」とするが、P5-a の対象 `web/src/lib/useSessionStream.ts` は分割されず **2290 行のまま**（直近の変更は `8a2daf1` unwrap 統合のみ）。計画では「798 行以降の約 1500 行を `session-sse.ts` / `session-actions.ts` へ切り出し」だった。**分割可能性の実測（ターン 31）**: `useSessionStream` フック（799-2290 行・約 1492 行）は useRef 20 / useState 1 / useEffect 5 / useCallback 14 / useMemo 1 を持つ。useCallback 14 個は **SSE/接続系 6**（acquirePreferRest / releasePreferRest / clearMutationTimers / runResync / resync / startMutationElapsed）と **アクション系 8**（sendPrompt / sendCommand / refreshTodos / abort / replyPermission / replyQuestion / rejectQuestion / clearPendingDescendants）に分類可能で、P5-a の `session-sse.ts` / `session-actions.ts` 2 分割計画と構造的に一致する。**分割の主な障壁は 20 個の ref が両グループにまたがって共有される点**（abortingScopesRef / preferRestHoldRef / safetyNetTimerRef 等の受け渡し設計が必要）。→ 完了記録を P5-a 未実施に訂正するか、ref 共有の設計（Context 化 or フック分割）を含めて分割を再開する |
| **OR-5** | **`useTaskModelConfig` と HomeView の同型 state の共通化が未実施（P5-b の将来課題）** | `web/src/components/task/use-task-model-config.ts` のコメントに「HomeView の同型 state と共通化する候補」と明記されたまま、HomeView 側のモデル/プロバイダ state（13 個前後）とは分離されたまま。REFACTORING_PLAN P5-b 表の「HomeView の同型 13 state と共通化候補（`lib/hooks/` へ）」も未実施。二重実装の温床として残る。→ `lib/hooks/` への共通化 or 完了記録の訂正 |

---

## 低（後でよい）

| ID | 要約 | 根拠 / 影響 |
|----|------|-------------|
| **OR-6** | **control-server 等の「テスト専用 export」が index.js から見ると未使用として残る** | `control-server.js` の `matchControlRoute` / `isLoopbackHostHeader` / `createRevocationStore` / `createControlRequestHandler`、`log-file.js` の `formatLogLine` / `shouldRotate` / `rotateFilePath` / `rotateFilePaths`、`log-buffer.js` の `pickEvictionIndex` / `resetLogBuffer`、`process-stop.js` の `parseChildPidOutput` / `disposeAuthHeaders` 等は、`index.js` からは参照されずテスト専用。公開 API を肥大させている。実害はないが、テスト専用 export を明示（`export` を維持しつつコメント化 or テスト側からモジュール内部関数を直接扱う設計）する余地あり |
| **OR-7** | **`host/src/index.js` の `execFileAsync`（`promisify(execFile)`）と `execFileSync` / `execSync` が混在** | `execFileAsync` の使用は 2 箇所のみで、`execFileSync` 3 箇所・`execSync` 5 箇所と同期/非同期が混在する。分割時の意図なき残存の可能性。呼び出し箇所の整理（同期を非同期へ統一 or 使わない方の削除）で起動処理のブロッキングを減らせる余地あり |
| **OR-8** | **`TaskView.tsx` が依然 5543 行で、分割は hooks 抽出（P5-b）のみでコンポーネント本体の分離が未完** | IMPROVEMENT.md の当初指摘（5518 行・hooks 262 個）から hooks 抽出で state 数は減ったが、ファイル本体は 5543 行のまま。P5-b の hooks 6 個抽出は完了したが、描画 JSX の分割（サブコンポーネント化）は Phase 5 のスコープ外として残っている。→ 表示専用ブロックの抽出を次フェーズで検討 |
| **OR-9** | **`host/src/index.js` の主要関数がまだ大きい** | `handleExistingInstance`（`:1289-1422` 約 134 行）、`spawnOpencode`（`:650-777` 約 128 行）、`buildTrayMenu`（`:2022-2096` 約 75 行）等が残る。P6-a は「トレイ / 終了処理は systray とモジュール状態の密結合のため分割保留（リスク大・利益小）」と記録済みだが、`spawnOpencode`（CLI 探索と起動）と `opencode-upgrade.js` の責務境界は分割余地がある |
| **OR-10** | **`runSchemaMigrations` の `PRAGMA user_version` スタンプがトランザクション外** | `web/src/lib/db.ts:191`。各マイグレーションの SQL は `db.transaction(() => {...})()` で囲まれるが、`db.pragma("user_version = N")` はトランザクションの外（後）で実行される。SQL 成功→スタンプ失敗でクラッシュした場合、次回 open で同じマイグレーションが再実行される（各 step がカラム存在チェックで冪等なため安全・BR-3 参照）。実害はないが、「マイグレーション SQL と version スタンプを同一トランザクションに含める」方が中断後の一貫性が明確になる。→ `db.pragma` をトランザクション内に移動する or 現状の冪等性をドキュメント化する |
| **OR-11** | **`resolveHostControlUrl` の host-control.json 参照が D2（dataDir 統一）に追従しておらず、非 win32 でカスタムポートが機能しない** | `scripts/lib/host-control.mjs` の `resolveHostControlUrl` は host-control.json を **`env.APPDATA/opencode-webui` 配下**（`:48-49`）から読む。一方 D2（REFACTORING_PLAN 決定）で dataDir は非 win32 で **`~/.opencode-webui`** に統一され（`scripts/lib/data-dir.mjs`）、host 側も `host/src/index.js:157` で `join(DATA_DIR, 'host-control.json')` に書く。したがって **非 win32（APPDATA が無い）では host が `~/.opencode-webui/host-control.json` にポートを書いても、web / guard の `resolveHostControlUrl` はそれを読まず既定ポート `http://127.0.0.1:18765` にフォールバック**する（実測: APPDATA 無しで `{port:9999}` を書いても `http://127.0.0.1:18765` を返す）。win32 では APPDATA が存在するため整合。このリポジトリは Windows 中心（EXE ランチャー・`CONTROL_PORT` 既定 18765）で実害は限定的だが、D2 で「パス解決を 1 実装に」としながら host-control URL だけが APPDATA 基準のまま残る**非対称**。→ `resolveHostControlUrl` も `dataDir()`（`~/.opencode-webui`）基準に揃える、または非 win32 のフォールバックを明文化する |
| **OR-12** | **`host/src/index.js` の `DATA_DIR` が共有 `dataDir()` を使わず `join(process.env.APPDATA, 'opencode-webui')` のまま（P1-c の共有化漏れ）** | `host/src/index.js:137` は `const DATA_DIR = join(process.env.APPDATA, 'opencode-webui')`。一方 P1-c / D2 で正本化された `scripts/lib/data-dir.mjs` の `dataDir()` は win32 で `%APPDATA%\opencode-webui`、**非 win32 で `~/.opencode-webui`** を返し、host の分割モジュール `audit-log.js` / `auth-config.js` / `auth-store.js` は既に `dataDir()` を import している。**`index.js` だけが共有化に追従していない**。win32 では `%APPDATA%` が存在するため両者は一致するが、**非 win32 では `join(undefined, 'opencode-webui')` となり、`LOCK_FILE`（`:156`）`/ `CONTROL_FILE`（`:157`）/ `BROWSER_BRIDGE_PAIRING_FILE`（`:158`）` が auth 系（`~/.opencode-webui`）と**別ディレクトリ**に分かれて動作が分裂する**。Windows 中心のため実害は限定的だが、P1-c の「dataDir 解決を共有化」が index.js に未適用なのは構造上の穴。→ `index.js` も `dataDir()` を import して `DATA_DIR` を置き換える |
| **OR-13** | **createSettingSync（P4-d）への移行が 2 モジュールのみで、単一キー設定 3 モジュールが write queue なしでサーバ PUT を直接行う** | REFACTORING_PLAN P4-d（IMPROVEMENT 2-2）は「設定同期パターン（write queue 3 箇所 / localStorage 15+ ファイル）の共通化」を計画した。`web/src/lib/setting-sync.ts` の `createSettingSync`（単一キー設定向けの write queue 直列化ミラー）が作成され、`default-model.ts` / `generation-model.ts` の 2 モジュールのみが移行した。**残る単一キー設定**（`hang-timeout.ts` / `token-saving-settings.ts` / `sidepanel-settings.ts`）はサーバへの `sendJson("PUT", ...)` を **write queue なし**で直接呼ぶ（`hang-timeout.ts:58` の `syncHangTimeoutToServer` 等。機械確認で PUT は hang-timeout x1 / token-saving x2 / sidepanel x1）。連続変更で PUT が並走した場合に**順序逆転**（古い値が後から届きサーバに残る）の理論上のリスクがある。`auto-settings.ts` のキー別 `autoSettingWriteQueues`（`Map<AutoSettingKey, Promise>`）は複数キー向けで、createSettingSync（単一キー向け）と用途が異なるため**移行不要**（正当）。→ 単一キーの 3 設定（hang-timeout / token-saving / sidepanel-width）を `createSettingSync` へ移行して write queue 直列化を適用する |
| **OR-14** | **P3-b 手順 4 の「goal-state の DB 依存注入による純化」とテスト分離が未実施（完了記録と実態の乖離）** | REFACTORING_PLAN P3-b は「手順 4 の『DB 依存の注入による純化』だけが構造変更なので、移動 → 純化を**別コミット**に分ける」「完了後: テストを `goal-state.test.ts`（純関数）/ `goal-db.test.ts`（DB モック）へ分離」と計画した。しかし実装では **`goal-state.ts` が 7 箇所で `getDb()` を直接呼ぶ**（`:162` `applyAssistantResult` / `:242` / `:299` / `:324` / `:352` / `:383` / `:392`）DB 依存のまま、**`goal-state.test.ts` / `goal-db.test.ts` / `goal-scheduler.test.ts` はいずれも存在しない**（機械確認）。goal 系テストは `goal-loop.test.ts` / `goal-loop.integration.test.ts` のみで、純関数としての単体テストが欠けている。動作は goal-loop テスト 84 本 PASS で健全だが、**「分割の目的（純関数化・単体テスト可能化）」が手順 4 で未達**のまま完了記録（`040a75a` Phase 3 ✅）が残る。これは OR-4（useSessionStream 未分割）と同じ「完了記録と実態の乖離」パターン。→ `applyAssistantResult` 等を「状態遷移結果を返す純関数 + 呼び出し側で DB 書込」に分離し、`goal-state.test.ts` を追加する。または完了記録を「純化未実施」に訂正する |
| **OR-15** | **`browser-bridge/mcp/memory-server.mjs` の `resolveDataDir` が共有 `data-dir.mjs` を使わず独自実装（P1-c/D2 の共有化漏れ）** | `browser-bridge/mcp/memory-server.mjs` の `resolveDataDir`（`:44-57`）は D2 の値（win32: `%APPDATA%\opencode-webui` / 非 win32: `~/.opencode-webui`）を**手書きで反映**しているが、共有 `scripts/lib/data-dir.mjs` の `dataDir()` を import していない。**差**: 共有 `dataDir()` は win32 フォールバックで `join(homedir(), 'AppData', 'Roaming')`（Node の `os.homedir()`）を使うのに対し、memory-server は `env.HOME || env.USERPROFILE || ''`（`:53`）を使う。`HOME` / `USERPROFILE` が両方未設定の環境では `path.join('', 'AppData', 'Roaming')` になり**パス先頭が空文字**になる。実運用では host が `LEAFCODE_DATA_DIR` を env 注入する（`:45`）ためこのフォールバックは最終手段で実害は限定的だが、D2 の「パス解決を 1 実装に」という方針に反する**重複実装**。なお P1-d のメモリロジック共有化（`shared/memory-schema.mjs` の `isMemoryKind` / `memoryContentError` / `inspectMemoryContent` / `toFtsPhrase` を memory-server が使用）は**完了済み**。→ `resolveDataDir` を共有 `data-dir.mjs` の `dataDir()` に置き換える（`LEAFCODE_DATA_DIR` の env チェックは wrapper で維持） |
| **OR-16** | **Phase 7 8-5 の specs バッジ運用が部分的にしか適用されていない（28 本中 26 本がバッジなし）** | `docs/specs/README.md`（ba5664b）は「各仕様の冒頭に実装ステータスバッジを**必ず**記載する」「新規仕様追加時は最初からバッジ行を付ける」と運用ルール化したが、実際にバッジが付与されているのは **2 本のみ**（`bat-encoding-safety.md` / `host-log-viewer.md`）。**残り 26 本はバッジなし**（機械確認: `実装ステータス: [✅🔶⬜]` パターンで 28 本中 2 本のみ一致）。specs/README の「仕様一覧（28 本）」はバッジ行へのリンクを提供するが、各ファイルにバッジが無いため、実装ステータスの追跡が README の記述だけで完結せず、ルールの趣旨（実装状況の可視化）が未達。8-5 は「README + 2 本にバッジ付与」で開始した段階で、残りは未実施。→ 残り 26 本にバッジ行を付与する（各 spec の実装状況に応じて ✅/🔶/⬜ を判定・付与） |
| **OR-17** | **CI の Node バージョンがジョブ間で不統一（encoding-check は Node 20、ci.yml は Node 24）** | `.github/workflows/encoding-check.yml` の bat-encoding ジョブは `node-version: "20"`（`npm run test:encoding` + `npm --prefix host test` を実行）、`.github/workflows/ci.yml` の web / e2e-smoke ジョブは `node-version: 24`。host の依存は systray2 / ws のみで engines 指定なし、host/src に Node 20 で未サポートの API（`Promise.withResolvers` / `Array.fromAsync` / `Object.groupBy` / `structuredClone` / `using`）は**使用されていない**（機械確認・検出は全てコメント内の単語 "using"）ため、現在は Node 20 でも host テストは通る。ただし**将来 host が Node 24 専用 API を使うと encoding-check だけが壊れる**リスクがあり、host をテストするジョブ（encoding-check と ci.yml の web は web 側のみで host テスト無し）でバージョンが食い違う。→ encoding-check.yml も node-version を 24 に揃える |
| **OR-18** | **workflow-graph-runtime の write_conflict テストが `blockedNodeIds` の内容を検証していない（BH-10 修正の本質がテスト非保護）** | `web/src/lib/workflow-graph-runtime.ts:87-95` の write_conflict 処理（BH-10 修正）は `writableReady.length > 1` のとき全 writable node を `blockedNodeIds` に push し `pauseReason = "write_conflict"` を設定する。`workflow-scheduler.ts:667` は `graphRuntime.blockedNodeIds.includes(node.node_key) && graphRuntime.pauseReason` のとき `pauseWorkflowForAttempt` を呼び、**blockedNodeIds への追加が無いとワークフローが無言停滞する**（BH-10 の本質）。しかし `workflow-graph-runtime.test.ts` の "prevents parallel writable claims"（`:61-67`）は `pauseReason === "write_conflict"` と `readyNodeIds === []` のみを検証し、**`blockedNodeIds` が a / b を含むことを検証していない**。そのため将来 blockedNodeIds への追加が漏れてもテストは通り続け、BH-10 の回帰を検出できない。→ write_conflict テストに `expect(result.blockedNodeIds).toEqual(["a","b"])` 相当の検証を追加する |
| **OR-19** | **lib → components の逆依存が 1 箇所（`opencode-extensions/agents.ts` → `components/settings/agent-utils`）** | `web/src/lib/opencode-extensions/agents.ts:5-8` は `import type { AgentDto as BaseAgentDto, AgentScope } from "@/components/settings/agent-utils"` で、**lib が components を import** する。`agent-utils.ts`（186 行・`parseAgent` / `filterAgents` / `groupAgents` / `scopeLabel` 等 9 export）は React / JSX を import しない**純ロジック**で、agents.ts からの import は **type-only** のため、実行時・バンドルへの影響は**無い**（サーバー側での UI 混入なし）。ただし「lib が components を参照する」モジュール境界の配置不整合は残る。この構造はリファクタリングで新たに導入されたものではなく分割前から存在（agent-utils は `03301ae` で最後に変更、components/settings に純ロジックが置かれたまま）。→ 純ロジック（agent-utils）を `lib/opencode-extensions/` 側へ移すか、少なくとも type 定義を lib に置く配置整理の余地あり（将来 agent-utils に値 export が増えた場合にレイヤー逆転が顕在化するリスクを予防） |
| **OR-20** | **1-3b の共通フック `useSubscriptionOAuth` に専用テストが無く、Claude 設定経路がテスト非カバー** | `web/src/components/settings/use-subscription-oauth.ts`（234 行・`useSubscriptionOAuth` + 2 型）は P5-d（IMPROVEMENT 1-3b）で ClaudeSubscriptionAuth / OpenAISubscriptionAuth の状態機械・ポーリング・認証開始を 1 実装に集約した共通フック。両カードは config でプロバイダ差を注入（Claude: `findMethodIndex(method.type === "oauth")`、OpenAI: `findMethodIndex(isBrowserOAuth)`）。**共通フックの専用テストは存在せず**（`use-subscription-oauth.test.ts` なし）、ClaudeSubscriptionAuth.test.tsx も**分割前から存在しない**（a156c48^ でも ClaudeSubscriptionAuth.tsx のみ）。`OpenAISubscriptionAuth.test.tsx`（5 本 PASS）のみが共通フックのロジックを間接検証する。つまり**OpenAI 設定経路はテスト済みだが、Claude 設定経路（`type === "oauth"` の findMethodIndex / authorizeEndpoint）はテスト非カバー**。共通フックの分岐（ポーリング最大 90 回・pageVisible 停止・generation ガード）は OpenAI テストで検証されるが、config 注入の差が正しく機能するかは Claude 側で未検証。→ 共通フックの単体テスト or ClaudeSubscriptionAuth 側のテスト追加（findMethodIndex の差を両経路で検証） |
| **OR-21** | **P4-c（9-1）の「値を 1 箇所に集約」が「デフォルト定数化 + コメント同期」で部分解消のまま（http-cache と stale-cache に別定義）** | REFACTORING_PLAN P4-c は「4 系統（http-cache / stale-cache 14 policy / sw.js v6 / proxy 内部）の値の二重管理を解消。作業は『値を 1 箇所に集約』が主」と計画した。実際の実装（`5f133af`）は `web/src/lib/http-cache.ts` に `DEFAULT_MAX_AGE_SECONDS = 30` / `DEFAULT_SWR_SECONDS = 600` を、`web/src/lib/stale-cache.ts` に `DEFAULT_FRESH_MS = 30_000` / `DEFAULT_STALE_MS = 600_000` を**それぞれ別定義**し、「Mirrors the client default in stale-cache.ts…keep them in step」という**コメントで同期を促すだけ**。現在の値は一致している（30s / 600s）が、**「1 箇所に集約」は未達**で、将来片方のデフォルトを変更したときコメントに頼らない限り他方が追従しないドリフトリスクが残る。`sw.js`（134 行）は鮮度 TTL を持たず（ビルド ID ベースの wipe が主）ため実質 2 系統の二重管理。→ デフォルト鮮度を共有モジュール（例: `scripts/lib/` or `web/src/lib/cache-freshness.ts`）に集約し、http-cache / stale-cache から import する |
| **OR-22** | **1-3 の `PermissionGhostSelect` 共通化後、3 ラッパー（AccessModeSelect / SkillPermissionSelect / SubagentPermissionSelect）に専用テストが無い** | `web/src/components/PermissionGhostSelect.tsx`（54 行・P5-d / IMPROVEMENT 1-3）は固定 options の GhostSelect ラッパーを共通化し、AccessModeSelect / SkillPermissionSelect / SubagentPermissionSelect（各 37-40 行の薄いラッパー）が options + 表示メタデータ（ariaLabel / icon / tone）を渡す。共通化は健全（`ACCESS_MODE_OPTIONS` は `{value,label,title}` で `PermissionOption` と型互換）。**ただし 3 ラッパーと PermissionGhostSelect に専用テストは無い**（`PermissionGhostSelect.test.tsx` / `AccessModeSelect.test.tsx` 等 4 ファイル全て不在）。間接カバレッジとして `TaskView.test.tsx` が 3 ラッパーを参照するが、BR-2 で 2 件失敗中のため完全ではない。各ラッパーは薄くバグが入りにくい構造だが、共通化後の「1 実装の挙動（valueLabel フォールバック・title 表示・tone）」は直接テストされていない。→ 3 ラッパーの options と表示を検証する軽量テストを追加（例: AccessModeSelect が ACCESS_MODE_OPTIONS を正しく渡し value に応じ tone/icon が変わる） |
| **OR-23** | **P3-c の `task-cost.ts` 分割後、コスト計算関数に専用テストが無い（task-service.test.ts 経由の間接カバーのみ）** | P3-c（`f2432e1`）でコスト推定を `task-service.ts` から `task-cost.ts`（143 行・6 関数 `estimateSessionCost` / `sessionUsageFingerprint` / `hasPositiveTokenUsage` / `exactMessageCost` / `estimateSessionCostWithCache` / `__clearSessionEstimateCacheForTest` + 2 定数）へ分離した。`task-cost.test.ts` は存在せず、`task-service.test.ts`（29 本 PASS）は `getTaskCost` を 5 回テストするが、`estimateSessionCostWithCache` / `exactMessageCost` / `SESSION_COST_FETCH_CONCURRENCY` を直接参照しない（機械確認）。`getTaskCost` は `reportedCost`（OpenCode 報告）優先で、無ければ `estimateSessionCostWithCache` にフォールバックするため、**フォールバック経路でのみ間接カバー**される（報告コストがあるケースは推定ロジック非実行）。`model-pricing-registry.test.ts`（3 本）も task-cost を直接参照しない。つまり**コスト推定ロジック（exactMessageCost の精度・キャッシュ上限・並列制御）は直接テストされていない**。P3-c の「テスト 476 行があるため低リスク」判断は既存テストの間接カバーを指すが、移設後は専用テストが無い。→ `task-cost.test.ts` に `exactMessageCost` / `estimateSessionCostWithCache`（キャッシュ・TTL・並列）の単体テストを追加 |